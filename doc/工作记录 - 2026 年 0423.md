# 工作记录 - 2026 年 0423

## 改动背景

任务详情右侧面板在窄宽度下，子任务区域的标题、截止时间、负责人和“详情”按钮会挤在一行里互相顶开，和你给的截图一致，表现出来就是布局发乱、信息不成块。

这次处理重点不是改业务逻辑，而是把右侧详情面板里子任务行的排版收紧，让长标题和多人负责人都能在抽屉宽度不足时正常收缩、换行。

## 主要修改点

### 1. 调整子任务行结构，拆出独立主内容容器

- 文件：
  - `src/components/TaskDetailPanel/index.tsx`
- 处理内容：
  - 给每条子任务新增 `subtask-main` 容器。
  - 把标题按钮和右侧元信息操作区统一收进这个容器里。
- 原因：
  - 之前是标题和右侧按钮直接平铺在外层行容器里，长标题一出现就会和右侧操作区抢宽度，抽屉一窄就容易乱。

### 2. 收紧子任务行样式，支持窄抽屉换行

- 文件：
  - `src/components/TaskDetailPanel/index.less`
- 处理内容：
  - 子任务行从垂直居中改成顶部对齐。
  - `subtask-main` 改成可换行的 `flex` 容器。
  - 标题按钮改成可收缩布局，保证长标题优先截断。
  - 右侧 `subtask-meta` 改成允许换行，并限制最大宽度。
  - 负责人按钮允许收缩到容器宽度内，不再用固定上限把整行撑爆。
- 原因：
  - 这块的根因是收缩规则不完整，不是某个按钮单独样式有问题，所以需要把标题区和操作区一起改成可收缩、可换行。

### 3. 补子任务布局回归测试

- 文件：
  - `tests/task-detail-subtask-layout-regression.test.mjs`
- 处理内容：
  - 新增断言，确保子任务行存在独立主内容容器。
  - 新增断言，确保子任务主内容区支持换行。
  - 新增断言，确保右侧元信息区和负责人按钮允许收缩。
- 原因：
  - 这类问题很容易在后续继续堆按钮或改样式时回退，所以要用文本回归把关键结构钉住。

### 4. 同步过期的子任务负责人回归断言

- 文件：
  - `tests/task-detail-subtask-actions-regression.test.mjs`
- 处理内容：
  - 把旧的“单负责人 string 参数”断言同步成当前“多负责人 string[] 参数”的断言。
  - 保持“子任务行必须有显式负责人入口”的检查不变。
- 原因：
  - 当前任务体系已经是多负责人逻辑，旧测试还在校验过时签名，继续保留会干扰真实回归结果。

## 异常处理

- 这次没有改子任务创建、截止时间更新、负责人更新的业务流程，只动了结构和样式。
- 没有动评论区、附件区、历史记录区，避免把修复范围扩散到无关区域。
- 旧测试失败不是这次引入的问题，而是测试断言和现有多负责人实现不一致，这次一并同步。

## 涉及模块

- `src/components/TaskDetailPanel/index.tsx`
- `src/components/TaskDetailPanel/index.less`
- `tests/task-detail-subtask-layout-regression.test.mjs`
- `tests/task-detail-subtask-actions-regression.test.mjs`

## 可能影响范围

- 任务详情右侧面板里的子任务列表排版
- 窄宽度抽屉下的子任务标题截断和操作按钮换行
- 多负责人子任务在详情面板里的展示稳定性

## 测试情况

### 已执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-subtask-layout-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-subtask-actions-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-drawer-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-rich-input-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过

## 本轮补充记录：表格省略内容悬浮显示完整值

### 改动背景

你这次反馈的是表格字段默认宽度放不下完整字段名和值时，虽然已经会省略显示，但用户无法直接看到完整内容。

目标是保持当前默认列宽不变，同时在鼠标滑过省略内容时用黑底白字浮窗显示完整内容。

### 主要修改点

### 1. 给表头省略内容补完整标题 Tooltip

- 文件：`src/components/TaskTable/index.tsx`
- 处理内容：
  - 表头内容统一包 Antd `Tooltip`。
  - Tooltip 使用 `color="#000"` 和白色文字。
  - 新增节点文本提取逻辑，能从带图标表头和自定义字段表头里提取完整文案。
- 原因：
  - 自定义字段表头可能是很长的字段名，默认列宽下会省略；鼠标滑过时需要看到完整字段名。

### 2. 给省略的单元格值补完整值 Tooltip

- 文件：`src/components/TaskTable/index.tsx`
- 处理内容：
  - 新增 `renderOverflowTooltip`，统一输出黑底白字 Tooltip。
  - 自定义字段文本值悬浮显示完整值。
  - 自定义字段标签值悬浮显示完整标签。
  - 任务 ID、分配人、关注人、创建/完成/更新时间等普通文本值也统一悬浮显示完整值。
  - 开始/截止日期悬浮显示完整日期。
  - 优先级标签悬浮显示完整优先级文案。
- 原因：
  - 默认宽度无法展示完整内容时，单元格仍按省略号显示，但完整内容可以通过悬浮浮窗查看。

### 3. 补表格 Tooltip 回归测试

- 文件：`tests/task-table-resizable-layout-regression.test.mjs`
- 处理内容：
  - 校验表头省略内容使用黑底白字 Tooltip。
  - 校验省略文本统一走黑底白字 Tooltip helper。
  - 校验自定义字段文本、标签、日期、优先级都接入完整内容 Tooltip。
  - 校验普通文本字段值也接入完整内容 Tooltip。
  - 校验表头 Tooltip 能从嵌套节点提取文案。
- 原因：
  - 防止后续继续改列宽或单元格渲染时，只保留省略号，漏掉完整内容浮窗。

### 异常处理

- 这次不改变当前列宽策略：任务标题默认 `407`，其他字段默认 `120`。
- 不改变字段编辑、日期选择、优先级选择和自定义字段保存逻辑，只补展示层的悬浮完整内容。

### 涉及模块

- `src/components/TaskTable/index.tsx`
- `tests/task-table-resizable-layout-regression.test.mjs`

### 可能影响范围

- 表格表头字段名悬浮提示
- 表格日期、优先级、自定义字段值的悬浮提示
- 默认列宽下省略内容的可读性

### 测试情况

## 本轮补充记录：移除字段配置里的“必填”开关

### 改动背景

你这次要处理的是任务清单字段配置弹窗里，“是否必填 / 开启后该字段必须填写”这个开关要直接去掉，不再让用户在前端配置这个能力。

这次按最小改动处理，只收口字段配置弹窗里的创建和编辑 UI，不去扩散改任务表格、自定义字段展示和后端字段结构，避免把影响面放大。

### 主要修改点

### 1. 删掉新的字段编辑弹窗里的“必填”开关

- 文件：
  - `src/components/CustomFieldEditorModal/index.tsx`
- 处理内容：
  - 删除“是否必填”这一行表单。
  - 删除“开启后该字段必须填写”的提示文案。
  - 删除本地 `required` 状态和对应的 `Switch` 依赖。
  - 新建字段和编辑字段提交时，不再从这个弹窗主动传 `required`。
- 原因：
  - 你要的是字段配置时彻底不显示这个开关，所以直接从当前主入口弹窗里删掉最稳，改动也最小。

### 2. 同步删掉旧字段管理弹窗里的“必填”开关

- 文件：
  - `src/components/CustomFieldsModal/index.tsx`
- 处理内容：
  - 删除旧弹窗里的“是否必填”表单项。
  - 删除 `Switch` 依赖和表单里的 `required` 字段定义。
  - 清掉编辑已有字段时残留的 `required` 表单赋值。
  - 保存时不再从旧弹窗提交 `required`。
- 原因：
  - 虽然当前主页面主要走新弹窗，但侧边栏里还保留了旧入口；一起收口能避免别的入口还看到同一个开关。

### 3. 补“必填开关移除”源码回归测试

- 文件：
  - `tests/custom-field-required-toggle-regression.test.mjs`
- 处理内容：
  - 增加断言，要求新弹窗源码里不能再出现“是否必填”和提示文案。
  - 增加断言，要求新旧两个弹窗都不再依赖 `Switch`。
  - 增加断言，要求旧弹窗里不能再渲染 `required` 表单项。
- 原因：
  - 这类 UI 开关很容易在后续复制旧代码时被带回来，所以先用源码回归把关键点钉住。

### 异常处理

- 这次没有改后端接口类型里的 `required` 字段定义，保持接口兼容，避免把前端 UI 调整扩散成接口协议调整。
- 已有字段列表里如果后端返回 `required=true`，旧的展示标签目前仍会按已有数据展示“必填”，这次只处理“字段配置时删除开关”的需求，不扩散改历史展示逻辑。

### 涉及模块

- `src/components/CustomFieldEditorModal/index.tsx`
- `src/components/CustomFieldsModal/index.tsx`
- `tests/custom-field-required-toggle-regression.test.mjs`

### 可能影响范围

- 任务表格里的新建自定义字段弹窗
- 侧边栏里的旧版自定义字段管理弹窗
- 自定义字段创建和编辑时的前端提交参数

### 测试情况

### 已执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/custom-field-required-toggle-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/custom-field-editor-update-endpoint-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过

## 本轮补充记录：字段配置右侧菜单增加“选择已创建的字段”

### 改动背景

你这次补充的新需求是：在字段配置浮层里，鼠标移到“添加自定义字段”后，右侧字段类型菜单底部也要补一个“选择已创建的字段”入口。

这个入口点击后，不是留在当前双列菜单里继续选，而是要直接打开完整的“已创建字段”弹窗，效果跟你给的第三张图一致。

### 主要修改点

### 1. 给字段配置右侧类型菜单补入口

- 文件：
  - `src/components/TaskTable/index.tsx`
- 处理内容：
  - 在 `fieldConfigPanel` 右侧类型菜单底部新增一个链接样式入口。
  - 入口文案是“选择已创建的字段”。
  - 图标和点击行为复用现有快捷新增面板里的这套入口。
- 原因：
  - 你要的是字段配置主菜单里也能直接走“已创建字段”链路，不能只在表头右侧快捷加号里有。

### 2. 统一“选择已创建字段”的打开链路

- 文件：
  - `src/components/TaskTable/index.tsx`
- 处理内容：
  - `handleOpenExistingFieldPicker` 先统一执行 `closeFieldConfig()`。
  - 再关闭表头快捷新增浮层，最后打开 `CustomFieldEditorModal` 的 `existing` 页签。
- 原因：
  - 如果不先关掉字段配置主浮层，就会出现主浮层和已创建字段弹窗叠在一起的问题。
  - 统一入口逻辑后，主菜单和快捷新增面板都走同一条稳定链路。

### 3. 补字段配置入口回归测试

- 文件：
  - `tests/custom-field-configuration-regression.test.mjs`
- 处理内容：
  - 新增断言，要求字段配置主菜单右侧类型面板底部必须出现“选择已创建的字段”。
  - 新增断言，要求这个入口复用 `handleOpenExistingFieldPicker`。
  - 新增断言，要求打开逻辑里先关字段配置浮层，再切到 `existing` 页签打开弹窗。
- 原因：
  - 避免后续只保留快捷新增入口，或者把主菜单入口删掉、改散。

### 异常处理

- 这次没有重做“已创建字段”弹窗，直接复用现有 `CustomFieldEditorModal` 的 `existing` 页签，避免重复维护两套列表 UI。
- 没有改字段创建、字段编辑和字段可见性切换逻辑，只补入口和弹窗切换链路。

### 涉及模块

- `src/components/TaskTable/index.tsx`
- `tests/custom-field-configuration-regression.test.mjs`

### 可能影响范围

- 任务表格工具栏里的字段配置浮层
- 字段配置右侧类型菜单和已创建字段弹窗之间的跳转链路
- 表头右侧快捷新增字段入口共用的打开逻辑

### 测试情况

### 已执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/custom-field-configuration-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/custom-field-existing-picker-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过

### 已执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-table-resizable-layout-regression.test.mjs`
  - 结果：通过

## 本轮补充记录：增大任务详情抽屉默认宽度

### 改动背景

你这次反馈的是任务详情右侧抽屉宽度偏窄，子任务区域里的标题、截止时间、负责人和“详情”按钮容易挤到两行显示。当前子任务行布局已经允许在窄宽度下换行兜底，但这次更合适的处理是把抽屉默认宽度加大，让默认打开时尽量保持单行展示。

### 主要修改点

### 1. 调大任务详情抽屉宽度常量

- 文件：`src/components/TaskDetailPanel/index.tsx`
- 处理内容：
  - `DETAIL_PANEL_DEFAULT_WIDTH` 从 `460` 调整到 `560`。

## 本轮补充记录：已创建字段页签只保留选择和使用

### 改动背景

你这次要收的是“添加自定义字段”弹窗里 `已创建字段` 这条链路。

现在页面在这个页签里还保留了编辑和删除入口，等于用户选已有字段时还能继续做字段配置，这和你给的参考图不一致。你要的是这里只负责“选中一个已经存在的字段，然后添加到当前清单”，不允许在这个入口继续改字段定义。

### 主要修改点

### 1. 收口已创建字段页签，只保留选择态

- 文件：
  - `src/components/CustomFieldEditorModal/index.tsx`
- 处理内容：
  - 删除 `已创建字段` 页签里的编辑按钮和删除按钮。
  - 保留字段名称和字段类型标签，用单选高亮表示当前选中项。
  - 点击整行即可选中字段，不再在列表里直接触发配置动作。
- 原因：
  - 这个页签的职责应该只是“挑一个已有字段来用”，不应该混入字段定义维护动作，否则交互边界会继续发散。

### 2. 按参考图补搜索和底部确认按钮

- 文件：
  - `src/components/CustomFieldEditorModal/index.tsx`
- 处理内容：
  - 在 `已创建字段` 页签顶部加“搜索已创建的字段”输入框。
  - 底部 footer 改成 `取消 + 添加到清单` 两个按钮。
  - 未选中字段前，“添加到清单”按钮禁用。
- 原因：
  - 这样交互更接近你给的参考图，也能避免列表点击后立刻提交，用户更容易确认自己到底选中了哪个字段。

### 3. 保持现有启用链路，不扩散改接口

- 文件：
  - `src/components/TaskTable/index.tsx`
- 处理内容：
  - 继续复用现有 `onPickExisting` 逻辑：
    - 把字段 `is_visible` 更新为 `true`
    - 把字段列加回当前表格可见列
    - 重新刷新字段列表
- 原因：
  - 你这次要改的是交互边界，不是后端协议；沿用现有启用链路是最小改动，也最稳。

### 4. 补“已创建字段只可选择”回归测试

- 文件：
  - `tests/custom-field-existing-picker-regression.test.mjs`
- 处理内容：
  - 新增断言，要求 `已创建字段` 页签不再出现编辑入口。
  - 新增断言，要求 `已创建字段` 页签不再出现删除入口。
  - 新增断言，要求页签里有搜索框和底部 `添加到清单` 按钮。
  - 新增断言，要求未选中字段前确认按钮禁用。
- 原因：
  - 这类 UI 需求后面很容易被“顺手加个编辑按钮”带回去，先用源码级回归把边界钉住。

### 异常处理

- 这次没有改字段新建、字段编辑、字段删除的主入口能力：
  - 新建字段仍然走 `新建字段` 页签。
  - 编辑已有字段仍然走字段配置面板里的编辑入口。
  - 删除已有字段仍然走编辑态弹窗里的删除按钮。
- 这次也没有改 `updateCustomField` 的接口契约，只收口 `已创建字段` 页签的前端行为。

### 涉及模块

- `src/components/CustomFieldEditorModal/index.tsx`
- `tests/custom-field-existing-picker-regression.test.mjs`

### 可能影响范围

- 任务清单页里“添加自定义字段”弹窗的 `已创建字段` 页签
- 选择已有字段加入当前清单的交互路径
- 已有字段在当前清单里的启用体验

### 测试情况

### 已执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/custom-field-existing-picker-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/custom-field-configuration-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/custom-field-editor-update-endpoint-regression.test.mjs`
  - 结果：通过
  - `DETAIL_PANEL_MIN_WIDTH` 从 `440` 调整到 `520`。
- 原因：
  - 默认抽屉宽度更宽后，子任务元信息区不用在刚打开时就换到第二行；最小宽度同步提高，避免用户拖窄后很快回到拥挤状态。

### 2. 同步详情抽屉基础样式

- 文件：`src/components/TaskDetailPanel/index.less`
- 处理内容：
  - `.detail-panel` 的 `width` 改成 `560px`。
  - `.detail-panel` 的 `min-width` 改成 `520px`。
- 原因：
  - 常量控制运行时宽度，Less 控制基础样式，两边需要保持一致，避免首屏样式和 React 状态宽度不一致。

### 3. 更新宽度回归测试

- 文件：
  - `tests/task-detail-rich-input-regression.test.mjs`
  - `tests/task-detail-drawer-regression.test.mjs`
- 处理内容：
  - 把默认宽度和最小宽度断言更新为 `560/520`。
  - 新增抽屉样式层的宽度断言。
- 原因：
  - 防止后续又把抽屉默认宽度改回偏窄，导致子任务区域再次默认双行。

### 异常处理

- 这次只调整抽屉宽度，不改子任务接口、子任务数据和负责人/截止时间交互。
- 子任务行原有的窄宽度换行兜底保留，避免极端拖窄或长标题时布局溢出。

### 可能影响范围

- 任务详情抽屉默认打开宽度
- 任务详情抽屉拖拽后的最小宽度
- 子任务列表默认展示密度

### 测试情况

### 已执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-drawer-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-rich-input-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-subtask-layout-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过

## 本轮补充记录：修复 Windows 关闭任务详情页滚动条抖动

### 改动背景

当前 Windows 浏览器关闭任务详情页时，会看到竖向滚动条宽度变化带来的抖动；Mac 浏览器默认多是覆盖式滚动条，滚动条不占内容宽度，所以不容易复现。

这次修复重点是稳定任务详情页内部滚动容器的滚动条占位，避免关闭详情页时内容区宽度因为滚动条出现或消失来回变化。

### 主要修改点

### 1. 给详情页滚动区预留稳定滚动条占位

- 文件：
  - `src/components/TaskDetailPanel/index.less`
- 处理内容：
  - 在 `.detail-scroll` 上新增 `scrollbar-gutter: stable;`。
- 原因：
  - Windows 传统竖向滚动条会占用宽度，如果滚动区在关闭时从有滚动条切到无滚动条，内容宽度会跳动。
  - 预留稳定 gutter 后，滚动条出现或消失时内容区宽度不再反复变化。

### 2. 补详情抽屉滚动条稳定性回归测试

- 文件：
  - `tests/task-detail-drawer-regression.test.mjs`
- 处理内容：
  - 新增断言，要求 `.detail-scroll` 必须保留 `scrollbar-gutter: stable;`。
- 原因：
  - 这个问题和平台滚动条机制有关，后续改详情页布局时容易误删，所以用测试固定下来。

### 异常处理

- 这次只动详情页内部滚动容器样式，不改关闭逻辑、不改任务数据、不改外层页面布局。
- 保留原来的 `overflow-y: auto`，不会影响详情页内容很多时的正常滚动。

### 涉及模块

- `src/components/TaskDetailPanel/index.less`
- `tests/task-detail-drawer-regression.test.mjs`

### 可能影响范围

- Windows 浏览器下任务详情页关闭时的滚动条占位稳定性
- 任务详情页内容滚动区宽度

### 测试情况

### 已执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-drawer-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过

## 本轮补充记录：子任务负责人只保留头像显示

### 改动背景

你这次指出的是任务详情右侧子任务列表里，负责人区域在已经分配负责人的情况下，把头像和名字一起常驻展示了。这个信息密度太高，也会继续挤占右侧抽屉宽度。

目标很明确：这里不再常驻显示名字，只保留头像；名字继续通过悬浮提示看。

### 主要修改点

### 1. 去掉子任务负责人按钮里的常驻姓名文本

- 文件：
  - `src/components/TaskDetailPanel/index.tsx`
- 处理内容：
  - 在子任务已分配负责人的分支里，删除 `assigneeUsers.map(getUserDisplayName).join('、')` 这段常驻名字展示。
  - 保留头像组展示。
  - 保留每个头像原本就有的 `Tooltip` 姓名提示。

## 本轮补充记录：任务整行空白区域支持打开详情

### 改动背景

你这次反馈的是“我负责的”任务表格里，当前只有左侧任务标题那一块空白区域能打开详情，点同一行右侧其他空白区域没有反应。

这类交互问题的根因不是详情页没打开，而是打开详情的点击事件只绑在标题列内部，整条任务行本身没有接住点击。所以用户看到的效果就是同一行里，只有一小块区域能点。

### 主要修改点

### 1. 把打开详情入口补到任务行 `onRow`

- 文件：
  - `src/components/TaskTable/index.tsx`
- 处理内容：
  - 给任务行的 `onRow` 返回值补上 `onClick: () => onTaskClick(record)`。
  - 保留现有拖拽能力，不改 section 行和新建行的行为。
- 原因：
  - 这样点击当前任务整条行上的空白区域，都会统一走现有的详情打开逻辑。
  - 改动只落在任务行这一层，不需要拆现有列结构，范围最小。

### 2. 保留现有交互控件的冒泡拦截

- 文件：
  - `src/components/TaskTable/index.tsx`
- 处理内容：
  - 不改复选框、优先级、负责人、日期这些单元格原本的 `stopPropagation` 处理。
- 原因：
  - 改成整行可点后，如果这些控件不拦截冒泡，用户点编辑控件时就会顺带打开详情。
  - 这次只修“空白区打不开详情”，不改已有编辑交互。

### 3. 补任务行点击详情回归测试

- 文件：
  - `tests/task-row-actions-regression.test.mjs`
- 处理内容：
  - 新增断言，要求任务行 `onRow` 必须统一处理打开详情。
  - 保留对优先级单元格拦截冒泡的检查，避免后续回退。
- 原因：
  - 这个问题很容易在继续改表格交互时再次出现，需要用回归测试钉住。

### 异常处理

- 这次没有改任务详情面板、任务数据接口和任务筛选逻辑。
- 没有改 section 行、分组行和行内新建任务行的点击行为，避免影响无关交互。
- 保留标题列里原有的详情热区，和整行点击并存，不会影响当前使用方式。

### 涉及模块

- `src/components/TaskTable/index.tsx`
- `tests/task-row-actions-regression.test.mjs`

### 可能影响范围

- “我负责的”“全部任务”“我创建的”等任务表格视图里的任务行点击行为
- 任务行空白区域打开详情的一致性
- 任务行内编辑控件的冒泡拦截稳定性

### 测试情况

### 已执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-row-actions-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过

## 本轮补充记录：右侧补白列支持点击打开详情

### 改动背景

你后面又补了一张图，指出任务行最右侧那块大空白区域还是点不开详情。

进一步排查后确认，这块不是普通任务字段单元格，而是表格额外加的一列补白占位列，专门用来撑满剩余宽度。之前这列样式上写了 `pointer-events: none`，导致鼠标点到这里时，行级 `onClick` 根本接不到事件，所以看起来就像“同一行只有中间一部分能点，最右边不能点”。

### 主要修改点

### 1. 去掉补白列的事件禁用

- 文件：
  - `src/components/TaskTable/index.less`
- 处理内容：
  - 删除 `.task-table-spacer-cell` 上的 `pointer-events: none`。
- 原因：
  - 让这块补白列恢复正常命中，点击时事件可以继续落到当前任务行。
  - 这样任务最右侧空白区域也能复用现有行级 `onClick` 打开详情。

### 2. 补补白列回归测试

- 文件：
  - `tests/task-row-actions-regression.test.mjs`
- 处理内容：
  - 新增断言，确认表格仍然保留 `tableLayoutSpacer` 这列。
  - 新增断言，确认 `.task-table-spacer-cell` 不允许再写 `pointer-events: none`。
- 原因：
  - 后面再调表格布局时，这种“为了补白顺手禁事件”的回退很容易再出现，要用测试钉住。

### 异常处理

- 这次没有改任务详情逻辑，没有改任务行 `onRow` 行为，只修最右侧补白列的事件命中。
- 没有动复选框、优先级、负责人、日期等单元格原有的交互拦截，避免引入误触。

### 涉及模块

- `src/components/TaskTable/index.less`
- `tests/task-row-actions-regression.test.mjs`

### 可能影响范围

- 任务表格最右侧补白区域的点击命中
- 任务行整行打开详情的一致性

### 测试情况

### 已执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-row-actions-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过

## 本轮补充记录：系统内置字段隐藏编辑铅笔

### 改动背景

你这次反馈的是字段配置面板里，接口返回 `creator_id` 为 `system` 的字段本身是系统内置字段，不应该允许用户再编辑，但当前 UI 右侧还是显示了编辑铅笔。

这个问题本质上不是接口没区分，而是前端渲染编辑按钮时，只判断了“是不是自定义字段”，没有继续判断“是不是系统内置字段”。

### 主要修改点

### 1. 给字段配置行补系统字段判断

- 文件：
  - `src/components/TaskTable/index.tsx`
- 处理内容：
  - 在字段配置行里新增 `const isSystemBuiltInField = cfRaw?.creator_id === 'system'`。
  - 编辑按钮从原来的 `cfRaw && (...)` 改成 `cfRaw && !isSystemBuiltInField && (...)`。
- 原因：
  - 系统内置字段虽然也在自定义字段接口里返回，但不该给用户暴露编辑入口。
  - 这次只隐藏编辑铅笔，不改字段显示、显隐切换和排序逻辑。

### 2. 补系统字段编辑入口回归测试

- 文件：
  - `tests/custom-field-configuration-regression.test.mjs`
- 处理内容：
  - 新增断言，要求字段配置行显式识别 `creator_id === 'system'`。
  - 新增断言，要求系统字段不能渲染编辑铅笔。
- 原因：
  - 后面再调整字段配置 UI 时，这个权限判断很容易被回退，需要用测试固定下来。

### 异常处理

- 这次只处理字段配置面板里的编辑铅笔显示条件，不改后端接口、不改字段编辑弹窗逻辑。
- 眼睛显隐按钮和字段排序入口保持不变，避免把改动范围扩大到无关交互。

### 涉及模块

- `src/components/TaskTable/index.tsx`
- `tests/custom-field-configuration-regression.test.mjs`

### 可能影响范围

- 字段配置面板里系统内置字段的操作入口展示
- 自定义字段和系统字段在字段配置面板里的权限区分

### 测试情况

### 已执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/custom-field-configuration-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过
  - 未分配时的“负责人”占位文案不变。
- 原因：
  - 你要求这里只显示头像和浮窗提示，当前实现多渲染了一层名字文本，所以直接在源头收掉最稳。

### 2. 补负责人展示回归测试

- 文件：
  - `tests/task-detail-subtask-assignee-display-regression.test.mjs`
- 处理内容：
  - 新增断言，确保已分配时仍然显示头像组。
  - 新增断言，确保已分配时不再常驻拼接负责人名字。
  - 新增断言，确保未分配时仍然保留“负责人”占位。
- 原因：
  - 防止后续为了“补信息”又把姓名文本重新塞回按钮里。

### 异常处理

- 这次只改显示，不动负责人选择、保存、悬浮提示和多负责人逻辑。
- 头像上的姓名提示仍然保留，避免去掉常驻名字后完全看不到人名。

### 可能影响范围

- 任务详情右侧子任务列表里的负责人展示
- 窄宽度抽屉下子任务行的信息密度

### 测试情况

### 已执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-subtask-assignee-display-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-subtask-actions-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过

## 本轮补充记录：删除子任务负责人的通用浮窗提示

### 改动背景

你这次继续收口的是子任务负责人入口上的通用提示文案“设置子任务负责人”。这个浮窗在当前界面里属于重复提示，鼠标移上去会多弹一层，不符合你现在要的更干净交互。

这里保留头像本身的人名提示就够了，不需要按钮外层再来一个通用说明。

### 主要修改点

### 1. 删除子任务负责人按钮外层的通用 Tooltip

- 文件：
  - `src/components/TaskDetailPanel/index.tsx`
- 处理内容：
  - 删除子任务负责人按钮外层的 `Tooltip title="设置子任务负责人"`。
  - 保留原有 `Popover` 打开负责人选择器的交互。
  - 保留头像自身的姓名提示。
- 原因：
  - 这层 Tooltip 只是在重复说明按钮用途，和你要的简洁交互相反，直接删掉最稳。

### 2. 补浮窗提示回归测试

- 文件：
  - `tests/task-detail-subtask-assignee-tooltip-regression.test.mjs`
- 处理内容：
  - 新增断言，确保子任务负责人入口不再包“设置子任务负责人”的通用 Tooltip。
  - 新增断言，确保头像自身的姓名 Tooltip 仍然保留。
- 原因：
  - 防止后面又把这层通用提示加回来，同时保证真正有用的人名提示不会被误删。

### 异常处理

- 这次没有改负责人选择器本身，也没有改点击打开 Popover 的逻辑。
- 头像姓名提示保留，所以去掉通用 Tooltip 后仍然能看到负责人是谁。

### 可能影响范围

- 任务详情右侧子任务列表里的负责人 hover 交互
- 子任务负责人入口的提示层级

### 测试情况

### 已执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-subtask-assignee-tooltip-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-subtask-assignee-display-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过

## 本轮补充记录：恢复子任务箭头详情入口

### 改动背景

这次不是新需求，而是前一轮已经改好的子任务箭头详情入口被误回滚了。

目标就是把这块 UI 恢复到确认过的状态：去掉“详情”文字，重新换回右箭头入口。

### 主要修改点

### 1. 恢复子任务详情入口的箭头按钮

- 文件：
  - `src/components/TaskDetailPanel/index.tsx`
- 处理内容：
  - 子任务详情入口恢复成 `RightOutlined` 图标按钮。
  - 恢复 `aria-label="查看详情"`。
  - 删除回滚后重新出现的“详情”文字内容。
- 原因：
  - 这块被误回滚到了旧的“眼睛图标 + 详情文字”按钮，需要恢复到你确认过的箭头样式。

### 2. 恢复箭头入口的轻量样式

- 文件：
  - `src/components/TaskDetailPanel/index.less`
- 处理内容：
  - `subtask-detail-btn` 恢复成 28px 的浅灰箭头按钮样式。
  - 删除回滚后重新出现的蓝色胶囊按钮视觉。
  - 顺手删掉又被带回来的 `.subtask-assignee-name` 死样式。
- 原因：
  - 这次回滚不只带回了“详情”文字，也把旧按钮壳一起带回来了，需要一并恢复。

### 3. 保持现有回归测试和当前 UI 一致

- 文件：
  - `tests/task-detail-subtask-actions-regression.test.mjs`
  - `tests/main-detail-font-color-regression.test.mjs`
- 处理内容：
  - 保留当前“箭头按钮 + aria-label + 无详情文案”的断言，不再退回旧预期。
- 原因：
  - 这次就是按现有测试恢复实现，避免后面再次误回滚。

### 异常处理

- 这次只恢复子任务详情入口，不去扩散修改别的字段入口和详情逻辑。
- 构建时遇到的是 `src/components/TaskTable/index.tsx` 里多处未使用变量报错，和这次箭头入口恢复不是同一处问题，所以没有顺手改那块无关代码。

### 涉及模块

- `src/components/TaskDetailPanel/index.tsx`
- `src/components/TaskDetailPanel/index.less`

### 可能影响范围

- 任务详情右侧子任务列表里的详情入口展示
- 子任务详情入口 hover 视觉

### 测试情况

### 已执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-subtask-actions-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/main-detail-font-color-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：未通过；当前失败点是 `src/components/TaskTable/index.tsx` 里一组未使用变量的 TypeScript 报错，不是这次恢复子任务箭头入口引入的问题

## 本轮补充记录：子任务日期入口改成“目标时间 + 截止”样式

### 改动背景

你这次给了明确参考图，要求任务详情页子任务里的时间入口不要再用“日历图标 + 日期”的样式，而是改成更接近飞书的“目标时间 + 截止”展示，比如 `4月29日 截止`。

这个需求本质上是日期入口的展示改造，不是交互改造，所以应该只收敛到子任务日期按钮的渲染文案。

### 主要修改点

### 1. 子任务已设置日期时显示“日期 + 截止”

- 文件：`src/components/TaskDetailPanel/index.tsx`
- 处理内容：
  - 子任务有截止时间时，按钮文案从 `M月D日` 改成 `${M月D日} 截止`。
  - 有截止时间时不再显示左侧日历图标。
  - 没有截止时间时仍然保留日历图标，继续作为可点击入口。
- 原因：
  - 这样既能贴近你给的参考图，又不会让“未设置日期”状态丢失入口提示。

### 2. 更新子任务日期入口回归测试

- 文件：`tests/task-detail-subtask-actions-regression.test.mjs`
- 处理内容：
  - 新增断言，要求有日期时显示“目标时间 + 截止”的样式。
  - 新增断言，要求无日期时保留日历图标入口。
- 原因：
  - 防止后续又退回成旧的“图标 + 日期”样式，或者把空状态入口一起改丢。

### 异常处理

- 这次只改子任务日期入口的展示，不改日期选择 Popover、日期清空逻辑和接口更新逻辑。
- Tooltip `设置子任务截止时间` 保留，交互语义不变。

### 涉及模块

- `src/components/TaskDetailPanel/index.tsx`
- `tests/task-detail-subtask-actions-regression.test.mjs`

### 可能影响范围

- 任务详情页子任务日期入口的文案展示
- 子任务行里日期入口的视觉风格

### 测试情况

### 已执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-subtask-actions-regression.test.mjs`
  - 结果：通过

### 待执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`

## 本轮补充记录：恢复任务表格列宽和铺满布局

### 改动背景

你这次反馈的是之前已经改好的任务表格布局被误回滚了，需要重新恢复。

这轮重点是把表格恢复成当前 Antd Table 上的可拖拽列宽，同时把除任务标题外的所有字段统一成适中的默认宽度；拖动某个字段时，只改变这个字段本身的宽度，不再带着其他字段一起挪位置；表格整体也不能缩成最小宽度显示。

### 主要修改点

### 1. 恢复表头列宽拖拽能力

- 文件：`src/components/TaskTable/index.tsx`
- 处理内容：
  - 恢复 `ResizableHeaderCell`。
  - 通过 Antd Table 的 `components.header.cell` 接入自定义表头单元格。
  - 给主要字段列和自定义字段列统一包一层 `withResizableHeader`。
- 原因：
  - 这样不替换表格组件，只在当前 Antd Table 上增加列宽拖拽能力，改动范围更小。

### 2. 非标题字段统一默认宽度

- 文件：`src/components/TaskTable/index.tsx`
- 处理内容：
  - 除任务标题外，普通字段默认宽度统一为 `120`。
  - 自定义字段默认宽度跟随普通字段，也统一为 `120`。
  - 任务标题列默认宽度按当前截图中的表头显示宽度设置为 `407`。
  - 任务标题列最小拖拽宽度继续保持 `140`，默认更宽但允许用户拖小。
  - 删除普通字段单独的 `width: 96/120/156` 等历史宽度覆盖，避免初始状态宽度不一致。
- 原因：
  - 你的最新要求是除任务标题外，其余字段使用统一且大小适中的默认显示宽度，基本能显示“开始时间 / 截止时间”这类字段。
  - 如果字段名或字段值超过默认宽度，就用省略号显示，不再为了完整内容把默认列宽撑开。

### 3. 用占位列吸收剩余宽度

- 文件：`src/components/TaskTable/index.tsx`
- 处理内容：
  - 在快速新增字段列后追加 `tableLayoutSpacer` 占位列。
  - `tableScrollX` 只统计真实字段列宽，不统计占位列。
  - 表格继续使用 `tableLayout="fixed"`。
- 原因：
  - 真实字段宽度固定后，多出来的容器宽度交给占位列吸收。
  - 这样拖动某一列时，只会影响当前列宽，不会把其他真实字段一起重新分摊位置。

### 4. 恢复表格铺满和拖拽样式

- 文件：`src/components/TaskTable/index.less`
- 处理内容：
  - 恢复固定标题列的白底和阴影。
  - 恢复表头拖拽热区和拖拽时的 `col-resize` 光标。
  - 恢复 `task-table-spacer-cell` 占位列样式。
  - 给表头、日期文本、优先级标签和自定义字段标签补齐单行省略样式。
  - 保持不再覆盖 Antd 内部 table 的 `min-width: 0`。
- 原因：
  - 不能再把整张表压成最小宽度显示，Antd 自身的横向滚动表格会保留 `min-width: 100%`，剩余宽度由占位列承接。
  - 默认宽度放不下完整字段和值时，按飞书式表格表现省略展示。

### 5. 补强表格布局回归测试

- 文件：`tests/task-table-resizable-layout-regression.test.mjs`
- 处理内容：
  - 校验标题列固定和默认 `407` 宽度。
  - 校验普通字段、自定义字段默认宽度统一为 `120`。
  - 校验普通字段不再保留单独默认宽度覆盖，也不再在列对象里单独写宽度。
  - 校验默认宽度放不下时表头、日期、优先级标签、自定义字段标签会省略显示。
  - 校验占位列、拖拽表头和样式兜底都存在。
- 原因：
  - 防止后续再次回滚后，只恢复了拖拽能力，却漏掉“非标题字段统一默认宽度”“超出省略”和“表格不按最小宽度显示”这几个关键点。

### 异常处理

- 这次只恢复前端表格布局和列宽状态，不改任务数据、字段配置接口和自定义字段保存逻辑。
- 保留当前表头快速新增字段列，只在它后面追加占位列，避免破坏现有新增字段入口。
- 拖拽时宽度会限制在最小宽度和最大宽度之间，避免字段被拖到不可见或异常过宽。

### 涉及模块

- `src/components/TaskTable/index.tsx`
- `src/components/TaskTable/index.less`
- `tests/task-table-resizable-layout-regression.test.mjs`

### 可能影响范围

- 任务表格默认字段宽度
- 任务标题固定列展示
- 任务表格横向滚动和列宽拖拽
- 自定义字段列的默认宽度和拖拽表现

### 测试情况

### 已执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-table-resizable-layout-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-table-antd-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-table-single-section-table-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/custom-field-configuration-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过
