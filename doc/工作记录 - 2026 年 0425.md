# 工作记录 - 2026 年 0425

## 改动背景

本次调整针对任务清单侧边栏里清单节点三点菜单的展示文案和菜单项。需求是去掉“分享”“归档清单”，同时把“重命名清单”改成“重命名”，把“移除清单”改成“删除”。

## 主要修改点

- 调整 `src/components/Sidebar/index.tsx` 中清单三点菜单的配置，只保留“重命名”和“删除”两个操作。
- 将清单菜单文案从“重命名清单”改成“重命名”。
- 将清单菜单文案从“移除清单”改成“删除”。
- 删除 Sidebar 里已经没有入口的分享、归档相关状态、方法、弹窗和依赖引用，避免留下无效代码和编译告警。
- 新增 `scripts/check-sidebar-tasklist-menu-items.mjs`，用来锁定这次菜单项和文案，防止后续回归。

## 异常处理手法

- 这次没有改删除和重命名本身的调用链，只保留原有异常提示逻辑，继续通过 `message.error` 提示失败原因。
- 在修改前先新增静态检查脚本，并先跑出红灯，确认检查能准确拦住旧菜单项。
- 修改后重新执行检查脚本和构建，确保没有因为删入口逻辑导致未使用变量、未使用 import 或类型检查失败。

## 涉及模块

- `src/components/Sidebar/index.tsx`
- `scripts/check-sidebar-tasklist-menu-items.mjs`

## 可能影响范围

- 影响任务清单侧边栏里单个清单节点的三点菜单展示。
- Sidebar 内原来的“分享清单”和“归档清单”入口已移除，这两个能力不再能从该菜单触发。
- 不影响清单重命名和删除的原有执行逻辑。

## 测试情况

- 红灯验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node scripts/check-sidebar-tasklist-menu-items.mjs`
  - 结果：失败，准确提示旧菜单里还存在“重命名清单”“移除清单”“分享”“归档清单”。
- 绿灯验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node scripts/check-sidebar-tasklist-menu-items.mjs`
  - 结果：通过。
- 构建验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过。

---

## 改动背景

本轮调整针对左侧默认“任务清单”分组。需求是把它从单纯的树容器改成一个可点击的默认分组入口：点击后右侧展示覆盖全部清单的清单总览页，并提供“全部清单”“我创建的”“我参与的”三个 tab；同时点击这个默认分组右侧加号时，新建清单要按“在当前默认分组下创建”的逻辑走，而不是创建到无分组状态。

## 主要修改点

- 调整 `src/components/Sidebar/index.tsx`：
  - 新增 `tasklists-root` 导航键，专门表示默认“任务清单”分组总览页；
  - 把根节点 `root` 改成可选中，点击后切到 `tasklists-root`；
  - 根节点选中态和右侧总览页联动；
  - 根节点加号创建清单时，改成优先落到默认分组 `defaultGroup` 下；
  - 保留并补回个人分组兼容函数 `getProjectGroupId(project, groups)` 和空分组占位节点常量，避免打坏已有侧栏回归。
- 调整 `src/pages/TaskList.tsx`：
  - 新增 `projectItems` 状态，保留原始清单数据给总览页直接展示；
  - 新增 `involvedProjectIds` 状态，用“我参与的任务”反推“我参与的清单”；
  - 当 `activeNav === 'tasklists-root'` 时，不渲染任务表，改为渲染清单总览组件。
- 新增 `src/components/TasklistOverview/index.tsx`：
  - 使用 antd `Tabs` + `List` 展示清单总览；
  - `全部清单` 展示所有清单，不区分自定义分组和默认分组；
  - `我创建的` 按 `creator_id` 过滤；
  - `我参与的` 按参与任务反推出来的清单集合过滤；
  - 每个清单项提供“打开”入口，点后直接切到该清单的任务表页。
- 新增 `tests/default-tasklist-overview-regression.test.mjs`，锁定这次默认根分组总览页的行为，避免后续回退。

## 异常处理手法

- 先补红灯测试，先确认当前代码确实没有默认分组总览页入口，再开始改实现。
- “我参与的清单”没有直接复用不存在的清单成员字段，而是复用现有任务接口 `involved_user_id` 反推清单集合，避免拍脑袋扩字段。
- 改动中触发了两个旧侧栏回归：
  - 空分组占位节点源码形态检查；
  - 个人分组 `user_group_id` 回退兼容检查。
  这次同步把 `Sidebar` 本地兼容函数和占位常量补回去，确保新需求不覆盖旧保护。
- 只新增一个很薄的总览组件，没有去改你现有 `TaskTable` 的复杂筛选、视图保存和任务编辑逻辑，控制改动范围。

## 涉及模块

- `src/components/Sidebar/index.tsx`
- `src/pages/TaskList.tsx`
- `src/components/TasklistOverview/index.tsx`
- `tests/default-tasklist-overview-regression.test.mjs`

## 可能影响范围

- 影响左侧默认“任务清单”根分组的点击行为和选中态。
- 影响点击根分组右侧加号时的新建清单归属，现会归到默认分组下。
- 右侧主内容区新增了一个清单总览页，但只在点击默认根分组时展示，不影响原来的“全部任务”“我创建的”“单个清单”任务表页。
- “我参与的清单”当前按参与任务反推，如果后端后续补了清单成员字段，可以再切到更直接的数据源。

## 测试情况

- 红灯验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/default-tasklist-overview-regression.test.mjs`
  - 结果：失败，准确提示默认“任务清单”根分组还没有切到清单总览页。
- 绿灯验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/default-tasklist-overview-regression.test.mjs`
  - 结果：通过。
- 回归验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/sidebar-empty-group-switcher-regression.test.mjs`
  - 结果：通过。
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/user-project-group-api-regression.test.mjs`
  - 结果：通过。
- 构建验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过。

---

## 改动背景

本轮调整针对任务详情页里子任务日期展示不完整的问题。当前子任务行的日期按钮只显示“截止时间”，即使子任务已经有开始时间，也不会像父任务那样显示“开始时间 – 截止时间”的区间文案。需求是让子任务日期展示和父任务保持一致，优先显示开始-截止区间。

## 主要修改点

- 调整 `src/components/TaskDetailPanel/index.tsx` 里子任务列表的日期展示逻辑：
  - 增加 `startDate` 读取；
  - 子任务日期按钮改成按父任务同样的规则显示：
    - 同时有开始和截止时间时，显示 `开始时间 – 截止时间`；
    - 只有开始时间时，只显示开始时间；
    - 只有截止时间时，继续显示 `截止时间 截止`；
    - 两者都没有时，继续显示日期图标入口。
- 新增 `tests/task-detail-subtask-date-range-regression.test.mjs`，锁定详情页子任务日期按钮不再只显示截止时间。

## 异常处理手法

- 先补红灯测试，先确认当前详情页子任务日期按钮文案确实还是 `xxx 截止`。
- 修复时只改子任务行的展示文案，不改截止时间弹层、保存接口和已有时间开关逻辑，控制改动范围。
- 测试里一开始用了过严的源码正则，受到 JSX 换行格式影响后，及时收口成更稳的结构匹配，避免把格式变化误判成行为回退。

## 涉及模块

- `src/components/TaskDetailPanel/index.tsx`
- `tests/task-detail-subtask-date-range-regression.test.mjs`

## 可能影响范围

- 影响任务详情页里子任务行的日期按钮文案展示。
- 不影响子任务截止时间弹层的打开、编辑和提交逻辑。
- 不影响父任务日期区的展示和交互。

## 测试情况

- 红灯验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-subtask-date-range-regression.test.mjs`
  - 结果：失败，准确命中详情页子任务日期按钮仍只显示截止时间。
- 绿灯验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-subtask-date-range-regression.test.mjs`
  - 结果：通过。
- 构建验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过。

---

## 改动背景

本轮调整针对子任务开始时间配置逻辑。当前现状是：任务详情页把子任务开始时间写成只读提示“跟随父任务”，主页面里的子任务开始时间也同样不能编辑；同时前端还有一层 `inheritParentStartForTasks` 会把子任务自己的开始时间强行覆盖成父任务开始时间。需求是先把主页面子任务开始时间配置放开，保证子任务如果已经有自己的开始时间，前端展示不能再被父任务时间覆盖。

## 主要修改点

- 调整 `src/components/TaskTable/index.tsx` 里的 `TaskDateCell`：
  - 去掉“子任务开始时间跟随父任务，不能单独修改”的硬拦截；
  - 去掉子任务开始时间的只读按钮分支；
  - 主页面里的子任务开始时间改成和普通任务一样，统一走 `DateValuePanel` 可编辑面板。
- 调整 `src/utils/taskDate.ts` 里的 `inheritParentStartForTasks`：
  - 子任务如果已经有自己的 `start`，前端继续保留子任务自己的开始时间；
  - 只有子任务没有开始时间时，才回退显示父任务开始时间。
- 新增 `tests/subtask-start-date-regression.test.mjs`，锁定两条回归：
  - 主页面子任务开始时间不能再是只读；
  - 子任务已有开始时间时，前端不能再被父任务开始时间覆盖。

## 异常处理手法

- 先做红灯测试，先确认当前代码里确实还存在“子任务开始时间不能单独修改”的硬拦截。
- 排查时把根因拆成两层：
  - 主表组件 `TaskDateCell` 主动禁止编辑；
  - `inheritParentStartForTasks` 又把已有子任务开始时间覆盖掉。
- 修复时只放开主页面子任务开始时间编辑，并只修改开始时间继承工具，不扩大到详情页的主任务日期弹层交互，控制本轮变更范围。
- 红灯测试里发现源码正则容易受格式影响后，及时把继承逻辑检查改成真实函数执行结果校验，避免测试误报。

## 涉及模块

- `src/components/TaskTable/index.tsx`
- `src/utils/taskDate.ts`
- `tests/subtask-start-date-regression.test.mjs`

## 可能影响范围

- 影响主页面任务表里子任务开始时间的编辑能力。
- 影响父任务展开后子任务开始时间的展示优先级。
- 当前没有顺手改详情页主任务日期区里“子任务开始时间跟随父任务”的只读提示，所以这块仍保持原样。

## 测试情况

- 红灯验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/subtask-start-date-regression.test.mjs`
  - 结果：失败，准确命中主页面子任务开始时间仍被只读拦截。
- 绿灯验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/subtask-start-date-regression.test.mjs`
  - 结果：通过。
- 回归验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node --test tests/subtask-sync.test.ts`
  - 结果：通过，之前的子任务同步逻辑未被这次修改打坏。
- 构建验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过。

---

## 改动背景

本轮调整针对任务表里的自定义文本字段渲染。后端现在会返回 `field_type: text` 配合 `render_hint: button`，前端需要把这类字段按按钮入口展示，同时按钮内容来自任务接口里的 `custom_fields_display[field_id]`。这次先做前端先行能力：列表里不直接露出原始值文本，只显示一个轻量图标按钮；点击后先在前端拿到这个值，不发后端请求。

## 主要修改点

- 在 `src/services/customFieldService.ts` 补上 `render_hint` 字段定义，避免前端丢掉后端返回的按钮提示。
- 在 `src/services/taskService.ts` 补上 `custom_fields_display` 接口映射，让任务对象能拿到每条任务自己的字段展示值。
- 在 `src/components/TaskTable/index.tsx` 里把 `text + render_hint=button` 映射成按钮渲染分支。
- 按钮渲染不再展示 `training_task_xxx` 这类原始文本，只保留一个轻量图标入口，避免视觉上像一段裸文本加链接图标。
- 点击按钮时，前端先读取 `task.custom_fields_display[field_id]`，通过轻提示证明已经拿到当前按钮对应的值，暂时不发后端请求。
- 保留旧 `button` 字段的兼容逻辑，避免影响历史数据。
- 新增 `tests/custom-field-render-hint-button-regression.test.mjs`，锁定 `render_hint`、`custom_fields_display` 和按钮渲染链路，防止后续回归。

## 异常处理手法

- 先补回归检查并在 `deepagent` 环境下跑红灯，确认当前代码确实没接住 `render_hint`。
- 按最小改动原则只补类型、映射和表格渲染，不扩散到无关模块。
- 构建阶段发现 `custom_fields_display` 改成必填会波及 mock 和内联任务对象后，及时收口为可选字段，避免把这次需求扩大成整库数据模型改造。
- 点击按钮时如果当前没有可用内容，统一提示“当前按钮还没有内容”，避免出现无反馈点击。

## 涉及模块

- `src/types/task.ts`
- `src/services/customFieldService.ts`
- `src/services/taskService.ts`
- `src/components/TaskTable/index.tsx`
- `src/components/TaskTable/index.less`
- `src/components/CustomFieldEditorModal/index.tsx`
- `src/components/CustomFieldsModal/index.tsx`
- `tests/custom-field-render-hint-button-regression.test.mjs`

## 可能影响范围

- 影响任务表里带 `render_hint=button` 的文本型自定义字段展示方式。
- 旧的 `button` 字段仍保留兼容逻辑，但当前新增样式会让按钮入口整体更轻，不再是强蓝主按钮。
- 目前点击后只是前端拿值并提示，还没有接真实跳转页或后端动作。

## 测试情况

- 红灯验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/custom-field-render-hint-button-regression.test.mjs`
  - 结果：失败，准确提示 `render_hint` 还没接进前端类型。
- 绿灯验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/custom-field-render-hint-button-regression.test.mjs`
  - 结果：通过。
- 构建验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过。
- 额外说明：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node scripts/check-custom-field-option-id-migration.mjs`
  - 结果：失败，但失败点是仓库里原本就存在的 `TaskDetailPanel` 历史动态 `*_value_label` 检查项，和这次按钮改动无关，本轮没有顺手扩修。

---

## 改动背景

本轮调整针对侧栏空清单分组的拖拽体验。现象有两部分：

- 清单拖不进空分组；
- 空分组占位文案“可拖拽清单加入该分组”的起点没有和正常清单行左对齐。

用户要求这轮只修 Sidebar 里的空分组拖拽和占位展示，不动别的组件。

## 主要修改点

- 调整 `src/components/Sidebar/index.tsx`：
  - 给空分组占位补上原生 `onDragOver`、`onDragLeave`、`onDrop`；
  - 新增 `emptyGroupDropTargetId`，让空分组在拖拽悬停时能稳定挂上 `drop-target` 高亮态；
  - 复用现有 `applySidebarTasklistDrop`，把拖进空分组也走同一套跨分组移动逻辑；
  - 关闭分组节点自身的拖拽入口，避免和“清单分组不支持拖动”的要求冲突。
- 调整 `src/components/Sidebar/index.less`：
  - 把空分组占位文案左侧内边距改成和清单图标列一致的起点，让提示文案与清单左对齐。
- 调整 `scripts/check-sidebar-tasklist-drag-ui.mjs`：
  - 增加空分组原生拖拽入口检查；
  - 增加占位文案左对齐样式检查。

## 异常处理手法

- 先补红灯，确保脚本能准确拦住两类回退：
  - 空分组再次失去原生拖拽入口；
  - 占位文案再次偏离清单左对齐。
- 修复过程中出现了一次运行时错误：`Cannot access 'handleEmptyGroupDragOver' before initialization`。
  - 根因是 `treeData` 的 `useMemo` 先引用了后面才声明的 `const` 事件处理函数；
  - 这次收口成函数声明，避免 `const` 的时序死区问题。

## 涉及模块

- `src/components/Sidebar/index.tsx`
- `src/components/Sidebar/index.less`
- `scripts/check-sidebar-tasklist-drag-ui.mjs`

## 可能影响范围

- 影响侧栏里空清单分组的拖拽接收和高亮反馈。
- 影响空分组占位提示文案的横向对齐。
- 不影响任务主页面、详情页和其他非 Sidebar 组件。

## 测试情况

- 红灯验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node scripts/check-sidebar-tasklist-drag-ui.mjs`
  - 结果：失败，准确提示空分组缺少原生拖拽入口和占位文案左对齐样式。
- 绿灯验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node scripts/check-sidebar-tasklist-drag-ui.mjs`
  - 结果：通过。
- 构建验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过。

---

## 改动背景

本轮调整针对任务侧边栏里“任务清单”的拖拽渲染细节。目标是把拖拽目标位置改成和参考图一致的蓝色横线，同时把拖拽源节点从原来的“灰色背景 + 蓝色边框”改成“浅蓝色阴影背景”，并移除原位置的灰色底和默认蓝框。

## 主要修改点

- 保持 `src/components/Sidebar/index.tsx` 里现有的 `dropIndicatorRender` 逻辑，只保留目标位置的蓝色横线提示，继续不在“拖入分组内部”时显示横线。
- 调整 `src/components/Sidebar/index.less` 中拖拽源节点的样式：
  - 去掉拖拽源节点原本的灰色背景；
  - 隐藏 Ant Tree 默认给 `.dragging` 节点加的蓝色描边伪元素；
  - 改成浅蓝色背景加轻量内阴影，贴近参考图的选中吸附效果。
- 保留目标分组的灰色吸附态和目标位置蓝色横线，不扩大这次修改范围。
- 更新 `scripts/check-sidebar-tasklist-drag-ui.mjs`，把本轮要求补进静态回归检查，锁定：
  - 拖拽源不能再是灰底；
  - 拖拽源必须隐藏默认蓝框；
  - 目标位置必须继续是与清单对象同宽的蓝色横线。

## 异常处理手法

- 先按红绿灯方式修改检查脚本，先跑出失败，再改样式，避免继续靠肉眼猜样式。
- 这次拖拽源蓝框的根因不是业务代码重复渲染，而是 Ant Tree 默认给 `.dragging:after` 生成了描边伪元素，所以修复点放在样式层，直接隐藏默认伪元素，避免去改拖拽逻辑。
- 改完后同时做三层验证：
  - 回归脚本；
  - 项目构建；
  - 实际页面 `http://192.168.61.219:5176` 的 DOM 计算样式和截图确认。

## 涉及模块

- `src/components/Sidebar/index.less`
- `scripts/check-sidebar-tasklist-drag-ui.mjs`

## 可能影响范围

- 影响任务侧边栏里清单节点被拖拽时的源节点视觉反馈。
- 不影响清单实际拖放落点计算，也不影响跨分组移动和同组内排序逻辑。
- 不影响分组节点拖拽排序的交互链路。

## 测试情况

- 红灯验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node scripts/check-sidebar-tasklist-drag-ui.mjs`
  - 结果：失败，准确提示拖拽源仍是灰色背景，且还缺少浅蓝背景和隐藏默认蓝框的样式。
- 绿灯验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node scripts/check-sidebar-tasklist-drag-ui.mjs`
  - 结果：通过。
- 构建验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过。
- 页面实测：
  - 在 `http://192.168.61.219:5176` 上通过 Playwright 检查 `.tasklist-tree-wrap .ant-tree-treenode.dragging` 的实际计算样式；
  - 结果：默认蓝框伪元素已隐藏，拖拽源节点已改成浅蓝色阴影反馈，目标蓝线规则仍保留。

---

## 改动背景

本轮继续收侧边栏任务清单的交互细节。用户要求三点：一是清单分组本身不支持拖动，所以不能再显示分组拖动图标和分组拖动态；二是清单项的拖拽图标要和分组展开/收起三角按钮处在同一列；三是清单项三点按钮固定右对齐，不能因为名称长短变化而左右漂移。

## 主要修改点

- 调整 `src/components/Sidebar/index.tsx` 中 `Tree` 的 `nodeDraggable` 判定，只允许 `tl:` 开头的清单节点参与拖拽，不再放开 `grp:` 分组节点。
- 调整 `src/components/Sidebar/index.tsx` 的清单标题结构，给名称区域补上 `tasklist-name-wrap` 包裹层，单独承担弹性宽度。
- 调整 `src/components/Sidebar/index.less`：
  - 隐藏分组节点和空分组占位节点上的 `ant-tree-draggable-icon`，彻底去掉“分组可拖动”的视觉暗示；
  - 继续隐藏清单节点自己的 `switcher-noop` 占位，让拖拽图标落到和分组三角按钮同一列；
  - 给 `tasklist-name-wrap` 增加 `overflow: hidden`；
  - 给 `tasklist-action-btn` 保持 `margin-left: auto`，并补上 `align-self: center`，把三点按钮固定在当前清单行的最右侧。
- 更新 `scripts/check-sidebar-tasklist-drag-ui.mjs`，把这三条要求补进回归检查。

## 异常处理手法

- 先补红灯检查，确认当前代码确实还把分组节点设成了可拖拽目标，并且还缺少名字包装层和图标列控制样式。
- 这次没有动清单拖放逻辑本身，只收树节点结构和样式，避免影响前面已经稳定的拖拽落点规则。
- 页面验证时先发现旧 bundle 缓存干扰，所以在最终确认前主动刷新页面，再看最新 DOM 结构，避免误判。

## 涉及模块

- `src/components/Sidebar/index.tsx`
- `src/components/Sidebar/index.less`
- `scripts/check-sidebar-tasklist-drag-ui.mjs`

## 可能影响范围

- 影响侧边栏里任务清单与清单分组的拖拽图标展示和清单项内部布局。
- 不影响清单拖放落点、跨分组移动和清单拖拽预览逻辑。
- 不影响分组展开/收起功能本身。

## 测试情况

- 红灯验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node scripts/check-sidebar-tasklist-drag-ui.mjs`
  - 结果：失败，准确提示分组节点仍被设为可拖拽，且样式里缺少清单项布局约束。
- 绿灯验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node scripts/check-sidebar-tasklist-drag-ui.mjs`
  - 结果：通过。
- 构建验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过。
- 页面实测：
  - 强刷 `http://192.168.61.219:5176` 后重新检查侧栏树结构；
  - 结果：分组项快照里不再带 `drag` 标识，清单项仍保留 `drag` 标识，说明分组拖动视觉提示已关闭。

---

## 改动背景

本轮调整针对子任务完成态在主页面和详情页之间不同步的问题。现场现象是：在详情页里把子任务标记完成或重启后，主页面里的同一条子任务不会立刻同步；反过来在主页面里操作子任务完成态，详情页里的子任务勾选态和负责人完成态也不会同步。接口返回的 `status`、`is_completed` 和 `assignee_completions` 已经是有效数据，所以这次重点排查前端本地状态同步链路。

## 主要修改点

- 新增 `src/utils/subtaskSync.ts`，统一收口“外部子任务更新如何同步到详情草稿和主表展开缓存”的逻辑，避免主页面和详情页各自维护一套分散规则。
- 在 `src/pages/TaskList.tsx` 增加 `lastUpdatedTask`，把每次 `onTaskUpdated` 的最新任务对象继续往 `TaskTable` 和 `TaskDetailPanel` 透传，作为跨区域同步的统一信号。
- 在 `src/components/TaskTable/index.tsx` 增加对外部子任务更新信号的监听：
  - 仅当更新对象是子任务时才处理；
  - 只回写当前已经展开父任务的 `subtasksByGuid` 缓存，保证主页面子任务勾选态、完成人数等展示立即同步。
- 在 `src/components/TaskDetailPanel/index.tsx` 增加对外部子任务更新信号的监听：
  - 仅当更新对象属于当前详情任务的直属子任务时才处理；
  - 统一回写 `subtaskDrafts`，保证从主页面触发的子任务完成/重启也能立刻同步到详情页。
- 新增 `tests/subtask-sync.test.ts`，锁定三条回归：
  - 详情页收到外部子任务更新时要同步本地草稿；
  - 无关父任务的更新不能污染当前详情；
  - 主表收到外部子任务更新时要同步已展开父任务缓存。

## 异常处理手法

- 先按红绿灯流程新增 `tests/subtask-sync.test.ts`，先跑出红灯，确认仓库里还没有这层子任务同步能力。
- 排查过程中先确认接口映射 `apiTaskToTask` 没有丢掉 `status`、`parent_task_guid`、`assignee_completions`，把根因收口到前端本地状态同步，而不是接口层。
- 修复时只新增一个小型同步工具，并在 `TaskList`、`TaskTable`、`TaskDetailPanel` 三处接入，不改完成态策略、不改接口调用链，控制改动范围。
- 为了让新增测试能直接在 Node 环境跑通，顺手把 `src/utils/taskDate.ts` 和 `src/utils/subtaskSync.ts` 的类型导入改成 Node 也能识别的相对路径，避免测试阶段被路径别名卡住。

## 涉及模块

- `src/pages/TaskList.tsx`
- `src/components/TaskTable/index.tsx`
- `src/components/TaskDetailPanel/index.tsx`
- `src/utils/taskDate.ts`
- `src/utils/subtaskSync.ts`
- `tests/subtask-sync.test.ts`

## 可能影响范围

- 影响主页面已展开父任务下的子任务状态展示同步。
- 影响任务详情页里子任务列表的完成态、负责人完成态和新增后续同步。
- 不影响任务状态接口本身，也不改变“仅我完成 / 为所有负责人完成 / 重启全部任务”的现有策略判断。

## 测试情况

- 红灯验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node --test tests/subtask-sync.test.ts`
  - 结果：失败，提示缺少 `src/utils/subtaskSync.ts`，说明同步能力尚未落地。
- 绿灯验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node --test tests/subtask-sync.test.ts`
  - 结果：通过，3 条子任务同步回归全部通过。
- 构建验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过。

---

## 改动背景

这轮需求收口为“先做清单跨分组移动”，不做“跨分组后按目标位置持久化排序”。前面已经先实际验证了后端接口能力：`/api/v1/task-center/projects/{project_id}/user-group` 支持跨个人分组移动，但项目侧没有独立的 `sort_order` 持久化接口，所以这次只保留跨分组移动链路，不再扩默认清单根页或跨组位置持久化。

## 主要修改点

- 复核 `src/components/Sidebar/index.tsx` 当前侧栏拖拽逻辑，确认清单项跨分组时已经走：
  - 前端本地 `applyTasklistDrop(...)` 先更新树结构；
  - 再调用 `moveProjectToGroup(projectId, targetGroupId)` 持久化目标分组。
- 收掉本轮中途为默认“任务清单”根分组补的额外页面态和新视图，避免把需求扩大成根页重构。
- 保留 `src/components/Sidebar/tasklistDrag.ts` 里跨分组落点计算能力，继续支持：
  - 拖到分组节点内；
  - 拖到目标清单前后；
  - 从一个分组拖到另一个分组。
- 调整 `scripts/check-sidebar-tasklist-drag-ui.mjs`，只保留和当前需求相关的回归校验，不再绑定默认根页 UI。

## 异常处理手法

- 先实际测试后端接口能力，再决定是否继续开发，避免前端做了用户最终不能落库的能力。
- 已确认后端支持“移动到其他分组”，不支持“清单排序持久化”，所以实现边界明确收在“跨分组移动”。
- 这轮没有扩散修改 `TaskTable`、主页面根视图和其它无关组件，只收口侧栏拖放链路和回归校验。

## 涉及模块

- `src/components/Sidebar/index.tsx`
- `src/components/Sidebar/tasklistDrag.ts`
- `scripts/check-sidebar-tasklist-drag-ui.mjs`

## 可能影响范围

- 影响左侧任务清单树里清单项拖到其他分组时的前端显示和后端分组持久化。
- 同组内位置调整仍然只是本地顺序优化，不是持久化排序能力。
- 不影响清单分组本身的排序接口，也不影响任务表内部 Section 拖拽。

## 测试情况

- 接口能力验证：
  - 实测 `PATCH /api/v1/task-center/projects/{project_id}/user-group`
  - 结果：`200`，清单可以跨个人分组移动。
  - 实测 `PATCH /api/v1/task-center/projects/{project_id}/sort-order`
  - 结果：`404`，后端没有清单排序接口。
- 回归验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node scripts/check-sidebar-tasklist-drag-ui.mjs`
  - 结果：通过。
- 构建验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过。

---

## 改动背景

这轮继续收口子任务时间展示细节。现象是：详情页子任务已经支持开始时间后，如果只配了开始时间、还没配截止时间，界面会直接显示开始时间文案；但实际期望是“没有截止时间就先不展示时间文案”，只保留右侧配置时间的图标。并且黑底白字的“设置子任务截止时间”提示，只应该在开始时间和截止时间都没配时出现。

## 主要修改点

- 调整 `src/components/TaskDetailPanel/index.tsx` 里子任务时间按钮的展示分支：
  - 只有存在截止时间时，才展示时间文案；
  - 同时存在开始时间和截止时间时，展示“开始 - 截止”；
  - 只有截止时间时，继续展示“截止时间 截止”；
  - 没有截止时间时，不再单独展示开始时间，而是只显示时间图标。
- 保留空状态提示的原有交互，但范围收窄：
  - 开始时间和截止时间都为空时，图标外层保留 tooltip；
  - 只要已经配了开始时间，即使还没配截止时间，也只显示普通图标，不再显示黑底提示。
- 更新 `tests/task-detail-subtask-date-tooltip-regression.test.mjs`，补上“无截止时间只显示图标”和“只有完全空状态才显示 tooltip”的回归校验。

## 异常处理手法

- 按红绿灯流程先改回归测试，再执行测试确认旧实现不满足当前规则。
- 修复时只收口子任务日期按钮这一处分支判断，不改日期弹层、不改保存接口、不动其它子任务交互，尽量把回归风险压到最小。
- 用构建再次确认 JSX 条件分支调整后没有引入编译问题。

## 涉及模块

- `src/components/TaskDetailPanel/index.tsx`
- `tests/task-detail-subtask-date-tooltip-regression.test.mjs`

## 可能影响范围

- 影响任务详情页右侧子任务列表的时间文案展示。
- 不影响子任务时间的实际保存逻辑，也不影响主页面子任务时间列展示规则。
- 不影响“点击图标打开截止时间配置面板”的现有交互。

## 测试情况

- 红绿灯回归：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-subtask-date-tooltip-regression.test.mjs`
  - 结果：通过，已覆盖“无截止时间不显示文案、仅空状态显示 tooltip”。
- 构建验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过。

---

## 改动背景

这轮继续收口详情页子任务行的展示细节。新要求有两点：第一，子任务没有截止时间时，右侧日期图标需要统一带“设置截止时间”提示；第二，子任务负责人区域不要再显示姓名，只保留头像，避免右侧列表信息过满。

## 主要修改点

- 调整 `src/components/TaskDetailPanel/index.tsx` 里的子任务日期按钮：
  - 有截止时间时，继续显示时间文案；
  - 同时有开始和截止时间时，显示“开始 - 截止”；
  - 没有截止时间时，统一显示日期图标，并挂上 `Tooltip("设置截止时间")`。
- 收口子任务负责人单人态展示：
  - 保留头像、tooltip 和完成角标；
  - 删除单人负责人后面的姓名文本，只显示头像。
- 更新回归测试：
  - `tests/task-detail-subtask-date-tooltip-regression.test.mjs` 改成校验“无截止时间统一显示设置截止时间提示”；
  - `tests/task-person-display-regression.test.mjs` 改成校验“子任务负责人单人态不再显示名字”。

## 异常处理手法

- 先按红绿灯补测试，确认当前实现仍然保留旧提示文案和负责人姓名。
- 实现阶段只改详情页子任务这一小块渲染，不改日期保存逻辑、不动主任务负责人展示，避免影响其它区域。
- 构建阶段发现一个无用局部变量，顺手删除，保证 TypeScript 构建干净通过。

## 涉及模块

- `src/components/TaskDetailPanel/index.tsx`
- `tests/task-detail-subtask-date-tooltip-regression.test.mjs`
- `tests/task-person-display-regression.test.mjs`

## 可能影响范围

- 影响任务详情页右侧子任务列表的日期提示文案。
- 影响任务详情页右侧子任务列表单人负责人展示样式。
- 不影响子任务日期编辑弹层、不影响负责人选择弹层，也不影响主页面对应列的显示规则。

## 测试情况

- 回归验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-subtask-date-tooltip-regression.test.mjs`
  - 结果：通过。
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-person-display-regression.test.mjs`
  - 结果：通过。
- 构建验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过。
