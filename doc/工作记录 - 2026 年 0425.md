# 改动背景

- 侧栏里的“任务清单”根节点在空目录时，没有像“新分组”那样显示展开/收起三角按钮。
- 同一个空目录场景下，根节点下面也缺少“可拖拽清单加入该分组”的提示词，导致交互和分组区不一致。

# 根因分析

- 左侧树组件里，“新分组”空态会插入一个占位子节点，所以 `Tree` 会把它当成可展开节点处理。
- “任务清单”根节点在空态时直接传了空数组，`Tree` 会把它当成叶子节点，所以三角按钮和占位提示词都会消失。
- 另外排查时发现，侧栏有几条源码正则回归测试已经落后于当前实现，测试目标没问题，但断言写法和现状不一致，导致会误报。

# 主要修改点

- 在 `src/components/Sidebar/index.tsx` 里补了根节点空态分支：
  - 当 `ungroupedProjects` 为空且默认分组存在时，复用现有的空分组占位节点构造逻辑。
  - 这样根节点空态也会稳定显示展开按钮和“可拖拽清单加入该分组”提示词。
- 在 `src/components/Sidebar/index.less` 里把分组行隐藏拖拽占位重新收窄到 `9px`，和现有中文注释保持一致，避免展开箭头列错位。
- 新增回归测试 `tests/sidebar-root-empty-placeholder-regression.test.mjs`，专门覆盖根节点空态占位逻辑。
- 顺手把几条已有侧栏源码回归测试更新到当前实现语义，避免旧断言误报：
  - `tests/sidebar-empty-group-switcher-regression.test.mjs`
  - `tests/default-tasklist-overview-regression.test.mjs`
  - `tests/sidebar-tree-switcher-alignment-regression.test.mjs`

# 异常处理手法

- 这次没有新增接口调用，也没有改接口异常处理链路。
- 主要还是复用现有空分组占位节点的拖拽事件处理，保持根节点空态和普通分组空态走同一套逻辑，减少分叉。

# 涉及模块

- `src/components/Sidebar/index.tsx`
- `src/components/Sidebar/index.less`
- `tests/sidebar-root-empty-placeholder-regression.test.mjs`
- `tests/sidebar-empty-group-switcher-regression.test.mjs`
- `tests/default-tasklist-overview-regression.test.mjs`
- `tests/sidebar-tree-switcher-alignment-regression.test.mjs`

# 可能影响范围

- 左侧任务清单树的根节点空态展示。
- 左侧清单分组空态和拖拽提示词展示。
- 左侧树节点拖拽图标占位带来的缩进对齐。
- 没有改任务详情、清单内容区、接口协议和数据结构。

# 测试情况

- 红灯验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/sidebar-root-empty-placeholder-regression.test.mjs`
  - 修复前失败，确认根节点空态没有占位子节点。
- 绿灯验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/sidebar-root-empty-placeholder-regression.test.mjs && node tests/sidebar-empty-group-switcher-regression.test.mjs && node tests/default-tasklist-overview-regression.test.mjs && node tests/sidebar-tree-switcher-alignment-regression.test.mjs`
  - 以上命令通过。
- 额外发现的历史测试问题：
  - `tests/sidebar-tasklist-drag-sort-order.test.ts` 当前分支仍失败，失败点是 `applyTasklistDrop` 没有返回测试里断言的 `sortOrder` 字段。
  - 这条失败和本次“任务清单空态三角按钮/提示词”修复无关，本轮没有继续改这块业务逻辑。

---

# 改动背景

- 这轮重点是把“个人清单分组管理”和“个人清单列表”彻底对齐到新接口，同时把左侧分组内清单顺序补齐。
- 用户明确要求分组和清单的数据都要以接口为准，不能继续只靠前端本地状态推导。
- 同时用 `Playwright` 直接查看了 `2026-04-25` 的 Swagger 和真实接口返回，确认 `user_id` 使用的是 `王五`。

# 根因分析

- 代码里个人分组接口大部分已经切到了 `/api/v1/task-center/user-project-groups` 和 `/api/v1/task-center/projects/{project_id}/user-group`，但清单拖拽排序这条链路没有接完整：
  - `moveProjectToGroup` 还不支持传 `sort_order`；
  - 侧栏同组拖拽仍然只改本地顺序，没有走接口持久化；
  - 分组内清单展示顺序也没有统一按接口返回的 `sort_order` 排。
- 用真实接口验证后又发现一个兼容细节：
  - `/api/v1/task-center/projects?user_id=王五` 返回的“未分组”清单，`user_group_id` 是 `null`，并不会自动回填成默认分组 ID。
  - 所以左侧根分组“任务清单”不能只读默认分组 ID，还要把接口明确返回未分组的清单一起兜住。

# 主要修改点

- 在 `src/services/projectService.ts` 里补齐 `moveProjectToGroup(projectId, groupId, sortOrder?)`：
  - 拖拽清单到个人分组时，接口 body 现在会带上 `sort_order`。
- 在 `src/types/project.ts` 里补齐 `sort_order?: number`：
  - 让前端展示和拖拽计算都能直接消费接口返回的排序字段。
- 在 `src/components/Sidebar/tasklistDrag.ts` 里补齐拖拽排序计算：
  - 同组和跨组拖拽都会算出新的 `sortOrder`；
  - 返回结果里把新的 `sortOrder` 和更新后的本地清单顺序一起带回去。
- 在 `src/components/Sidebar/index.tsx` 里改成“接口优先”：
  - 根分组和自定义分组里的清单都按 `sort_order` 排序展示；
  - 根分组会同时展示“默认分组下的清单”和“接口明确返回未分组的清单”；
  - 同组拖拽不再只改本地，改成统一调用 `/projects/{project_id}/user-group`；
  - 拖拽成功后会重新调用 `listProjects()` 回刷，以后端落库结果为准。
- 在回归测试里补了接口约束：
  - `tests/user-project-group-api-regression.test.mjs`
  - `tests/default-tasklist-overview-regression.test.mjs`

# 异常处理手法

- 侧栏拖拽时仍然保留本地乐观更新，保证交互不发卡。
- 接口失败时会回滚到拖拽前的本地数据，并提示错误信息。
- 接口成功后立即重新拉取 `/api/v1/task-center/projects`，统一以后端返回结果刷新侧栏，避免前端本地顺序和后端真实顺序不一致。

# 涉及模块

- `src/components/Sidebar/index.tsx`
- `src/components/Sidebar/tasklistDrag.ts`
- `src/services/projectService.ts`
- `src/types/project.ts`
- `tests/user-project-group-api-regression.test.mjs`
- `tests/default-tasklist-overview-regression.test.mjs`

# 可能影响范围

- 左侧任务清单树的分组归属展示。
- 左侧根分组“任务清单”的未分组兜底逻辑。
- 个人分组内清单拖拽排序和同组重排。
- 拖拽成功后的侧栏刷新节奏。

# 测试情况

- 红灯验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/user-project-group-api-regression.test.mjs`
  - 修复前失败，确认“同组拖拽只改本地，没有走接口持久化”。
- 绿灯验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/user-project-group-api-regression.test.mjs`
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/default-tasklist-overview-regression.test.mjs`
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 以上命令通过。
- Swagger 与真实接口核对：
  - 通过 `Playwright` 查看 `http://192.168.30.238:8000/docs` 和 `/openapi.json`，确认当前个人分组管理接口集合是：
    - `GET/POST /api/v1/task-center/user-project-groups`
    - `PUT/DELETE /api/v1/task-center/user-project-groups/{group_id}`
    - `PATCH /api/v1/task-center/user-project-groups/{group_id}/sort-order`
    - `PATCH /api/v1/task-center/projects/{project_id}/user-group`
    - `GET /api/v1/task-center/projects`
  - 用 `user_id=王五` 真实联调确认：
    - 个人分组查询、创建、改名、排序可用；
    - 清单列表从 `/api/v1/task-center/projects` 获取可用；
    - 清单拖拽入分组、同组重排都可用，接口会返回新的 `sort_order`。
- 额外发现的接口问题：
  - 真实联调后，`DELETE /api/v1/task-center/user-project-groups/{group_id}?user_id=王五` 在目标分组已经没有清单时仍返回 `500 INTERNAL_ERROR`。
  - 这看起来是后端接口问题，不是前端调用路径或参数错误；当前只做记录，没有在前端绕过。

---

# 改动背景

- 用户又补了一条侧栏交互细节：清单分组名称也要像清单名称一样，支持超长省略显示。
- 同时要求分组名的可视显示长度，比清单名再收窄一点，按清单名显示宽度的 `0.8` 处理。

# 根因分析

- 左侧清单名称已经复用了 `NameOverflowPreview`，鼠标悬停可以看到完整名称。
- 但清单分组名称当前还是 `Text ellipsis`，只有基础省略，没有复用现有完整预览逻辑。
- 样式上分组名也没有单独的宽度约束，所以长分组名会和清单名占用同样的可视空间，不符合这次要求。

# 主要修改点

- 在 `src/components/Sidebar/index.tsx` 里，把清单分组标题和根分组“任务清单”标题都改成复用 `NameOverflowPreview`：
  - 分组名超长时会省略显示；
  - 悬停时可以看到完整名称；
  - 沿用现有清单名的交互，不再重复造一套 tooltip 逻辑。
- 在 `src/components/Sidebar/index.less` 里给分组名补了单独包裹层 `group-name-wrap`：
  - 宽度按 `calc(100% * 0.8)` 收窄；
  - `group-name` 本身补齐 `overflow: hidden`、`text-overflow: ellipsis`、`white-space: nowrap`。
- 新增一条样式回归测试：
  - `tests/sidebar-group-name-ellipsis-regression.test.mjs`
- 同时更新了已有侧栏源码回归测试：
  - `tests/default-tasklist-overview-regression.test.mjs`

# 异常处理手法

- 这轮没有新增接口调用，也没有改接口异常处理。
- 主要是复用现有 `NameOverflowPreview` 组件，减少重复实现，保证分组名和清单名的超长展示行为一致。

# 涉及模块

- `src/components/Sidebar/index.tsx`
- `src/components/Sidebar/index.less`
- `tests/default-tasklist-overview-regression.test.mjs`
- `tests/sidebar-group-name-ellipsis-regression.test.mjs`

# 可能影响范围

- 左侧任务清单分组标题的显示宽度。
- 左侧根分组“任务清单”的标题显示方式。
- 分组名超长时的 tooltip 预览行为。

# 测试情况

- 红灯验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/default-tasklist-overview-regression.test.mjs`
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/sidebar-group-name-ellipsis-regression.test.mjs`
  - 修复前分别失败，确认分组名还没复用 `NameOverflowPreview`，也没有 0.8 宽度限制。
- 绿灯验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/default-tasklist-overview-regression.test.mjs`
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/sidebar-group-name-ellipsis-regression.test.mjs`
  - 上述命令通过。

---

# 改动背景

- 主页面里任务详情抽屉打开时，点击某条任务左侧的展开按钮查看子任务，详情抽屉会立刻自动关闭。
- 这和当前需求不一致，正确行为应该是：主表格展开子任务列表时，任务详情抽屉保持原状态，不自动收回。

# 根因分析

- `src/components/TaskDetailPanel/index.tsx` 里有一条全局 `document.pointerdown` 监听。
- 这条逻辑会把“点击抽屉外任意区域”统一当成关闭详情处理。
- 主表格里的子任务展开箭头正好位于抽屉外侧，所以点展开按钮时，会先命中这条“外部点击关闭”逻辑，导致抽屉被误关。
- 任务表自己的展开逻辑本身没有主动关闭详情，问题根因就在详情抽屉的外部点击判定过宽。

# 主要修改点

- 在 `src/components/TaskDetailPanel/index.tsx` 里新增 `isTaskDetailKeepOpenTarget` 判定：
  - 专门识别带 `data-keep-task-detail-open="true"` 的交互入口；
  - 详情抽屉的外部点击关闭逻辑会跳过这类点击，避免误关。
- 在 `src/components/TaskTable/index.tsx` 里，给子任务展开箭头 `CaretRightOutlined` 增加 `data-keep-task-detail-open="true"` 标记：
  - 只对白名单入口放行；
  - 不改变其它点击主页面空白处自动关闭详情的现有行为。
- 新增回归测试 `tests/task-detail-keep-open-on-subtask-expand-regression.test.mjs`：
  - 校验详情抽屉外部点击逻辑已经包含保持展开白名单；
  - 校验主表格子任务展开按钮已经带上对应标记。

---

# 改动背景

- 这轮要修的是左侧参考清单区域里，清单分组名称超长时没有按预期省略显示。
- 前面虽然已经把分组名接上了 `NameOverflowPreview` 和 `ellipsis` 样式，但页面里长分组名还是会完整撑开显示。

# 根因分析

- 这次不是“省略样式没写上”，而是“省略生效条件没成立”。
- `src/components/Sidebar/index.less` 里的 `.group-title-main` 只有 `flex: 1`，但自己不是 `flex` 容器，导致下面 `.group-name-wrap` 的 `flex: 0 1 calc(100% * 0.8)` 实际没有参与收缩。
- 同时 `.ant-tree-node-content-wrapper` 缺少 `min-width: 0`，长分组名会把整行内容包装层撑宽，文本本身拿不到真正受限的容器宽度，所以 `text-overflow: ellipsis` 不会触发。

# 主要修改点

- 在 `src/components/Sidebar/index.less` 里补了两处最小布局修正：
  - 给 `.group-title-main` 增加 `display: flex`，让分组标题主区域真正成为可收缩的弹性容器；
  - 给 `.ant-tree .ant-tree-node-content-wrapper` 增加 `min-width: 0`，避免长分组名把整行撑开。
- 在 `tests/sidebar-group-name-ellipsis-regression.test.mjs` 里补了两条回归断言：
  - 分组标题主容器必须是 `flex`；
  - 树节点内容包装层必须允许收缩。

# 异常处理手法

- 这轮没有新增接口和异步逻辑，也没有改异常处理链路。
- 处理方式就是只修布局约束，不碰分组数据流和交互逻辑，控制影响范围。

# 涉及模块

- `src/components/Sidebar/index.less`
- `tests/sidebar-group-name-ellipsis-regression.test.mjs`

# 可能影响范围

- 左侧任务清单树的根分组和自定义分组标题宽度收缩行为。
- 分组标题右侧操作按钮和文本之间的剩余空间分配。
- 没有改接口协议、分组排序、拖拽逻辑和详情区逻辑。

# 测试情况

- 红灯验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/sidebar-group-name-ellipsis-regression.test.mjs`
  - 收紧断言后，修复前失败，确认 `.group-title-main` 不是 flex 容器。
- 绿灯验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/sidebar-group-name-ellipsis-regression.test.mjs`
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/default-tasklist-overview-regression.test.mjs`
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 上述命令通过。
- 页面级验证：
  - 用本地 `Playwright` 直接把分组名替换成超长字符串后复查 DOM 尺寸；
  - 修复后 `.group-name` 的 `scrollWidth=638`、`clientWidth=157`，说明文本已经真实溢出并会触发省略显示。

---

# 改动背景

- 这轮要修的是左侧边栏头部右上角两个图标的位置太靠右，通知红点提示会被侧边栏右边界裁掉一部分。
- 从页面表现看像是红点组件自己的偏移有问题，但实际需要的是把这两个头部操作组件整体往左收一点，给红点留出安全空间。

# 根因分析

- 根因不在 `NotificationBell` 自己，而在 `src/components/Sidebar/index.less` 的 `.sidebar-header-actions`。
- 这个容器虽然已经用 `inline-flex` 把 `UserSwitcher` 和 `NotificationBell` 排成一行，但右侧没有任何内边距。
- 同时侧边栏外层用了 `overflow: hidden`，通知红点又会从按钮右上角略微溢出，所以只要按钮组贴右边界太近，红点就会被裁掉。

# 主要修改点

- 在 `src/components/Sidebar/index.less` 里，只给 `.sidebar-header-actions` 补了一行 `padding-right: 8px;`。
- 这次没有改按钮逻辑、没有改 `NotificationBell` 的 badge 偏移，也没有改侧边栏宽度和布局结构，保持最小范围修复。
- 新增 `scripts/check-sidebar-header-actions-badge-spacing.mjs`，把“侧边栏头部右侧操作区必须给通知红点预留安全间距”固化成回归检查。

# 异常处理手法

- 这轮没有新增接口调用，也没有改异步流程。
- 处理方式是先用静态检查脚本把缺失的布局约束钉住，再做单点样式修复，避免以后改头部排版时把问题带回来。

# 涉及模块

- `src/components/Sidebar/index.less`
- `scripts/check-sidebar-header-actions-badge-spacing.mjs`

# 可能影响范围

- 左侧边栏头部右上角的身份切换和通知入口整体位置。
- 通知红点在侧边栏窄宽度下的可见性。
- 没有改任务列表、详情区、接口调用和通知下拉逻辑。

# 测试情况

- 红灯验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node scripts/check-sidebar-header-actions-badge-spacing.mjs`
  - 修复前失败，确认右侧操作区还没有预留通知红点的安全间距。
- 绿灯验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node scripts/check-sidebar-header-actions-badge-spacing.mjs`
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node scripts/check-sidebar-identity-switcher.mjs`
  - 上述命令通过。
- 编译验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 本轮修复后通过。

# 异常处理手法

- 这轮没有新增接口调用，也没有改异步请求链路。
- 修复方式是把“允许不关闭详情”的范围收口到指定入口，避免一刀切放开所有主页面点击，减少副作用。

# 涉及模块

- `src/components/TaskDetailPanel/index.tsx`
- `src/components/TaskTable/index.tsx`
- `tests/task-detail-keep-open-on-subtask-expand-regression.test.mjs`

# 可能影响范围

- 主页面任务表里带子任务的任务展开交互。
- 任务详情抽屉的“点击外部关闭”行为。
- 其它没有打白名单标记的主页面点击，仍然保持原来的关闭详情逻辑。

# 测试情况

- 红灯验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-keep-open-on-subtask-expand-regression.test.mjs`
  - 修复前失败，确认详情抽屉外部点击逻辑没有对白名单入口做豁免。
- 绿灯验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-keep-open-on-subtask-expand-regression.test.mjs`
  - 上述命令通过。
- 编译验证：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 本轮修复后通过。
