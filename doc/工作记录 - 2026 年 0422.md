# 工作记录 - 2026 年 0422

## 改动背景

任务详情页里，评论输入框按回车是发送评论，这个行为正常。但描述输入框在按回车换行时，内容没有稳定保留下来，编辑体验会被打断。

## 主要修改点

### 1. 收紧统一输入框的内容回写逻辑

- 文件：`src/components/TaskRichInput/index.tsx`
- 处理内容：
  - 给外部 `value` 同步加了一层独立的归一化处理。
  - 增加了初始同步标记，区分首次挂载和输入过程。
  - 输入过程中如果外部内容和当前快照一样，就直接跳过回写，避免描述区按回车后生成的新换行被重复刷新打断。
  - 去掉了 `onInput` 里把当前 `innerHTML` 再写回自身的动作，避免光标跳回句首和中文输入法组合态被打断。
- 原因：
  - 描述输入依赖 `contentEditable` 的原生换行行为，频繁回写 DOM 容易把光标、换行状态和中文输入法组合态一起冲掉。

### 2. 补了回归测试

- 文件：`tests/task-detail-rich-input-regression.test.mjs`
- 处理内容：
  - 新增了对“描述模式不要因为相同 HTML 反复重写编辑器 DOM”的断言。
- 原因：
  - 防止后续又把这层同步逻辑改回去，导致描述回车换行再次失效。

## 异常处理

- 没有改评论模式的 Enter 提交逻辑，避免影响现有“回车发送、Shift+Enter 换行”的行为。
- 没有动任务详情页的其他编辑区，控制修改范围只落在统一输入组件里。

## 可能影响范围

- 任务详情里的描述输入换行体验
- 统一输入框的外部 value 同步
- 评论输入发送逻辑不受影响

## 测试情况

### 已执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-rich-input-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过

## 本轮补充记录：任务历史记录改成飞书式结构化时间线

### 改动背景

前一版历史记录已经能打开，但展示还是偏“纯文本列表”，人物信息和附件卡片的层级不够像飞书右侧历史记录。你这次要求的是按截图继续复刻，重点是人物信息要高亮、要有操作人头像，字体、行间距、日期块和附件卡片都要更贴近原样。

### 主要修改点

### 1. 历史记录消息改成结构化渲染

- 文件：
  - `src/components/TaskDetailPanel/index.tsx`
- 处理内容：
  - 新增 `renderTaskActivityMessage`
  - 把操作人、被操作人、任务名、字段名拆成独立片段渲染
  - 操作人和人物名统一用蓝色高亮
  - 保留 `formatTaskActivityText` 作为整句兜底文案，挂在 `title` 上
- 原因：
  - 这样可以控制每个信息块的字号、颜色和换行，不会再被一句整文本限制住。

### 2. 历史记录列表改成更接近飞书的时间线布局

- 文件：
  - `src/components/TaskDetailPanel/index.tsx`
  - `src/components/TaskDetailPanel/index.less`
- 处理内容：
  - 行结构固定成“时间列 + 头像列 + 内容列”
  - 日期块改成“今天 / 昨天 / 周几 + 数字”的双层样式
  - 头像改成 18px 圆头像占位
  - 行间距和字重收紧，整体更像飞书右侧面板
- 原因：
  - 这块最关键的是信息密度和视觉节奏，不是单纯把内容排出来。

### 3. 附件类记录补成独立卡片

- 文件：
  - `src/components/TaskDetailPanel/index.tsx`
  - `src/components/TaskDetailPanel/index.less`
- 处理内容：
  - 附件相关历史记录补成独立附件卡片
  - 卡片里单独展示文件名和大小
- 原因：
  - 截图里的附件不是一行纯文本，而是单独的白底卡片。

### 4. 回归测试同步收紧

- 文件：
  - `tests/task-detail-history-regression.test.mjs`
- 处理内容：
  - 把“历史记录只输出纯文本”的旧约束换成结构化渲染约束
  - 增加对人物高亮、任务链接视觉、附件卡片和紧凑行高的检查
- 原因：
  - 防止后续再把这块改回成纯字符串拼接。

### 异常处理

- 当前后端只返回 `actor_id`，没有单独的人名字段，所以人名展示走当前可用的 `user_id` 标识，当前数据里能直接对上中文名时会正常高亮。
- 没有额外引入新接口，避免把页面复杂度继续往上堆。
- 结构化渲染只影响历史记录模式，不动原任务详情、评论、附件和三点菜单其它入口。

### 可能影响范围

- 任务详情右侧历史记录面板
- 历史记录的可读性和视觉密度
- 附件类历史记录的展示方式

### 测试情况

### 已执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-history-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过

## 本轮补充记录：修复“我分配的”页面空数据

### 改动背景

你这次反馈的是左侧“我分配的”页面明明应该展示“我创建且已经分配了负责人”的任务，但页面里是空的。排查后确认不是没调接口，而是接口拉回数据后，前端又用旧的单负责人字段把数据过滤空了。

### 主要修改点

### 1. 修正“我分配的”列表前端筛选逻辑

- 文件：`src/pages/TaskList.tsx`
- 处理内容：
  - 保留“我分配的”先按 `creator_id = currentUserId` 请求的逻辑。
  - 把“是否已分配负责人”的前端判断，从只看 `assignee_id`，改成同时兼容 `assignee_ids` 和旧的 `assignee_id`。
  - 关键判断旁边补了中文注释，说明这里为什么要兼容新旧字段。
- 原因：
  - 现在任务已经支持多负责人，后端可能只回 `assignee_ids`。如果页面还只看 `assignee_id`，就会把本来有负责人的任务误判成“未分配”，最后整页看起来像没数据。

### 2. 补“我分配的”回归测试

- 文件：`tests/my-assigned-quick-regression.test.mjs`
- 处理内容：
  - 新增断言，保证“我分配的”仍然按当前用户创建人过滤。
  - 新增断言，保证页面筛选已分配任务时必须兼容 `assignee_ids`，不能退回成只看 `assignee_id`。
- 原因：
  - 这个问题本质上是新旧字段兼容漏掉了一层，后面很容易被再次改回去，所以要单独钉一条测试。

### 异常处理

- 这次没有改接口参数，也没有动“我负责的”“我创建的”等其它导航页，修改范围只收敛在“我分配的”这条本地筛选分支。
- 前端继续兼容旧的 `assignee_id`，避免后端不同环境字段不一致时再次出问题。

### 可能影响范围

- 左侧“我分配的”页面列表展示
- 多负责人任务在快捷入口里的可见性
- 其它导航页不受影响

### 测试情况

### 已执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/my-assigned-quick-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-multi-assignee-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过

## 本轮补充记录：历史记录面板按截图继续放大字号和行距

### 改动背景

你这次要求历史记录面板继续贴近参考图，重点不是改结构，而是把行距、字号、头像和附件卡片再放大一点，让整块列表更接近飞书那种阅读密度。

### 主要修改点

### 1. 放大历史记录列表的文字和时间列

- 文件：`src/components/TaskDetailPanel/index.less`
- 处理内容：
  - 把时间列字号调大。
  - 把正文改成更大的字号和更松一点的行高。
  - 把日期块的数字、标签字号一起提上去。
- 原因：
  - 参考图里的信息层级更清楚，视觉上不是特别紧的 12px 小字列表。

### 2. 放大头像和附件卡片

- 文件：
  - `src/components/TaskDetailPanel/index.tsx`
  - `src/components/TaskDetailPanel/index.less`
- 处理内容：
  - 头像从 18px 调到 20px。
  - 附件卡片的内边距、图标尺寸和卡片圆角一起放大。
- 原因：
  - 让右侧历史记录更像截图里的信息块，不会显得太挤。

### 3. 同步回归测试断言

- 文件：`tests/task-detail-history-regression.test.mjs`
- 处理内容：
  - 把历史记录每行布局断言改成新的列宽。
  - 把正文行高断言改成新的值。
- 原因：
  - 避免后续样式回调时把这次放大的视觉层级又改回去。

### 可能影响范围

- 任务详情右侧历史记录面板
- 历史记录列表的视觉密度和可读性

### 测试情况

### 待执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-history-regression.test.mjs`
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`

## 本轮补充记录：动态页显示真实用户名并合并同时间记录

### 改动背景

你这次又补了两个很具体的界面要求：

- 动态里的“我”要改成真实 `user_name`
- 同一个时间的多条修改要共用一个时间栏显示，组内更紧，组间要有明显层级距离

前一版虽然已经接了真实接口，但动态页和详情历史里还保留了“当前用户显示为我”的特殊分支，另外动态页还是一条记录一行时间，没把同一分钟的操作归并到同一个时间块里。

### 主要修改点

### 1. 动态页成员名改成真实 `user_name`

- 文件：
  - `src/components/ActivityView/index.tsx`
  - `src/components/TaskDetailPanel/index.tsx`
- 处理内容：
  - 动态页拉团队成员后，名字从 `member.user_name ?? member.user_id` 取值。
  - 去掉动态页和详情历史里“当前用户显示为我”的特殊逻辑，统一走真实姓名解析。
- 原因：
  - 你要求的是完整复刻参考效果，人员展示必须统一成真实名字，不能混进“我”这种特例文案。

### 2. 动态页改成“日期组 + 分钟组”两级结构

- 文件：
  - `src/components/ActivityView/index.tsx`
  - `src/components/ActivityView/index.less`
- 处理内容：
  - 在日期分组下面新增按 `HH:mm` 的时间组。
  - 同一分钟的多条动态共用一个时间栏，右侧内容区继续逐条堆叠。
  - 组内条目间距收紧，时间组之间额外拉开距离，做出更清楚的视觉层级。
- 原因：
  - 这样更接近你截图里的飞书动态布局，连续操作不会重复占满时间列，阅读节奏更顺。

### 3. 回归测试同步补齐

- 文件：
  - `tests/activity-view-regression.test.mjs`
  - `tests/task-detail-user-profile-regression.test.mjs`
- 处理内容：
  - 新增断言，保证动态页成员名优先用 `user_name`。
  - 新增断言，保证动态页存在分钟级时间组结构和对应样式。
  - 新增断言，保证动态页和详情历史都不再把当前用户写成“我”。
- 原因：
  - 这几个点都是很容易在后续样式调整时被改回去的，直接用回归测试钉住更稳。

### 异常处理

- 这次没有改接口参数，也没有新增接口调用，还是复用现有“我的动态”和团队成员接口。
- 为了让全量构建通过，顺手清掉了 `Sidebar` 和 `TaskTable` 里几个未使用的本地变量定义，没有改动它们的实际业务行为。

### 可能影响范围

- 左侧“动态”页面的人名展示
- 动态列表的时间分组方式和阅读层级
- 任务详情里的历史记录人名展示

### 测试情况

### 已执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/activity-view-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-user-profile-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过

## 本轮补充记录：筛选按钮数量改成条件数显示

### 改动背景

你这次补充说明，筛选按钮里显示的 `3` 不是固定文案，而是要跟当前筛选条件数量联动，条件增减时数字也要跟着变。

### 主要修改点

### 1. 筛选按钮数量改成动态条件数

- 文件：`src/components/TaskTable/index.tsx`
- 处理内容：
  - 筛选按钮继续沿用当前的蓝色数量块样式。
  - 数量来源保持为当前有效筛选条件数量，跟 `activeFilterConditions.length` 绑定。
- 原因：
  - 这样按钮上的数字就能真实反映当前筛选条件数，不会出现写死数字的情况。

### 2. 同步回归测试

- 文件：`tests/task-table-toolbar-regression.test.mjs`
- 处理内容：
  - 把旧的红色 Badge 断言改成新的数量块结构断言。
  - 检查按钮文案里确实渲染了当前筛选条件数量。
- 原因：
  - 防止后面又回退成旧的角标写法，或者把数量展示改丢。

### 测试情况

### 已执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-table-toolbar-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-table-filter-regression.test.mjs`
  - 结果：通过

## 本轮补充记录：筛选按钮数量语义和浮层外层样式调整

### 改动背景

你这次补充了两个交互细节：

- 筛选按钮上的数量应该跟当前已添加的条件行数一致，不是等到每一行都把值填完才显示。
- 筛选浮层外面还有一层多余的默认边框和阴影，视觉上只保留内部卡片这一层就够了。

### 主要修改点

### 1. 筛选按钮数量改成按条件行数展示

- 文件：`src/components/TaskTable/index.tsx`
- 处理内容：
  - 保留真实筛选匹配还是走 `activeFilterConditions`。
  - 新增单独的展示数量 `displayFilterCount`，按当前条件行数显示。
  - 对单个默认空白条件做排除，避免页面刚打开就显示 `1`。
- 原因：
  - 这样和你截图里的操作体验一致，已经添加了 3 条条件时，按钮会直接显示 `3`。

### 2. 去掉筛选浮层外层默认壳

- 文件：
  - `src/components/TaskTable/index.tsx`
  - `src/components/TaskTable/index.less`
- 处理内容：
  - 给筛选 `Popover` 单独加了 `task-filter-popover` 类名。
  - 去掉外层默认背景、阴影、圆角和箭头，只保留内部筛选卡片本身的边框和阴影。
- 原因：
  - 避免出现你标出来的外层多余边框，视觉上更贴近参考图。

### 3. 回归测试补齐

- 文件：
  - `tests/task-table-toolbar-regression.test.mjs`
  - `tests/task-table-filter-regression.test.mjs`
  - `tests/task-table-filter-popover-style-regression.test.mjs`
- 处理内容：
  - 增加“按钮数量按条件行数展示”的断言。
  - 保留“真实筛选逻辑按有效条件算”的断言。
  - 增加“筛选浮层去掉外层默认壳样式”的断言。
- 原因：
  - 防止后续这两个细节又被回退。

### 测试情况

### 已执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-table-toolbar-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-table-filter-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-table-filter-popover-style-regression.test.mjs`
  - 结果：通过

## 本轮补充记录：左侧新增动态入口并接入我的动态列表

### 改动背景

你这次要的是左侧边栏新增“动态”按钮，点进去后展示“我的动态”列表，记录点击后直接打开对应任务的详情抽屉。这个不是单纯加一个菜单项，而是要把侧边栏、动态页、任务详情抽屉三者串成一条线。

### 主要修改点

### 1. 动态列表改成真实接口驱动

- 文件：
  - `src/services/taskService.ts`
  - `src/components/ActivityView/index.tsx`
- 处理内容：
  - 新增 `listMyActivities`，调用 `/api/v1/task-center/activities/me`。
  - 动态页不再依赖 `mock/api.ts`。
  - 列表按日期分组渲染，并保留人物高亮、任务名高亮、时间和头像列。
- 原因：
  - 这样动态页才是真正的用户动态，不会只停留在静态假数据。

### 2. 左侧菜单新增“动态”

- 文件：`src/components/Sidebar/index.tsx`
- 处理内容：
  - 在顶部主菜单里恢复 `动态` 入口。
  - 使用历史记录风格图标，和右侧动态页的语义一致。
- 原因：
  - 入口要和参考图一致，用户从左侧就能直接进动态流。

### 3. 动态记录点击后打开任务详情抽屉

- 文件：
  - `src/components/ActivityView/index.tsx`
  - `src/pages/TaskList.tsx`
- 处理内容：
  - 点击一条动态先拉任务详情，再复用现有 `TaskDetailPanel` 打开抽屉。
  - 没有新拆路由，也没有新建额外详情页。
- 原因：
  - 这样操作路径最短，也和你给的参考图一致。

### 4. 回归测试补齐

- 文件：
  - `tests/activity-view-regression.test.mjs`
  - `tests/sidebar-hidden-entries-regression.test.mjs`
- 处理内容：
  - 补了动态接口、动态视图和侧边栏入口的断言。
- 原因：
  - 防止后面又把动态页改回 mock，或者把菜单入口删掉。

### 可能影响范围

- 左侧边栏菜单
- 动态列表页
- 任务详情抽屉打开链路

### 测试情况

### 待执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/activity-view-regression.test.mjs`
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/sidebar-hidden-entries-regression.test.mjs`
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`

## 本轮补充记录：主列表父任务展开后子任务不显示

### 改动背景

主列表里点击有子任务的父任务后，展开图标会变，但子任务行不出来。实际排查下来，不是接口完全没返回，而是展开状态和表格行 key 没对上，导致 antd Table 认不出要展开哪一行。

### 主要修改点

### 1. 把展开状态映射到真实表格行 key

- 文件：`src/components/TaskTable/index.tsx`
- 处理内容：
  - 新增 `collectExpandedRowKeys`。
  - 递归扫描当前 `tableRows`，把命中的任务 `guid` 转成实际 `rowKey`。
  - `expandedRowKeys` 不再直接塞 `expandedTaskGuids`，改成用映射后的结果。
- 原因：
  - 当前表格的任务行 key 是 `sectionGuid::task.guid`，不是单独的 `task.guid`。
  - 直接用 `task.guid` 只会让展开状态和表格行识别错位。

### 2. 补回归测试

- 文件：`tests/subtask-expand-rowkey-regression.test.mjs`
- 处理内容：
  - 增加断言，禁止再把 `expandedTaskGuids` 直接当成 `expandedRowKeys`。
  - 增加断言，要求必须经过 `collectExpandedRowKeys` 转换。
- 原因：
  - 防止以后分组逻辑再改动时，把展开 key 逻辑改回去。

### 异常处理

- 没有改子任务缓存、接口拉取、分组筛选这些链路，只修展开 key 映射，尽量控制影响面。
- 之前已有的子任务缓存逻辑继续保留，避免这次改动顺手引入别的回归。

### 可能影响范围

- 主任务表里所有带子任务的任务行展开
- 分组视图下的子任务展示
- `antd Table` 的展开状态同步

### 测试情况

### 已执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/subtask-expand-rowkey-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过

## 本轮补充记录：主页面和详情页人物浮窗改成显示 user_name

### 改动背景

你这次反馈的是主页面和任务详情页里，人员头像的浮窗提示还在显示 `user_id`，没有显示接口已经返回的 `user_name`。排查后确认，任务详情页大部分链路已经能吃到 `user_name`，但主页面 `TaskTable` 里团队成员映射还是把 `name` 写成了 `user_id`，负责人头像浮窗也优先用了这份旧数据。

### 主要修改点

### 1. 主页面团队成员映射改成优先用 user_name

- 文件：`src/components/TaskTable/index.tsx`
- 处理内容：
  - `listMembers()` 返回后，`setUsers` 改成优先使用 `user_name`，头像也接上 `avatar_url`。
- 原因：
  - 这是主页面很多人员展示的基础数据源，如果这里先把名字写死成 `user_id`，后面 Tooltip 再怎么取也只能拿到错的值。

### 2. 主页面负责人浮窗优先使用任务成员里的名字和头像

- 文件：`src/components/TaskTable/index.tsx`
- 处理内容：
  - 给负责人选择器补了 `taskMembers` 入参。
  - 当前任务已挂在 `task.members` 上的 `name/avatar` 现在会作为头像组和 Tooltip 的第一优先级，再兜底团队成员列表。
- 原因：
  - 任务接口本身已经返回了 `assignees.user_name`，这份数据比团队成员列表更贴近当前任务，不该再被旧的 `user_id` 覆盖回去。

### 3. 主页面创建人浮窗优先使用 task.creator.name

- 文件：`src/components/TaskTable/index.tsx`
- 处理内容：
  - 创建人列 Tooltip 改成先读 `record.creator.name` 和 `record.creator.avatar`，再兜底 `users` 列表。
- 原因：
  - 任务详情和主表要保持一致，创建人如果接口里已经给了 `creator_name`，就不应该再回退成 `creator_id`。

### 4. 补用户资料展示回归测试

- 文件：`tests/task-detail-user-profile-regression.test.mjs`
- 处理内容：
  - 新增主页面团队成员映射、负责人浮窗取名优先级、创建人 Tooltip 取名优先级断言。
- 原因：
  - 这个问题是“接口已有字段，但展示链路中途被旧值覆盖”，很容易在后续重构时再次出现，需要测试把它钉住。

### 异常处理

- 这次没有改接口请求，只改展示层取值优先级。
- 仍然保留 `user_id` 兜底，避免某些环境没返回 `user_name` 时页面出现空 Tooltip。

### 可能影响范围

- 主页面负责人头像 Tooltip
- 主页面创建人头像 Tooltip
- 详情页负责人头像 Tooltip 和主页面展示一致性

### 测试情况

### 已执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-user-profile-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-multi-assignee-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过

## 本轮补充记录：多级子任务详情分组继续沿用顶级父任务

### 改动背景

上一轮把子任务详情的分组显示改成了跟随父任务，但你这次继续补充了一个更深一层的场景：子任务下的子任务打开详情时，右侧分组还是不对。继续对照主表后确认，主表里所有层级的子任务，实际都是跟随顶级父任务所在分组展示的，不是跟随“直接父任务”。

### 主要修改点

### 1. 子任务详情分组来源从直接父任务改成顶级父任务

- 文件：`src/components/TaskDetailPanel/index.tsx`
- 处理内容：
  - 把 `tasklistOwner` 的来源从父任务链最后一个节点改成第一个节点
  - 子任务场景下，任务详情里的清单和分组展示统一跟随 `rootParentTask`
- 原因：
  - 父任务链是“顶级父任务 -> 直接父任务”的顺序，多级子任务要和主表分组保持一致，就必须取顶级父任务

### 2. 回归测试同步改成顶级父任务规则

- 文件：
  - `tests/task-detail-subtask-section-render-regression.test.mjs`
- 处理内容：
  - 断言从 `directParentTask` 改成 `rootParentTask`
  - 断言子任务详情分组展示优先沿用顶级父任务的 `tasklists`
- 原因：
  - 防止后续又把多级子任务回退成只跟随直接父任务

## 异常处理

- 顶级任务仍然沿用当前任务自己的 `tasklists`，只对子任务场景启用父任务分组来源。
- 父任务链为空时继续回退到当前任务本身，不会因为异常链路导致详情空白。

## 可能影响范围

- 多级子任务详情右侧“任务清单和分组”显示
- 一级子任务详情分组显示也继续保持正确

## 测试情况

### 已执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-subtask-section-render-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-section-source-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-section-sync-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-multi-section-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过

## 本轮补充记录：任务详情右上角新增查看历史记录

### 改动背景

任务详情右上角三点菜单需要新增“查看历史记录”，点击后调用任务动态接口，并按飞书右侧历史记录样式展示当前任务的所有变更记录。

### 主要修改点

### 1. 新增任务历史记录接口封装

- 文件：
  - `src/services/taskService.ts`
- 处理内容：
  - 新增 `ApiTaskActivity` 类型。
  - 新增 `listTaskActivities(taskId, page, pageSize)` 方法。
  - 请求路径使用 `api/v1/task-center/tasks/${taskId}/activities`，并带上当前 `user_id`。
- 原因：
  - 接口调用统一收口在服务层，详情页只消费类型明确的数据，不直接拼接口和猜字段。

### 2. 任务详情三点菜单新增历史记录入口

- 文件：
  - `src/components/TaskDetailPanel/index.tsx`
- 处理内容：
  - 三点菜单新增“查看历史记录”。
  - 点击后切换到历史记录模式，并拉取当前任务的动态数据。
  - 切换任务时自动关闭历史记录模式，避免显示上一个任务的数据。
- 原因：
  - 入口要和截图里的飞书操作路径一致，同时避免任务切换时出现旧数据残留。

### 3. 新增右侧历史记录面板渲染

- 文件：
  - `src/components/TaskDetailPanel/index.tsx`
  - `src/components/TaskDetailPanel/index.less`
- 处理内容：
  - 历史记录模式下复用右侧详情面板宽度，顶部显示返回按钮和标题。
  - 按日期分组，展示“今天/昨天/周几 + 日期数字”。
  - 每条记录展示时间、操作人头像占位和中文操作文案。
  - 支持加载中和空数据状态。
- 原因：
  - 保持和参考图一致的右侧时间线风格，同时不影响原有任务详情、评论和附件逻辑。

### 4. 补充回归测试

- 文件：
  - `tests/task-detail-history-regression.test.mjs`
  - `tests/task-menu-items-regression.test.mjs`
- 处理内容：
  - 新增历史记录服务层、菜单入口、右侧面板渲染的源码级回归测试。
  - 调整旧菜单测试，不再把详情页“查看历史记录”当成禁用旧入口。
- 原因：
  - 防止后续整理菜单时误删历史记录入口，或绕过服务层直接在组件里拼接口。

### 异常处理

- 历史记录接口失败时会清空本地历史数据，并提示“加载历史记录失败”。
- 历史记录为空时展示空态，不影响任务详情本身继续使用。
- 请求使用组件内取消标记，避免切换任务或关闭面板后旧请求回写状态。

### 可能影响范围

- 任务详情右上角三点菜单。
- 任务详情右侧历史记录模式展示。
- 任务服务层新增活动记录接口封装。
- 不影响任务列表主表菜单，也不改原有评论和附件接口。

### 测试情况

### 已执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-history-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-menu-items-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过

## 本轮补充记录：子任务详情分组显示改成跟主表一致

### 改动背景

你这次指出的是子任务打开详情后，右侧“任务清单和分组”没有正确显示当前分组。继续排查后发现，主表里子任务是跟着父任务所在分组展示的，但详情页这块之前始终直接拿当前子任务自己的 `tasklists[0]` 当来源，所以当子任务接口返回和主表展示语义不一致时，右侧就会显示成“未分组”或别的旧值。

### 主要修改点

### 1. 子任务详情优先沿用直接父任务的清单和分组引用

- 文件：`src/components/TaskDetailPanel/index.tsx`
- 处理内容：
  - 新增 `directParentTask`
  - 新增 `tasklistOwner`
  - 子任务场景下，任务详情里的当前清单来源、当前分组引用、已添加分组引用，都优先从直接父任务读取
  - 非子任务场景仍然沿用当前任务自身的 `tasklists`
- 原因：
  - 这样右侧详情的分组显示会和主表对子任务的分组展示规则保持一致

### 2. 顺手补齐分组接口加载占位

- 文件：`src/components/TaskDetailPanel/index.tsx`
- 处理内容：
  - 新增 `detailTasklistSectionsLoading`
  - 拉取当前清单分组接口时补上 loading 状态
  - 接口未返回前，右侧分组显示和弹层标签区统一显示“分组加载中”
- 原因：
  - 避免接口返回前先闪“选择分组”或空分组文案，影响判断

### 3. 回归测试补齐子任务分组显示场景

- 文件：
  - `tests/task-detail-subtask-section-render-regression.test.mjs`
  - `tests/task-detail-section-source-regression.test.mjs`
  - `tests/task-detail-multi-section-regression.test.mjs`
- 处理内容：
  - 新增断言：子任务详情必须优先使用直接父任务的清单分组引用
  - 已有分组来源测试补上加载态约束
  - 已有多分组测试同步接受 `tasklistOwner.tasklists`
- 原因：
  - 防止后续又回退成固定读取当前子任务自己的 `tasklists[0]`

### 4. 顺手清掉一个阻塞构建的未使用变量

## 本轮补充记录：当前分组筛选子菜单改成串行布局

### 改动背景

你这次要的是任务表顶部“分组”下拉里的子菜单布局调整，不是继续改分组逻辑。原来这里还是一组横向按钮，视觉上更像操作按钮区，不像参考图里的菜单列表，所以需要改成一行一个分类项的串行展示。

### 主要修改点

### 1. 分组方式改成纵向菜单项结构

- 文件：`src/components/TaskTable/index.tsx`
- 处理内容：
  - 把分组面板里的 `group-mode-actions` 按钮区改成 `group-mode-list` 纵向列表。
  - 每个分组选项改成一行一个菜单项，点击后继续沿用原来的 `setGroupMode` 切换逻辑。
  - 当前选中项右侧增加勾选图标，方便快速判断当前生效的分组方式。
- 原因：
  - 这次需求是只改分组子菜单布局，参考图本质上是“菜单项列表”，不是“按钮集合”。

### 2. 补齐串行菜单样式

- 文件：`src/components/TaskTable/index.less`
- 处理内容：
  - 新增 `group-mode-list`、`group-mode-item`、`group-mode-item-active` 等样式。
  - 调整为一行一个菜单项，支持 hover 和选中高亮。
  - 选中时显示右侧勾选，不选中时隐藏勾选图标。
- 原因：
  - 让下拉面板更接近飞书这类筛选子菜单的阅读和点击习惯，同时尽量保持现有整体风格不跳。

### 3. 补充布局回归测试

- 文件：`tests/task-table-toolbar-regression.test.mjs`
- 处理内容：
  - 新增对分组下拉“串行菜单列表结构”的源码级断言。
  - 断言菜单项具备独立标签区和选中态 class，防止后续又回退成横向按钮排布。
- 原因：
  - 这次改的是展示结构，最好直接锁住 DOM 组织方式，避免以后整理工具栏时又改回去。

## 异常处理

- 没有改 `groupModeOptions`、`buildGroupedTasksByMode` 和“展示分组”的筛选逻辑，只动下拉面板的展示结构和样式。
- 任务分组模式下的“展示分组”复选区继续保留，避免这次布局调整影响你前面已经确认好的分组筛选能力。

## 可能影响范围

- 任务表顶部“分组”下拉的子菜单展示方式。
- 不影响已有的任务分组、负责人、开始时间、截止时间、创建人、自定义字段分组逻辑。

## 测试情况

### 已执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-table-toolbar-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-table-grouping-modes-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-table-single-section-table-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过

## 本轮补充记录：负责人分组切换叠加问题和重复展示分组清理

### 改动背景

你这次反馈得很具体：只有“负责人”分组在和其他分组互相切换时，会出现页面数据不断叠加的情况；而且分组下拉里下面那块“展示分组”本身是重复的，因为上面已经列了分类项。所以这次要一起处理两个点。

### 主要修改点

### 1. 给分组任务行补上分组上下文 key

- 文件：`src/components/TaskTable/index.tsx`
- 处理内容：
  - 把 `buildTaskTreeRows` 里的任务行 `key` 从纯 `task.guid` 改成 `sectionGuid::task.guid`。
  - 这样同一个任务即使在“负责人”分组里被分到多个组，也不会因为统一 Table 里的重复 key 造成旧行残留。
- 原因：
  - “负责人”分组本身允许同一任务出现在多个分组里，继续用裸 `task.guid` 当全表唯一 key 会冲突，切换分组时最容易看到叠加残影。

### 2. 删掉重复的“展示分组”区域

- 文件：`src/components/TaskTable/index.tsx`
- 处理内容：
  - 去掉了分组下拉里下面那块“展示分组/全选/复选项”区域。
  - 保留上方的分组分类列表，不再重复展示一套筛选入口。
- 原因：
  - 你截图里已经明确，上面的分类项本身就是当前分组的筛选入口，下面再来一组会重复，也会让交互更乱。

### 3. 补充回归测试

- 文件：`tests/task-table-toolbar-regression.test.mjs`
- 处理内容：
  - 新增对任务行 key 必须带分组上下文的断言。
  - 新增对“展示分组”相关代码不能再保留的断言。
- 原因：
  - 防止后面再把 key 改回去，或者把重复筛选区加回来。

## 异常处理

- 没有改负责人分组的聚合逻辑，只改了行 key 和下拉展示，尽量把风险压小。
- 原来其它需要 `Checkbox` 的地方保留不动，只是把这次删掉分组复选区带来的 import 误删补回来了。

## 可能影响范围

- 任务表在“负责人”分组和其它分组之间切换时的渲染稳定性。
- 分组下拉的展示结构。

## 测试情况

### 已执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-table-toolbar-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-table-grouping-modes-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-table-single-section-table-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过

- 文件：`src/components/TaskTable/index.tsx`
- 处理内容：
  - 删除未使用的 `optionValues`
- 原因：
  - 这是当前工作区里已有的构建阻塞项，不清掉就没法完成本轮有效构建验证

## 异常处理

- 这轮没有改主表分组逻辑，只改任务详情如何选择展示来源。
- 只有在子任务并且已经拿到直接父任务链时，才切到父任务分组引用。
- 分组接口加载失败时，仍然回退为空数组，但不会残留上一个清单的旧分组数据。

## 可能影响范围

- 子任务详情右侧“任务清单和分组”显示
- 任务详情分组弹层的加载占位
- 不影响主表任务分组展示逻辑

## 测试情况

### 已执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-subtask-section-render-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-section-source-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-section-sync-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-multi-section-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过

## 本轮补充记录：详情页同步接口返回的最新任务分组

### 改动背景

这轮问题不是分组接口没返回，而是详情页已经请求到了最新任务数据，接口里也带了正确的 `section_id`，但右侧“任务清单和分组”没有及时刷新，仍然停在旧的“未分组”。排查后发现，详情页打开后会再调一次 `getTask(task.guid)`，但是只在标题、状态、开始时间、截止时间变化时才把最新任务回写到页面层，没有把 `tasklists/section` 的变化算进去。

## 本轮补充记录：描述输入框首次打开不应丢失已保存内容

### 改动背景

你这次提到的是任务描述编辑框：每次点进去都像是空白输入框，回车换行后还会把原来保存的描述覆盖掉。继续排查后确认，不是描述内容本身没传进来，而是 `contentEditable` 在 `autoFocus` 后先进入了编辑态，导致首次内容回填被跳过了。

### 主要修改点

### 1. 把描述输入的初始回填提前到布局阶段

- 文件：`src/components/TaskRichInput/index.tsx`
- 处理内容：
  - 把描述内容同步从普通 `useEffect` 调整成 `useLayoutEffect`
  - 让保存好的描述先灌进编辑器，再触发自动聚焦
- 原因：
  - 这样能保证第一次打开描述框时，编辑器里看到的是已有内容，不会先变成空白框

### 2. 把“进入编辑态”的时机改成真正开始输入

- 文件：`src/components/TaskRichInput/index.tsx`
- 处理内容：
  - 取消“聚焦即编辑”的做法
  - 只在描述区真正发生输入时，才把本地编辑标记打开
- 原因：
  - 这样可以避免光标一聚焦就把外部回填挡住，也能保住后续输入的光标位置

### 3. 补了回归测试

- 文件：`tests/task-detail-rich-input-regression.test.mjs`
- 处理内容：
  - 新增断言，要求描述输入首次回填发生在布局阶段
- 原因：
  - 防止后续再把初始内容回填改回普通 effect，导致空白输入框问题复发

### 异常处理

- 没有动评论输入的回车发送逻辑，避免影响“回车发送、Shift+Enter 换行”。
- 没有改任务描述的保存接口，只修输入框内部时序。

### 可能影响范围

- 任务详情里的描述输入编辑体验
- 描述框首次打开时的已保存内容展示
- 描述框回车换行后的光标稳定性

### 测试情况

### 已执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-rich-input-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过

### 主要修改点

### 1. 详情页刷新时增加任务分组差异判断

- 文件：`src/components/TaskDetailPanel/index.tsx`
- 处理内容：
  - 在 `getTask(task.guid)` 的刷新逻辑里新增 `tasklistsChanged`
  - 如果接口返回的 `tasklists` 数量变化，或者同位置的 `tasklist_guid / section_guid` 变化，就触发 `onTaskUpdated(fresh)`
- 原因：
  - 这样接口返回了新的任务分组后，外层选中任务状态会立刻更新，右侧详情就不会继续显示旧分组

### 2. 补回归测试锁住这个同步场景

- 文件：
  - `tests/task-detail-section-sync-regression.test.mjs`
- 处理内容：
  - 约束详情页刷新后必须比较 `tasklists` 长度
  - 约束详情页刷新后必须比较 `tasklist_guid / section_guid`
  - 约束分组变化时继续走 `onTaskUpdated(fresh)` 同步到页面层
- 原因：
  - 防止后续再把详情页刷新条件收窄，只同步标题时间这些字段，漏掉分组变化

## 异常处理

- 这轮没有改接口请求本身，只补详情页收到最新任务后的状态同步判断。
- 仍然沿用原来的轻量回写策略，不做无差别回写，避免不必要重渲染。
- 只有当任务分组真的变化时，才把最新任务对象同步到外层。

## 可能影响范围

- 任务详情右侧“任务清单和分组”显示
- 详情页打开后首次刷新任务数据的状态同步
- 不影响主页面任务分组加载接口

## 测试情况

### 已执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-section-sync-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-section-source-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-multi-section-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过

## 本轮补充记录：任务详情分组数据改成和主页面同源接口

### 改动背景

你这次指出的是任务详情右侧“任务清单和分组”这块，当前弹层里的分组数据要求和主页面分组保持一致，也就是必须直接使用接口返回的数据。排查后发现，主页面任务清单视图会调用 `listSections(projectId)` 拉分组，但详情页这块之前直接读的是父组件缓存的 `tasklists.sections`，两边数据源不一致时就会出现你截图里的偏差。

### 主要修改点

### 1. 详情页单独拉当前清单分组接口

- 文件：`src/components/TaskDetailPanel/index.tsx`
- 处理内容：
  - 引入 `listSections`
  - 新增 `detailTasklistSections`
  - 当 `currentTasklist` 变化时，单独调用 `listSections(currentTasklist.guid)`
  - 把接口返回的 `section_id / name / sort_order / is_default` 映射成前端 `Section`
- 原因：
  - 这样详情页分组候选和主页面任务分组都来自同一个接口，不会再因为本地缓存滞后导致不一致

### 2. 详情页分组展示和候选统一改用接口源

- 文件：`src/components/TaskDetailPanel/index.tsx`
- 处理内容：
  - 新增 `tasklistSectionSource = detailTasklistSections`
  - 当前已添加分组、可选分组搜索结果、当前主分组、子任务默认分组都统一从这份接口分组源计算
- 原因：
  - 只把弹层候选切到接口还不够，红框里显示的已添加分组和默认 section 也必须跟接口源保持一致

### 3. 回归测试补齐“同源接口”约束

- 文件：
  - `tests/task-detail-multi-section-regression.test.mjs`
  - `tests/task-detail-section-source-regression.test.mjs`
- 处理内容：
  - 约束详情页必须引入并调用 `listSections(currentTasklist.guid)`
  - 约束详情页不能再直接用 `currentTasklist.sections` 过滤候选分组
  - 约束主页面和详情页都走同一套 sections 接口来源
- 原因：
  - 防止后续整理详情页时又退回到只读本地缓存的实现

## 异常处理

- 当前清单不存在时，详情页会清空本地分组源，避免展示旧清单分组。
- 接口加载失败时，详情页分组源会回退为空数组，不会把上一个清单的数据残留到当前弹层。
- 接口结果继续先按 `sort_order` 排序，再映射成前端结构，保证和主页面分组顺序一致。

## 可能影响范围

- 任务详情右侧“任务清单和分组”弹层
- 任务详情里当前分组文案展示
- 详情页新建子任务时的默认分组选择
- 不影响主页面任务分组接口和拖拽逻辑

## 测试情况

### 已执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-section-source-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-multi-section-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过

## 本轮补充记录：默认关注人也允许删除，关注列表按真实数据展示

### 改动背景

这轮你把关注人的规则重新定清楚了：默认关注人也必须能删，关注列表要允许删空。按这个规则，前端就不能继续把创建者和负责人强制当成“默认关注人”补回列表，否则用户删掉以后还会重新看到，行为上就是假的删除成功。

### 主要修改点

### 1. 详情页关注人列表不再补默认关注人

- 文件：
  - `src/components/TaskDetailPanel/index.tsx`
- 处理内容：
  - 删除详情页里基于 `buildDefaultParticipantIds` 的关注人展示兜底。
  - `followedUserIds` 现在只根据真实的 `participant_ids` 和 `follower` 成员整理。
  - 所有关注人都继续保留“删除”按钮，不再区分默认关注人和普通关注人。
- 原因：
  - 默认关注人既然也能删，前端就不能再把他们硬补回来。

### 2. 主表关注人数只按真实关注人统计

- 文件：
  - `src/components/TaskTable/index.tsx`
- 处理内容：
  - 删除主表里“创建者和负责人默认也算关注人”的人数兜底。
  - 关注人数现在只基于 `participant_ids` 和 `follower` 角色去重统计。
- 原因：
  - 主表数字要和详情页列表一致，避免详情删空了，主表还显示有人关注。

### 3. 回归测试切到新规则

- 文件：
  - `tests/task-detail-followers-regression.test.mjs`
  - `tests/task-create-default-followers-regression.test.mjs`
- 处理内容：
  - 去掉“默认关注人不可删”和“默认关注人展示兜底”的旧断言。
  - 新增约束：详情页和主表都不能再把创建者、负责人强制补进关注人展示。
  - 保留既有负责人变更、新建任务时补调 participants 接口的约束。
- 原因：
  - 业务规则已经改了，测试必须跟着一起切换。

### 异常处理

- 没有改添加关注人的接口，也没有改删除关注人的接口。
- 这轮只改展示和统计逻辑，避免前端继续把已删除的人补回来。
- 负责人变更、新建任务、新建子任务时仍会按既有逻辑补调 participants；后续如果用户手动删掉，也会按真实数据消失。

### 可能影响范围

- 任务详情里的关注人弹层。
- 主表关注人数列。
- 关注人删到空时的详情页和主表显示。
- 默认关注人和普通关注人的展示一致性。

### 测试情况

### 已执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-followers-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-create-default-followers-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过

## 本轮补充记录：任务列表支持负责人、日期、创建人和字段选项分组

### 改动背景

你这轮要求把任务列表顶部“分组”从当前单一的任务分组，扩成按不同分类条件筛选显示。目标覆盖这几类：

- 负责人
- 开始时间
- 截止时间
- 创建人
- 任务分组
- 自定义字段里的单选/多选选项

也就是说，“分组”不再只是 section 分组，而是要根据不同字段把任务重新组织成不同的分组桶。

### 主要修改点

### 1. 分组模式扩展成系统字段 + 自定义字段

- 文件：
  - `src/components/TaskTable/index.tsx`
- 处理内容：
  - 新增 `BaseGroupModeKey`，支持 `section / none / assignee / start / due / creator`。
  - 新增 `CustomFieldGroupModeKey`，支持 `custom:<fieldId>` 形式的自定义字段分组 key。
  - 分组面板里动态拼出系统分组选项和当前清单下可分组的单选/多选自定义字段。
- 原因：
  - 这轮需求的本质是“统一分组能力”，不能再把分组模式写死成两个值。

### 2. 抽出统一分组函数生成 groupedTasks

- 文件：
  - `src/components/TaskTable/index.tsx`
- 处理内容：
  - 新增 `buildGroupedTasksByMode`，统一产出分组后的 `groupedTasks`。
  - “任务分组”继续沿用 section 逻辑，并保留只展示指定 section 的筛选能力。
  - “负责人”按负责人用户分组，无负责人任务归到“未分配”。
  - “创建人”按创建人分组。
  - “开始时间”分成：已开始、今天、明天、未来 7 天、以后、未安排。
  - “截止时间”分成：已逾期、今天、明天、未来 7 天、以后、未安排。
  - 自定义字段分组支持：
    - 单选字段按当前选项归组；
    - 多选字段会进入多个分组；
    - 未设置归到“未设置”。
- 原因：
  - 所有分组最终都要回到同一个表格渲染结构，统一函数最稳，也方便后续继续扩展。

### 3. 只在“任务分组”模式下保留 section 特有交互

- 文件：
  - `src/components/TaskTable/index.tsx`
- 处理内容：
  - 非 section 分组模式下，不再显示 section 专属操作按钮。
  - 非 section 分组模式下，不再展示“新建任务分组”入口。
  - 非 section 分组模式下，不允许 section 拖拽和 section 级新建入口误触发。
- 原因：
  - 开始时间、截止时间、创建人这些分组桶是计算出来的，不是真正可维护的 section，不能继续复用 section 交互。

### 4. 新增分组回归测试

- 文件：
  - `tests/task-table-grouping-modes-regression.test.mjs`
  - `tests/task-table-toolbar-regression.test.mjs`
- 处理内容：
  - 新增源码级回归，约束分组模式类型、日期分组桶、自定义字段分组支持和统一分组函数。
  - 更新原有工具栏回归，让它继续约束“任务分组模式下的 section 过滤能力”。
- 原因：
  - 这轮分组逻辑扩张比较大，必须用回归把几个关键入口钉住。

### 异常处理

- 日期分组统一走 `getDateGroupKey`，避免开始时间和截止时间分类边界不一致。
- 多选自定义字段会进入多个分组，这轮按产品预期保留多归属行为，不强行压成单组。
- 非 section 分组模式下保留统一表格渲染，不拆成多套表格结构，降低回归风险。

### 可能影响范围

- 任务列表顶部“分组”面板。
- 分组标题行和任务归属。
- 自定义字段单选/多选字段的分组能力。
- section 相关交互只在“任务分组”模式下可用。

### 测试情况

### 已执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-table-grouping-modes-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-table-toolbar-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-table-single-section-table-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过

## 本轮补充记录：任务详情页改成固定右侧抽屉

### 改动背景

你这次要的是把当前任务详情页改成抽屉样式，而且切换不同任务时，抽屉的位置不能跟着变，只能在同一个右侧区域里切内容。参考图里的效果是右侧固定面板，里面的评论、附件、历史记录都随任务变化，但外层位置要稳住。

### 主要修改点

### 1. 任务详情面板改成固定抽屉壳

- 文件：
  - `src/components/TaskDetailPanel/index.tsx`
  - `src/components/TaskDetailPanel/index.less`
- 处理内容：
  - 给详情容器加了抽屉专用 class。
  - 外层改成 `position: fixed`，固定在右侧并贴边显示。
  - 加了独立阴影和层级，视觉上更像飞书右侧抽屉。
  - 保留原来的宽度拖拽能力，抽屉宽度还是可调。
- 原因：
  - 这样切换任务时，不会因为组件重挂载或布局重新计算导致右侧面板位置抖动。

### 2. 切任务时改成“只换内容，不换壳”

- 文件：
  - `src/pages/TaskList.tsx`
  - `src/components/TaskDetailPanel/index.tsx`
- 处理内容：
  - 去掉了 `TaskDetailPanel` 上的 `key={selectedTask.guid}`。
  - 任务切换时不再强制销毁整个详情组件。
  - 在详情组件里用 `useLayoutEffect` 手动清理任务级状态。
  - 清理了评论草稿、历史记录、附件、子任务、分组弹层、预览态和滚动位置。
- 原因：
  - 抽屉外壳要固定，任务切换只是数据切换，不应该把整个面板重建一遍。

### 3. 补了任务切换回归测试

- 文件：
  - `tests/task-detail-drawer-regression.test.mjs`
- 处理内容：
  - 约束详情页不能再靠 `key={selectedTask.guid}` 重挂载。
  - 约束详情容器必须是固定抽屉壳。
  - 约束任务切换时要清理任务级状态。
- 原因：
  - 防止后面有人把抽屉又改回跟随布局流的普通面板。

### 异常处理

- 抽屉内部保留原有编辑逻辑，没有改评论、附件、历史记录的业务流程。
- 切换任务时先清理本地状态，再由接口重新灌新任务数据，避免旧任务内容串进来。
- 没有改左侧列表和主表布局，只动了详情页的承载方式，改动面尽量收小。

### 可能影响范围

- 任务列表页右侧详情抽屉
- 历史记录、评论、附件、子任务这些详情内部模块
- 右侧抽屉宽度拖拽和关闭逻辑

### 测试情况

### 已执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-drawer-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过

## 本轮补充记录：移除子任务详情页的设为独立任务入口

### 改动背景

子任务详情页右上角三点菜单里，会显示“设为独立任务”入口。你这次要求删除这个选项，让子任务详情页的三点菜单只保留当前需要的操作，避免误点后把子任务从父任务下面拆出去。

### 主要修改点

### 1. 删除详情页三点菜单里的独立任务入口

- 文件：`src/components/TaskDetailPanel/index.tsx`
- 处理内容：
  - 删除 `moreMenu` 里按 `task.parent_task_guid` 追加的“设为独立任务”菜单项。
  - 删除菜单点击里的 `detach` 分支。
  - 删除只服务该菜单项的 `handleDetachTask` 处理函数。
- 原因：
  - 当前需求只是不让用户从子任务详情页执行拆分动作，保留无入口的处理函数会增加后续维护误解。

### 2. 同步回归测试

- 文件：`tests/task-menu-items-regression.test.mjs`
- 处理内容：
  - 把任务详情页菜单断言改成“不应该出现设为独立任务入口”。
  - 保留“查看历史记录”和“删除”菜单的约束，避免误删其它必要操作。
- 原因：
  - 防止后续改菜单时又把这个入口加回来。

### 异常处理

- 没有修改设置父任务接口和主列表里的父任务能力，只移除详情页三点菜单入口。
- 没有改删除任务、历史记录、评论、附件和子任务列表逻辑，控制影响范围。

### 可能影响范围

- 子任务详情页右上角三点菜单。
- 普通任务详情页右上角三点菜单。

### 测试情况

### 已执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-menu-items-regression.test.mjs`
  - 结果：先失败确认旧入口存在，修改后通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过

## 本轮补充记录：任务详情抽屉支持点击外部空白关闭

### 改动背景

当前任务详情页已经是固定在右侧的抽屉样式，但关闭方式只有右上角关闭按钮。你这次要求补上更自然的交互：点击抽屉外的空白区域时，也要能直接关闭详情页。

### 主要修改点

### 1. 给详情抽屉补外部点击关闭逻辑

- 文件：`src/components/TaskDetailPanel/index.tsx`
- 处理内容：
  - 给详情抽屉根节点补了 `detailPanelRef`。
  - 新增全局 `pointerdown` 监听，统一判断点击位置。
  - 点到抽屉外部时，直接复用现有 `onClose()` 关闭详情页。
- 原因：
  - 当前详情抽屉没有遮罩层，所以需要自己判断“是不是点到了抽屉外”。

### 2. 放过详情页相关浮层，避免误关闭

- 文件：`src/components/TaskDetailPanel/index.tsx`
- 处理内容：
  - 新增 `isTaskDetailFloatingTarget`，排除 `Popover`、`Dropdown`、`Select` 下拉、日期面板、`Modal` 和富文本浮层。
  - 点到这些 portal 浮层内部时，不触发关闭。
- 原因：
  - 详情页里有很多通过 portal 挂到 body 的浮层，如果不排除，点三点菜单、日期选择器、附件预览时会被误判成点了抽屉外。

### 3. 同步补抽屉关闭回归测试

- 文件：`tests/task-detail-drawer-regression.test.mjs`
- 处理内容：
  - 新增对抽屉根节点 ref、全局 `pointerdown` 监听、内部点击保护和浮层排除的断言。
- 原因：
  - 防止后续改详情抽屉交互时，把这次“点空白关闭”又弄丢。

### 异常处理

- 没有新增遮罩层，也没有改主列表点击逻辑，保持当前布局不变。
- 点击抽屉内部编辑区、滚动区和各种浮层时，都不会触发关闭，避免误操作。

### 可能影响范围

- 任务列表页右侧详情抽屉关闭交互。
- 任务详情里的三点菜单、日期面板、选人浮层和附件预览弹窗。

### 测试情况

### 已执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-drawer-regression.test.mjs`
  - 结果：先失败确认缺少外部点击关闭约束，修改后通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过

## 本轮补充记录：完成任务列表筛选模块

### 改动背景

之前任务列表里的“筛选”还是占位实现，只支持两个简单复选框，和你给的参考图差很多。这次按截图把筛选模块补成真正可用的条件面板，支持一级分类、二级条件、值选择、多条件追加，并且默认按“且”逻辑生效。

### 主要修改点

### 1. 重建筛选条件模型

- 文件：`src/components/TaskTable/index.tsx`
- 处理内容：
  - 新增 `FilterFieldConfig`、`FilterCondition`、`FilterOperator` 等结构。
  - 系统字段里接入负责人、开始时间、截止时间、完成时间、分配人、关注人、创建人、创建时间、更新时间、优先级、任务 ID。
  - 明确排除“任务来源”和“来源类别”，这两个字段当前不进入筛选一级分类。
- 原因：
  - 筛选面板和真实过滤计算都需要统一的数据结构，不能再靠临时复选框拼。

### 2. 自定义字段按类型接入筛选

- 文件：`src/components/TaskTable/index.tsx`
- 处理内容：
  - 新增 `buildCustomFilterFieldConfig`，把自定义字段按类型转成筛选字段。
  - 单选、多选、人员、数值、日期、文本分别走不同的二级条件和第三段值控件。
  - 自定义字段选项值直接复用字段配置里的全部 options。
- 原因：
  - 你要求一级分类和新建字段时选的类型有关，所以不能把所有自定义字段都当成同一种输入。

### 3. 筛选面板改成条件行 UI

- 文件：
  - `src/components/TaskTable/index.tsx`
  - `src/components/TaskTable/index.less`
- 处理内容：
  - 用条件数组渲染筛选行，首行显示“当”，后续默认显示“且”。
  - 支持“添加条件”“清空”和单行删除。
  - 日期类支持“等于 / 早于 / 晚于 / 介于 / 为空 / 不为空”，同时提供“今天 / 本周 / 上周 / 下周 / 本月 / 上月 / 下月 / 指定日期”。
  - 人员、文本、单选、多选类支持“包含 / 不包含 / 为空 / 不为空”。
  - 数值类支持“等于 / 不等于 / 小于 / 小于等于 / 大于 / 大于等于 / 为空 / 不为空”。
- 原因：
  - 这块要对齐你给的飞书截图，不只是能筛，还要把交互结构和层级做出来。

### 4. 接入真实任务过滤计算

- 文件：`src/components/TaskTable/index.tsx`
- 处理内容：
  - `tasksAfterFilter` 改成统一走 `activeFilterConditions.every(...)`。
  - 新增系统字段值提取、自定义字段值提取、日期区间判断、数值比较和包含判断。
  - 工具栏筛选角标改成显示当前有效条件数量。
- 原因：
  - 这样筛选面板不只是展示壳子，而是真的能驱动列表结果变化。

### 5. 补回归测试

- 文件：
  - `tests/task-table-toolbar-regression.test.mjs`
  - `tests/task-table-filter-regression.test.mjs`
- 处理内容：
  - 约束筛选面板不再保留旧 Checkbox 方案。
  - 约束一级分类、二级条件、自定义字段类型映射、默认且逻辑和值控件分支。
- 原因：
  - 防止后续改工具栏或字段模型时把这次筛选模块又改回占位版本。

### 异常处理

- 没有改“保存视图”逻辑，这次只完成筛选模块本身。
- “任务来源”和“来源类别”按你的要求先排除，不进入当前筛选模型。
- 保留了 `TaskList` 上层原有接口，筛选状态在 `TaskTable` 内部独立维护，避免把别的页面联动带坏。

### 可能影响范围

- 任务列表工具栏里的筛选按钮和角标。
- 任务列表的筛选结果计算。
- 自定义字段在筛选面板里的展示和交互。

### 测试情况

### 已执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-table-toolbar-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-table-filter-regression.test.mjs`
  - 结果：先失败确认旧实现缺口，修改后通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过

## 本轮补充记录：任务详情当前分组没有稳定展示

### 改动背景

任务详情右上“任务清单 | 任务分组”这一行，当前任务明明已经有对应分组，但切换任务后有时会退回成“选择分组”。实际排查下来，问题不在接口没数据，而是在详情抽屉切任务时先把分组缓存清空了；如果还是同一个任务清单，分组加载 effect 不会重新执行，缓存就一直空着。

### 主要修改点

### 1. 切任务时同步恢复分组加载态

- 文件：`src/components/TaskDetailPanel/index.tsx`
- 处理内容：
  - 详情抽屉切换任务时，保留原来的 `detailTasklistSections` 清空逻辑。
  - 同时把 `detailTasklistSectionsLoading` 改成根据当前任务是否带 `tasklist_guid` 进入加载态。
- 原因：
  - 这样在新任务分组还没回来的这段时间，界面会继续显示“分组加载中”，不会先闪成“选择分组”。

### 2. 同清单切任务时也重新拉分组接口

- 文件：`src/components/TaskDetailPanel/index.tsx`
- 处理内容：
  - 当前清单分组的加载 effect 依赖从 `currentTasklist` 扩成 `currentTasklist` 和 `task.guid`。
- 原因：
  - 之前只看 `currentTasklist`，同一个任务清单里切不同任务时，这个 effect 不会重跑。
  - 但前面的切任务逻辑已经把分组缓存清空了，所以最后就会停在空态。

### 3. 新增回归测试

- 文件：`tests/task-detail-section-source-regression.test.mjs`
- 处理内容：
  - 增加断言，要求切任务清空分组缓存后要同步进入加载态。
  - 增加断言，要求同一任务清单内切任务时也要重新拉分组接口。
- 原因：
  - 防止后续再把“清空缓存”和“重新加载”拆开，导致这个问题回归。

### 异常处理

- 这次没有改任务接口和任务分组保存逻辑，只修详情抽屉里的状态同步链路，尽量把影响面收在当前 bug 上。
- 如果分组接口本身请求失败，界面仍然会走原来的失败兜底，不会卡死在 loading。

### 可能影响范围

- 任务详情右上任务分组展示
- 同一个任务清单内连续切换任务时的详情状态同步
- 子任务详情里的任务分组展示

### 测试情况

### 已执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-section-source-regression.test.mjs`
  - 结果：先失败确认问题约束存在，修改后通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-multi-section-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-subtask-section-render-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过

## 本轮补充记录：子任务详情不再显示任务清单分组行

### 改动背景

你这次要求把子任务详情里的“任务清单 | 任务分组”这一整行直接隐藏，只让非子任务显示。这个调整是纯展示层需求，不改任务分组的底层数据和普通任务详情的现有能力。

### 主要修改点

### 1. 子任务详情隐藏任务清单分组组件

- 文件：`src/components/TaskDetailPanel/index.tsx`
- 处理内容：
  - 把任务清单分组这一行的渲染条件从 `currentTasklist` 改成 `currentTasklist && !isSubtask`。
- 原因：
  - 这样普通任务仍然保留原来的任务清单和任务分组入口，子任务详情则直接不显示这块区域，符合当前交互要求。

### 2. 回归测试改成子任务不显示

- 文件：`tests/task-detail-subtask-section-render-regression.test.mjs`
- 处理内容：
  - 把原来“子任务详情继续显示任务清单分组”的断言改成“子任务详情不再渲染这行组件”。
  - 保留普通任务详情仍有 `tasklists` 解析逻辑的约束，避免把非子任务能力一起删掉。
- 原因：
  - 防止后续改详情页结构时，把子任务这块入口又带回来。

### 异常处理

- 这次没有改分组接口调用、切换分组逻辑和任务保存逻辑，只调整详情页这一行的显示条件。
- 普通任务详情里的任务清单、分组弹层和分组切换能力保持不变。

### 可能影响范围

- 子任务详情页右上字段区
- 普通任务详情页不受影响

### 测试情况

### 已执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-subtask-section-render-regression.test.mjs`
  - 结果：先失败确认旧逻辑仍在渲染，修改后通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/group-terminology-copy-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-multi-section-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：未通过，当前工作区已有 `src/components/ActivityView/index.tsx` 的无用导入 `UserOutlined`，TypeScript 在这处报 `TS6133`，和本次子任务详情改动无关

## 本轮补充记录：左侧边栏清单拖拽交互补可视化反馈

### 改动背景

左侧边栏现在已经能拖拽清单和清单分组，但拖拽过程里的反馈比较弱，尤其是两个场景不够直观：

- 同一个清单分组里重排顺序时，不容易看清会插到前面还是后面。
- 把清单拖到别的分组里时，目标分组和最终落点提示不够明显。

你这次要求的是先把这块 UI 做清楚，重点是拖拽响应、目标位置和落点反馈更像 antd 组件体系下的交互。

### 主要修改点

### 1. Sidebar 补拖拽中的节点状态和落点状态

- 文件：`src/components/Sidebar/index.tsx`
- 处理内容：
  - 新增 `draggingTasklistKey`，用来标记当前拖拽中的节点。
  - 新增 `dropIndicatorState`，记录当前目标节点和落点位置。
  - Tree 补了 `onDragStart`、`onDragOver`、`onDragEnd`，让拖拽过程能实时驱动 UI 状态。
- 原因：
  - 之前只靠 Tree 默认样式，用户很难判断“当前拖的是谁”和“最终会落到哪”。

### 2. Tree 自定义拖拽把手和落点指示线

- 文件：`src/components/Sidebar/index.tsx`
- 处理内容：
  - `draggable.icon` 改成 antd 的 `DragOutlined`。
  - Tree 补 `dropIndicatorRender`，把默认细线升级成更明显的蓝色落点线。
  - 根分组、普通分组、清单节点标题里补了简短的落点提示标签，比如“放入此分组”“插到前面”“插到后面”。
- 原因：
  - 这样用户拖拽时能同时看到“当前目标节点是谁”和“落点属于插入还是进入分组”。

### 3. Sidebar 样式补拖拽态高亮

- 文件：`src/components/Sidebar/index.less`
- 处理内容：
  - 拖拽中的节点加了浅蓝底、阴影和边框。
  - 目标节点加了整体高亮。
  - `gap-top`、`gap-bottom`、`inner` 三种目标位置分别画了上线、下线和虚线框。
  - 自定义落点指示线改成更明显的渐变蓝线和外发光。
- 原因：
  - 同一个分组内重排更容易看清“会插到前还是后”。
  - 跨分组移动时，分组容器本身也会明确变成目标态。

### 4. 新增拖拽 UI 回归测试

- 文件：`tests/sidebar-drag-ui-regression.test.mjs`
- 处理内容：
  - 增加对拖拽图标、拖拽状态、落点状态、自定义指示线和拖拽样式的断言。
- 原因：
  - 防止后续再把这层拖拽反馈改回成默认弱提示。

### 异常处理

- 这次没有改清单移动到分组的接口调用，也没有改现有的同组内本地重排规则，只补可视化反馈。
- 当前后端仍然没有清单 `sort_order` 的独立持久化接口，所以同组内清单顺序依旧是本地重排体验优化，不是永久排序能力。

### 可能影响范围

- 左侧边栏 Tree 的拖拽交互
- 同组内清单顺序重排的可视化反馈
- 清单拖入其他分组时的目标高亮和落点提示

### 测试情况

### 已执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/sidebar-drag-ui-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/sidebar-expanded-groups-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/sidebar-create-tasklist-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/sidebar-group-action-size-regression.test.mjs`
  - 结果：通过

## 本轮补充记录：动态页按参考图继续压缩间距并放大字号

### 改动背景

你这次要的是把右侧“动态”页继续往参考图靠，重点不是改结构，而是把字号、行距、头像和时间列都调得更像飞书的时间线列表，同时把正文里的 HTML 污染清掉，避免把 `<div>`、`<br />` 直接渲染出来。

### 主要修改点

### 1. 动态列表改成更紧凑的飞书式时间线

- 文件：
  - `src/components/ActivityView/index.tsx`
  - `src/components/ActivityView/index.less`
- 处理内容：
  - 去掉顶部筛选按钮，只保留标题。
  - 每条动态改成“时间列 + 头像列 + 正文列”的三栏结构。
  - 头像从 `20px` 放大到 `24px`。
  - 正文字号和行高一起放大，人物信息和任务名继续高亮。
  - 日期左栏、条目间距、时间列宽度都往参考图靠。
- 原因：
  - 参考图的视觉密度更大，原来的布局太松，扫读感不够强。

### 2. 动态正文先清洗再展示

- 文件：`src/components/ActivityView/index.tsx`
- 处理内容：
  - 复用 `normalizeRichContent`，先把动态里的富文本内容归一化。
  - 再把字符串值转成纯文本，避免 HTML 标签原样出现在列表里。
  - 评论、附件、字段值这些内容都统一走清洗后的展示逻辑。
- 原因：
  - 这样可以防止接口里带出来的富文本污染界面，也更接近历史记录面板的显示方式。

### 3. 同步回归测试

- 文件：`tests/activity-view-regression.test.mjs`
- 处理内容：
  - 把任务点击断言改成真实实现里的 `activity.task_id`。
  - 增加对 `normalizeRichContent`、头像尺寸、字号、行距和高亮色的断言。
  - 增加对顶部筛选按钮移除的断言。
- 原因：
  - 防止后续把这次的视觉密度又改回去。

### 4. 顺手修了构建残留

- 文件：`src/components/TaskDetailPanel/index.tsx`
- 处理内容：
  - 补回项目里已经在用的头像辅助函数，避免构建阶段因为残留改动报错。
  - 放宽了头像入参类型，兼容当前已有的成员/任务人对象。
- 原因：
  - 这块不是需求重点，但如果不先收住，项目会卡在 `tsc` 阶段，没法完成验证。

### 测试情况

### 已执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/activity-view-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：通过

## 本轮补充记录：任务详情人物信息改为显示姓名和有效头像

### 改动背景

任务详情里负责人、关注人、评论作者等人物信息之前会直接显示 `user_id`，和当前接口返回的人物对象不匹配。你这次要求按参考图里的字段关系展示：优先提取 `user_name`，如果 `avatar_url` 有有效值就显示头像图片，如果为空就保留当前默认头像兜底。

### 主要修改点

### 1. 任务详情统一人物展示逻辑

- 文件：`src/components/TaskDetailPanel/index.tsx`
- 处理内容：
  - 新增 `mapTeamMemberToUser`，把团队成员的 `user_name` 和 `avatar_url` 转成前端 `User`。
  - 调整 `getUserDisplayName`，统一用姓名优先、用户 id 兜底。
  - 调整 `renderUserAvatar`，有有效 `avatar` 时传给 `Avatar.src`，没有时继续显示首字母或默认图标。
  - 负责人、子任务负责人、关注人列表、关注人摘要和评论作者都改成走统一展示逻辑。
- 原因：
  - 避免页面不同位置各自拼 `user_id`，后续接口字段变化时只需要维护一套展示规则。

### 2. 评论回复保留 user_id 作为真实提及目标

- 文件：`src/components/TaskDetailPanel/index.tsx`
- 处理内容：
  - 评论作者展示改成 `author_name` 优先。
  - 点击“评论”插入的 `@` 文案显示姓名。
  - `data-mention-id` 和提交的 mention id 继续使用原始 `author_id/user_id`。
- 原因：
  - 前端显示要友好，但后端识别提及对象仍然需要稳定的用户 id。

### 3. 历史记录人物不再把当前用户强制显示成“我”

- 文件：`src/components/TaskDetailPanel/index.tsx`
- 处理内容：
  - 历史记录里当前用户也走真实 `user_name` 展示。
- 原因：
  - 和本轮“显示姓名，不显示 user_id/特殊别名”的规则保持一致。

### 4. 补充回归测试

- 文件：`tests/task-detail-user-profile-regression.test.mjs`
- 处理内容：
  - 增加任务、评论、团队成员人物字段接入约束。
  - 增加详情页姓名展示、头像 src 兜底、评论作者展示和历史记录展示约束。
- 原因：
  - 防止后续又把人物信息退回直接显示 `user_id`。

### 异常处理

- `avatar_url` 为空字符串、空值或无效空白时，不会传给头像图片，继续走当前默认头像兜底。
- 评论回复只替换展示文案，不改变 mention id，避免影响后端通知和提及解析。
- 添加关注人成功后保持弹层打开；切换任务时才关闭弹层，避免状态串到下一条任务。

### 可能影响范围

- 任务详情页负责人、关注人、子任务负责人和评论作者展示。
- 任务详情历史记录里人物展示。
- 评论回复时插入的 `@` 展示文案。

### 测试情况

### 已执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-user-profile-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-followers-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/comment-image-preview-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-multi-assignee-regression.test.mjs`
  - 结果：通过
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：未通过，当前工作区已有 `src/components/Sidebar/index.tsx` 的拖拽 UI 相关未使用变量，例如 `Tag`、`SidebarDropIndicatorState`、`getSidebarDragPlacement`、`dropIndicatorState` 等，TypeScript 报 `TS6133/TS6196`，和本次任务详情人物信息展示改动无关。
