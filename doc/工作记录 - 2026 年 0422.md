# 工作记录 - 2026 年 0422.md

## 本轮记录：删除评论输入组件底部删除线按钮

### 改动背景

你反馈任务详情页底部输入组件里有一个多余的 UI，需要按截图删掉。定位后，这个按钮是富文本工具栏里的“删除线”按钮，也就是底部工具栏靠左那排格式按钮中的 `S` 图标。

### 主要修改点

- 文件：
  - `src/components/TaskRichInput/index.tsx`
  - `tests/task-detail-rich-input-regression.test.mjs`
- 处理方式：
  - 删除工具栏中的“删除线”按钮渲染。
  - 同时移除对应的图标引用，避免保留无用 import。
  - 补一条回归测试，约束工具栏里不再出现删除线按钮和对应 tooltip 文案。

### 异常处理手法

- 只删除这一项按钮，不调整其它格式按钮顺序和发送、上传、@、链接逻辑。
- 用现有富文本回归测试兜底，避免删错按钮或误伤评论输入框其它能力。

### 涉及模块

- `src/components/TaskRichInput/index.tsx`
- `tests/task-detail-rich-input-regression.test.mjs`

### 可能影响范围

- 任务详情页描述输入框工具栏。
- 任务详情页评论输入框工具栏。
- 不影响评论发送、附件上传、图片预览和链接插入。

### 测试情况

- 已执行：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-rich-input-regression.test.mjs`
    - 结果：通过
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/comment-image-preview-regression.test.mjs`
    - 结果：通过
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
    - 结果：通过，仍有既有的 `taskService.ts` 动态/静态重复导入 warning
## 本轮补充记录：评论和更多按钮移到时间同一行

### 改动背景

你反馈任务详情评论卡片里的“评论”和“三点更多”按钮位置不对，现在单独占了一行；你希望它们移动到评论头部，和时间显示在同一行，放在你截图标记的位置。

### 主要修改点

- 文件：
  - `src/components/TaskDetailPanel/index.tsx`
  - `src/components/TaskDetailPanel/index.less`
  - `tests/comment-image-preview-regression.test.mjs`
- 处理方式：
  - 把评论卡片里的 `comment-actions` 从 `comment-meta` 下面挪进 `comment-meta` 内部。
  - 保留原有“评论当前评论”和“更多评论操作”逻辑，只调整 DOM 位置。
  - 用 `margin-left: auto` 让动作区自动顶到右侧，和作者、时间保持同一行。
  - 更新回归测试，明确约束评论按钮和更多按钮必须跟时间同行，不能再单独占一行。

### 异常处理手法

- 不改评论回复预填、更多菜单、删除/编辑事件，只调整布局容器。
- 继续保留 hover 显示按钮的交互，避免因为换位置把按钮一直暴露出来。
- 样式只改评论头部这块，不碰评论正文、附件预览和编辑态输入框。

### 涉及模块

- `src/components/TaskDetailPanel/index.tsx`
- `src/components/TaskDetailPanel/index.less`
- `tests/comment-image-preview-regression.test.mjs`

### 可能影响范围

- 任务详情评论列表里每条评论头部的布局。
- 不影响评论发送、评论编辑、评论回复预填和附件预览逻辑。

### 测试情况

- 已执行：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/comment-image-preview-regression.test.mjs`
    - 结果：通过
- 待执行：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-rich-input-regression.test.mjs`
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`

## 本轮补充记录：任务状态方框提示统一，并把详情页完成入口改到标题前

### 改动背景

你反馈主视图和任务详情页里，任务前面的方框需要加鼠标滑动提示，而且要按当前状态显示不同文案；同时详情页顶部右上角这个“完成任务 / 已经完成任务”入口不够统一，要求改成和主视图一致的方框，放到任务标题前面，并重新整理排版。

### 主要修改点

- 文件：
  - `src/components/TaskTable/index.tsx`
  - `src/components/TaskDetailPanel/index.tsx`
  - `src/components/TaskDetailPanel/index.less`
  - `tests/task-status-checkbox-tooltip-regression.test.mjs`
- 处理方式：
  - 主视图任务行前面的方框增加状态化 tooltip。
  - tooltip 文案按状态区分：
    - 未完成：`标记已完成`
    - 已完成：`标记未完成`
  - tooltip 统一使用黑底白字。
  - 详情页顶部删除原来的“完成任务 / 任务已完成”文字入口，改成标题前 `Checkbox`。
  - 详情页标题区改成横向布局，方框和标题在同一行，已完成任务标题保留删除线视觉。
  - 详情页子任务前面的方框提示文案也同步成同一套状态文案和黑底白字 tooltip，避免交互割裂。

### 异常处理手法

- 当前任务状态切换单独抽成 `handleToggleTaskStatus`，避免标题区里重复写两套 patch 逻辑。
- 只改勾选入口和标题区布局，不改任务状态接口、不改主列表刷新逻辑、不改详情页其它字段区。
- 先补失败回归测试，再做最小改动，避免把这轮 UI 调整扩散到评论、关注人、子任务编辑等其它功能。

### 涉及模块

- `src/components/TaskTable/index.tsx`
- `src/components/TaskDetailPanel/index.tsx`
- `src/components/TaskDetailPanel/index.less`
- `tests/task-status-checkbox-tooltip-regression.test.mjs`

### 可能影响范围

- 主视图任务行前面的任务状态勾选框。
- 任务详情页标题区的状态切换入口和标题排版。
- 任务详情页子任务列表前面的状态勾选提示。
- 不影响任务状态接口、不影响评论区、不影响关注人和附件逻辑。

### 测试情况

- 已执行：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-status-checkbox-tooltip-regression.test.mjs`
    - 结果：通过
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/comment-image-preview-regression.test.mjs`
    - 结果：通过
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-rich-input-regression.test.mjs`
    - 结果：通过
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-parent-chain-regression.test.mjs`
    - 结果：通过
- 待执行：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`

## 本轮补充记录：PERMISSION_DENIED 权限错误做前端可读反馈

### 改动背景

你给了一个典型接口返回：

```json
{
  "success": false,
  "code": "PERMISSION_DENIED",
  "msg": "仅任务创建人、负责人或团队管理员可执行此操作",
  "data": null
}
```

问题不在请求层，因为统一请求封装本来就会抛出后端 `msg`。真正的问题是前端很多任务操作在 `catch` 里直接写死了“更新失败”“状态更新失败”这类提示，把后端返回的权限原因盖掉了，所以用户看不到真实报错。

### 主要修改点

- 文件：
  - `src/components/TaskTable/index.tsx`
  - `src/components/TaskDetailPanel/index.tsx`
  - `tests/permission-denied-feedback-regression.test.mjs`
- 处理方式：
  - 在任务主视图和任务详情里增加统一的错误消息提取函数 `getActionErrorMessage`。
  - 任务状态切换、字段状态切换、自定义字段更新、详情页负责人更新、详情页当前任务状态切换、详情页子任务状态切换等操作，失败时优先展示 `err.message`。
  - 如果异常对象里拿不到后端错误信息，再回退到原来的通用提示文案。

### 异常处理手法

- 不改统一请求层，不改接口协议，继续沿用后端 `success/code/msg` 返回结构。
- 只修正前端把真实错误覆盖掉的关键入口，保证最小改动。
- 用回归测试约束：
  - 请求层继续抛后端 `msg`
  - 主视图和详情页关键任务操作必须通过统一错误提取函数做反馈

### 涉及模块

- `src/components/TaskTable/index.tsx`
- `src/components/TaskDetailPanel/index.tsx`
- `tests/permission-denied-feedback-regression.test.mjs`

### 可能影响范围

- 主视图切换任务状态失败时的提示。
- 主视图更新自定义字段失败时的提示。
- 详情页更新负责人失败时的提示。
- 详情页切换当前任务/子任务状态失败时的提示。
- 不影响接口调用本身，只影响前端错误提示内容。

### 测试情况

- 已执行：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/permission-denied-feedback-regression.test.mjs`
    - 结果：通过
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-status-checkbox-tooltip-regression.test.mjs`
    - 结果：通过
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-parent-chain-regression.test.mjs`
    - 结果：通过
- 待确认：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`

## 本轮补充记录：放大左侧清单分组三点和加号按钮

### 改动背景

你反馈左侧清单分组右侧的三点按钮和添加按钮尺寸太小、显示不明显。随后又补充要求清单行自己的三点按钮也要做同样处理。定位后确认：

- 分组标题右侧的三点和加号共用 `.group-action-btn`。
- 根“任务清单”和各分组标题右侧的加号也共用 `.group-action-btn`。
- 清单行自己的三点按钮使用 `.tasklist-action-btn`，原来仍是 `20px` 尺寸、`12px` 字号和浅灰色。

### 主要修改点

- 文件：
  - `src/components/Sidebar/index.less`
  - `tests/sidebar-group-action-size-regression.test.mjs`
- 处理方式：
  - 将分组右侧操作按钮和清单行右侧三点按钮尺寸统一调整为 `26px`。
  - 将图标字号统一调整为 `15px`。
  - 将按钮颜色统一改成 `#1f2329`，提高默认可见性。
  - 增加 `font-weight: 700`，并给 Ant Design 的 SVG 图标补 `stroke` 和 `stroke-width`，避免只设置字体粗细但图标本身不变粗。

### 异常处理手法

- 只调整 `.group-action-btn` 和 `.tasklist-action-btn` 的视觉样式，不改新增清单和更多菜单的点击逻辑。
- 保留原来的悬停显示机制，避免侧边栏常态下显得过于拥挤。
- 用回归测试约束按钮尺寸、字号、颜色和 SVG 加粗方式，防止后续样式回退。

### 涉及模块

- `src/components/Sidebar/index.less`
- `tests/sidebar-group-action-size-regression.test.mjs`

### 可能影响范围

- 左侧边栏清单分组标题右侧的三点按钮。
- 左侧边栏清单分组标题右侧的添加按钮。
- 左侧边栏清单行右侧的三点按钮。
- 不影响任务详情页和任务表格。

### 测试情况

- 已执行：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/sidebar-group-action-size-regression.test.mjs`
    - 结果：通过
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/sidebar-create-tasklist-regression.test.mjs`
    - 结果：通过
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/sidebar-dark-font-regression.test.mjs`
    - 结果：通过
- 构建情况：
  - 已执行 `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 结果：未通过，阻塞点来自既有未提交改动：
    - `src/components/TaskDetailPanel/index.tsx` 第 635 行和第 638 行，`filter(Boolean)` 后仍被 TypeScript 推断成 `(string | undefined)[]`。
    - `src/components/TaskTable/index.tsx` 存在 `FlagOutlined` / `HistoryOutlined` 重复导入，以及 `TaskTitleCellProps` 缺少 `onDeleteTask`、`onCancelTask` 传参。
  - 这些构建错误和本轮侧边栏按钮样式调整无关。

## 本轮补充记录：顶部用户切换下拉增加切换团队

### 改动背景

你反馈团队管理页左上角这个下拉里，需要新增“切换团队”的逻辑。这里现在只有“切换当前身份”，也就是只支持在当前团队下切换 user_id，不支持在当前 user_id 参加的多个团队之间切换，所以点开后没法直接切换团队上下文。

### 主要修改点

- 文件：
  - `src/config/appConfig.ts`
  - `src/components/UserSwitcher/index.tsx`
  - `tests/user-switcher-team-switch-regression.test.mjs`
- 处理方式：
  - 给 `appConfig` 增加 `current_team_id` 的本地持久化读取逻辑。
  - 新增 `switchCurrentTeam(teamId)`，和现有 `switchCurrentUser(userId)` 一样，切换后直接刷新页面。
  - 顶部 `UserSwitcher` 打开时同时拉成员列表和团队列表。
  - 下拉菜单新增“切换团队”分组，展示当前 `user_id` 参加的团队。
  - 当前团队会显示选中勾和“当前”标签，点其它团队后完成切换并刷新页面。

### 异常处理手法

- 保持最小改动，不改团队管理页主体，不改团队接口协议，只在现有切换下拉里补团队维度。
- 团队和成员列表共用一次加载态，避免点开下拉时出现两套不同步的空状态。
- 加载失败时统一提示“加载切换列表失败”，避免沿用旧的“加载成员失败”文案把问题说偏。

### 涉及模块

- `src/config/appConfig.ts`
- `src/components/UserSwitcher/index.tsx`
- `tests/user-switcher-team-switch-regression.test.mjs`

### 可能影响范围

- 左侧边栏顶部用户切换按钮的下拉内容。
- 页面刷新后的当前团队上下文。
- 依赖 `appConfig.team_id` 的任务、清单、分组、团队相关请求会在切换后自动走新团队。
- 不影响现有“切换当前身份”的逻辑。

### 测试情况

- 已执行：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/user-switcher-team-switch-regression.test.mjs`
    - 结果：先失败后通过，已验证本轮新增团队切换能力确实生效
- 本轮未执行：
  - `npm run build`

## 本轮补充记录：任务行内新建输入行与表头列对齐

### 改动背景

你反馈主视图里在分组下新建任务时，代填入的那一行和表头列没有对齐。现象上看，新建态这一行像一块单独浮起来的卡片，输入框和后面的字段也没有严格贴住表头列宽；而你给的参考图里，新建行应该更像真正嵌在表格里的那一行。

### 主要修改点

- 文件：
  - `src/components/TaskTable/index.less`
  - `tests/tasklist-inline-create-layout-regression.test.mjs`
- 处理方式：
  - 调整 `.inline-create-row` 的布局样式，去掉额外外边距、圆角和整块蓝色描边。
  - 让行内新建各内部 `.cell` 默认按列宽拉伸，避免只按内容宽度挤压。

## 本轮补充记录：评论输入框改成 Enter 发送、Shift+Enter 换行

### 改动背景

你要求把任务详情页评论区的输入行为改成更接近日常聊天工具的方式：直接按 `Enter` 发送评论，按 `Shift+Enter` 保留换行。这里需要注意，任务描述输入区还是原来的编辑逻辑，不能一起改掉。

### 主要修改点

- 文件：
  - `src/components/TaskRichInput/index.tsx`
  - `tests/task-detail-rich-input-regression.test.mjs`
- 处理方式：
  - 在统一富文本输入组件里，只针对 `comment` 和 `comment-edit` 两种模式拦截回车。
  - `Enter` 直接触发现有 `onSubmit`，复用原来的评论发送和评论编辑保存逻辑。
  - `Shift+Enter` 不拦截，继续让编辑器正常换行。
  - 增加输入法组合态保护，中文输入上屏时按回车不会误触发送。
  - 同步更新发送按钮的 tooltip 文案，避免页面提示和真实行为不一致。

### 异常处理手法

- 发送前继续复用现有 `isRichContentEmpty` 判断，空内容不会因为键盘提交被发出去。
- 只改评论相关模式，不改 `description` 模式，避免把任务描述区的“回车编辑、失焦保存”逻辑带坏。
- 中文输入法通过组合态标记单独兜底，减少候选词确认时误发送的风险。

### 涉及模块

- `src/components/TaskRichInput/index.tsx`
- `tests/task-detail-rich-input-regression.test.mjs`

### 可能影响范围

- 任务详情页底部评论输入框。
- 任务详情页评论编辑输入框。
- 不影响任务描述输入区、不影响 @ 提及、链接插入、图片/附件上传逻辑。

### 测试情况

- 已执行：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-rich-input-regression.test.mjs`
    - 结果：通过
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/comment-image-preview-regression.test.mjs`
    - 结果：通过

## 本轮补充记录：任务创建时默认把创建人和负责人同步到关注人

### 改动背景

你补充了一个明确规则：当前“关注的人”在任务创建时，应该默认把创建人和已选负责人也同步进去。现状是前端新建任务时只传了 `assignee_id`，没有补 `participants`，所以刚创建出来的任务不会自动带上默认关注人。

### 主要修改点

- 文件：
  - `src/services/taskService.ts`
  - `src/components/TaskTable/index.tsx`
  - `src/components/TaskDetailPanel/index.tsx`
  - `tests/task-create-default-followers-regression.test.mjs`
- 处理方式：
  - 在任务服务层增加 `applyParticipantIdsToTask`，统一把 `participant_ids` 和本地 `members.follower` 同步起来。
  - 主表行内新建任务成功后，整理默认关注人集合：
    - 创建人
    - 已选负责人
  - 详情页新建子任务成功后，同样整理：
    - 当前创建人
    - 子任务负责人
  - 两个创建入口都在创建成功后补调 `addParticipants` 接口，再用共享函数把本地 `createdTask` 立即补齐，避免页面必须刷新才能看到关注人数和关注人头像。

### 异常处理手法

- 默认关注人集合统一去重，避免“创建人和负责人是同一个人”时重复写入。
- 只复用现有 `participants` 接口，不额外引入新的“创建任务带关注人”协议假设，降低和后端接口定义冲突的风险。
- 本地状态同步抽到服务层一个小函数里，避免主表和详情页各自手写 `participant_ids`/`members` 合并逻辑，减少后续再改漏一边的概率。

### 涉及模块

- `src/services/taskService.ts`
- `src/components/TaskTable/index.tsx`
- `src/components/TaskDetailPanel/index.tsx`
- `tests/task-create-default-followers-regression.test.mjs`

### 可能影响范围

- 主表行内新建任务后的默认关注人展示
- 详情页新建子任务后的默认关注人展示
- 主表“关注人”列人数统计
- 详情页关注人摘要和弹层列表
- 不影响已有的手动添加/删除关注人逻辑

### 测试情况

- 已执行：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-create-default-followers-regression.test.mjs`
    - 结果：通过
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-followers-regression.test.mjs`
    - 结果：通过
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/tasklist-inline-create-blur-regression.test.mjs`
    - 结果：通过
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-subtask-actions-regression.test.mjs`
    - 结果：通过
  - 保留标题列主宽度，但让标题输入框 `width: 100%`，铺满当前标题列。
  - 让这行视觉上回归表格内部行，而不是独立浮层，和参考图更接近。

### 异常处理手法

- 不改行内新建的提交逻辑，不改点空白保存、回车保存、分组头部加号触发逻辑。
- 只改 `TaskTable` 里的新建行样式，不碰普通任务行、详情页、筛选区和分组头部布局。
- 先补失败回归，再做最小样式调整，避免把“错位”问题修成另一套交互。

### 涉及模块

- `src/components/TaskTable/index.less`
- `tests/tasklist-inline-create-layout-regression.test.mjs`

### 可能影响范围

- 主视图分组下的“行内新建任务”编辑态布局。
- 不影响新建任务接口、不影响点空白保存和回车保存逻辑。

### 测试情况

- 已执行：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/tasklist-inline-create-layout-regression.test.mjs`
    - 结果：通过
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/tasklist-inline-create-blur-regression.test.mjs`
    - 结果：通过
- 待确认：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`

## 本轮补充记录：行内新建任务改为按真实表格列逐列渲染

### 改动背景

在上一轮只改样式后，你继续指出关键点：新建行不只是“看起来顺眼”，而是应该和表头字段一一对齐，或者同步调整表头分布。继续维持“整行合并成一个大单元格，再自己用 flex 排版”的方案，理论上就做不到真正逐列对齐，所以这轮把实现方式改了。

### 主要修改点

- 文件：
  - `src/components/TaskTable/index.tsx`
  - `src/components/TaskTable/index.less`
  - `tests/tasklist-inline-create-layout-regression.test.mjs`
- 处理方式：
  - 删除行内新建任务对整行 `colSpan` 合并的做法。
  - 将行内新建状态改成按真实表格列逐列渲染：
    - 标题列
    - 优先级列
    - 负责人列
    - 开始时间列
    - 截止时间列
    - 创建人列
    - 预估工时/创建时间等占位列
  - 外部点击保存的判定从原来的内层假行节点，改成绑定到真实的 `.inline-create-table-row`。
  - 新建行样式改成基于真实 `td` 容器排版，保证和表头宽度、列分布天然同步。

### 异常处理手法

- 不改新建任务接口，不改“回车确认”“点空白保存”“分组头部加号触发行内新建”等行为。
- 只重构 `inlineCreate` 这一类行的渲染方式，不碰普通任务行、分组行和详情页逻辑。
- 先把回归测试改成约束“真实列对齐”，再实施结构调整，避免继续在错误方案上叠样式补丁。

### 涉及模块

- `src/components/TaskTable/index.tsx`
- `src/components/TaskTable/index.less`
- `tests/tasklist-inline-create-layout-regression.test.mjs`

### 可能影响范围

- 主视图里分组下的新建任务编辑行。
- 行内新建任务的点空白保存判定。
- 不影响普通任务行的列渲染和数据更新逻辑。

### 测试情况

- 已执行：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/tasklist-inline-create-layout-regression.test.mjs`
    - 结果：通过
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/tasklist-inline-create-blur-regression.test.mjs`
    - 结果：通过
- 待确认：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`

## 本轮补充记录：任务详情支持同一清单挂多个任务分组

### 改动背景

你反馈任务详情页右侧“任务清单和分组”这里，现在同一个任务在当前清单里只能保留一个任务分组；你要求改成支持挂到当前清单下多个任务分组，并且弹层样式参考飞书任务：

- 红框区域展示已经添加的任务分组
- 蓝框区域展示搜索中的任务分组候选列表

排查后发现根因有两层：

- 详情页一直只读 `task.tasklists[0]`，前端状态天然只会保留一个任务分组
- 详情页虽然曾尝试通过 `handleTaskPatch({ tasklists })` 回写，但 `handleTaskPatch` 和 `updateTaskApi` 实际并没有把 `tasklists` 透传到接口层，所以保存也不生效

### 主要修改点

- 文件：
  - `src/components/TaskDetailPanel/index.tsx`
  - `src/components/TaskDetailPanel/index.less`
  - `src/services/taskService.ts`
  - `tests/task-detail-multi-section-regression.test.mjs`
- 处理方式：
  - 详情页不再把任务分组固定成 `task.tasklists[0]`，改成先筛出“当前清单下的全部任务分组引用”。
  - 保留“第一个任务分组作为当前主分组”的兜底逻辑，避免子任务创建、旧展示文案这些现有链路被意外改坏。
  - 任务分组弹层改成两块：
    - 已添加任务分组：用 chip/tag 形式展示，支持单个移除
    - 搜索中的任务分组：输入关键词后在列表里展示当前清单下还没添加的候选分组，点击即可添加
  - 当当前清单下只剩最后一个任务分组时，禁用移除按钮，避免任务被误移成“当前清单无分组引用”的脏状态。
  - `handleTaskPatch` 正式支持把 `tasklists` 映射到接口 patch。
  - `updateTaskApi` 增加 `tasklists?: TasklistRef[]`，让详情页的多任务分组变更能真正回写后端。
  - 补充独立回归测试，约束：
    - 服务层支持 `tasklists` patch
    - 详情页不再依赖 `task.tasklists[0]`
    - 详情页必须包含“已添加任务分组 + 搜索任务分组列表”的结构和样式类名

### 异常处理手法

- 只改任务详情页“任务清单和分组”这一块，不动评论、附件、关注人、父任务链等无关逻辑。
- 对子任务新建逻辑继续沿用“当前主分组”作为默认 `section_id`，避免这轮把原来稳定的子任务归组行为也一起改掉。
- 任务分组增删失败时优先展示真实错误信息，成功后再给明确成功提示。

### 涉及模块

- `src/components/TaskDetailPanel/index.tsx`
- `src/components/TaskDetailPanel/index.less`
- `src/services/taskService.ts`
- `tests/task-detail-multi-section-regression.test.mjs`

### 可能影响范围

- 任务详情页右侧“任务清单和分组”的展示和交互
- 任务详情页里把任务加入当前清单多个任务分组的能力
- 子任务创建时默认继承的当前主分组
- 不影响主任务表格的分组渲染逻辑；主表本来就按 `task.tasklists.some(...)` 判断，能直接复用这次详情页回写后的多分组数据

### 测试情况

- 已执行：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-multi-section-regression.test.mjs`
    - 结果：通过
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-rich-input-regression.test.mjs`
    - 结果：通过
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-followers-regression.test.mjs`
    - 结果：通过
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/comment-image-preview-regression.test.mjs`
    - 结果：通过
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
    - 结果：通过，仍有既有的 `taskService.ts` 动态/静态重复导入 warning

## 本轮补充记录：侧栏 Tree 选中保护，避免空节点 scrollTo 报错

### 改动背景

你贴了两个报错，其中前端这条是：

```text
Uncaught TypeError: Cannot read properties of null (reading 'scrollTo')
```

堆栈落在 antd Tree 内部。排查后确认不是你业务代码里直接调了 `scrollTo`，而是侧栏 `Tree` 在受控 `selectedKeys` 模式下，拿到了一个当前 `treeData` 里还不存在的 key，内部尝试滚动定位激活节点时命中了空引用。

这类时机一般出现在：

- 当前激活清单 `activeKey` 已经切换
- 但侧栏的 `projects/treeData` 还没同步刷新出来
- 组件仍然把目标 key 直接传给了 `Tree.selectedKeys`

### 主要修改点

- 文件：
  - `src/components/Sidebar/index.tsx`
  - `tests/sidebar-selected-key-guard-regression.test.mjs`
- 处理方式：
  - 在 `Sidebar` 里新增 `selectableTreeKeySet`，递归遍历当前 `treeData`，收集真实存在且可选的节点 key。
  - 计算 `selectedKeys` 时，不再直接返回 `encodeTasklistKey(activeKey.guid)`，而是先判断这个 key 是否已经存在于当前树里。
  - 如果 key 还没进 `treeData`，先返回空数组，等树节点真正渲染出来后再恢复选中。
  - 关键判断处补了中文注释，说明这是为了避免 Tree 内部滚动定位不存在节点。

### 异常处理手法

- 只改侧栏 `selectedKeys` 的生成逻辑，不动树的展开、拖拽、创建清单、分组排序这些现有行为。
- 通过当前 `treeData` 做白名单过滤，属于纯前端保护，不依赖额外状态，不会引入新的异步竞态。

### 涉及模块

- `src/components/Sidebar/index.tsx`
- `tests/sidebar-selected-key-guard-regression.test.mjs`

### 可能影响范围

- 侧栏任务清单树的选中态同步。
- 可以避免在清单切换、创建清单、侧栏数据刷新等时机下，Tree 因为不存在的选中节点触发内部空指针。
- 不影响树的展开收起、拖拽分组、拖拽清单和菜单操作。

### 测试情况

- 已执行：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/sidebar-selected-key-guard-regression.test.mjs`
    - 结果：通过
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/sidebar-expanded-groups-regression.test.mjs`
    - 结果：通过
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
    - 结果：通过，仍有既有的 `taskService.ts` 动态/静态重复导入 warning

## 补充说明：Mongo `idx_involved` 索引冲突属于后端问题

### 现象说明

你贴的另一条错误是：

```text
Index must have unique name ... existing index name: "idx_involved"
```

这个报错发生在创建任务接口：

- 前端触发点：`src/components/TaskTable/index.tsx` 里的行内新建任务
- 请求入口：`src/services/taskService.ts` 里的 `createTaskApi`
- 最终失败位置：后端返回的 Mongo `IndexKeySpecsConflict`

### 根因判断

现有 Mongo 里已经有一个名字叫 `idx_involved` 的索引：

- 旧 key：`{ involved_user_ids: 1, is_deleted: 1, status: 1 }`

但当前后端又试图创建另一个同名索引：

- 新 key：`{ involved_user_ids: 1, is_deleted: 1, is_completed: 1 }`

Mongo 要求：

- 索引名必须唯一
- 同名索引的 key 规格也必须一致

所以这不是前端参数问题，而是后端索引迁移没有处理干净。

### 后端建议修法

后端二选一：

- 方案 1：给新索引换一个新名字，比如 `idx_involved_completed`
- 方案 2：如果确认要废弃旧索引，先显式删除旧的 `idx_involved`，再创建新的 `idx_involved`

如果后端是自动 `create_index` / `createIndex`：

- 不要在保留旧索引的情况下，直接复用原索引名去创建不同 key 的索引
- 最好给索引初始化加幂等迁移逻辑，先检查已有索引名和 key 是否一致，再决定跳过、删旧重建，还是新建新名字

### 当前处理边界

- 这个仓库里没有后端索引初始化代码，所以这轮没有在本仓库里直接修 `idx_involved`
- 前端这边保留真实后端报错透出，不会把它吞成模糊的“创建失败”

## 本轮补充记录：任务完成态改为读取接口 `is_completed`

### 改动背景

你说明了任务状态切换接口的新返回结构，当前任务是否完成不再靠旧的 `status` 字段判断，而是明确通过：

```json
"is_completed": true
```

来表示当前任务已完成。前端现状是：

- 勾选框和完成态样式仍然统一读 `task.status === 'done'`
- 但 `task.status` 的来源还是 `apiTaskToTask(api.status)`

这会导致一个问题：

- 如果后端已经切到 `is_completed`
- 但 `status` 没再返回旧的 `done`
- 前端映射出来的 `task.status` 就可能不是 `done`
- 最终表现为任务名前面的方框勾选状态不对

### 主要修改点

- 文件：
  - `src/services/taskService.ts`
  - `tests/task-completion-flag-regression.test.mjs`
  - `tests/custom-field-api-integration-regression.test.mjs`
- 处理方式：
  - `ApiTask` 模型增加 `is_completed: boolean`
  - `normalizeTaskStatus` 改成同时接收 `status` 和 `is_completed`
  - 归一化逻辑里优先判断：
    - `is_completed === true` 时，直接映射为前端 `Task.status = 'done'`
  - `apiTaskToTask` 改为：
    - `status: normalizeTaskStatus(api.status, api.is_completed)`
  - 主视图、详情页和活动流里的勾选框逻辑不额外分叉，继续读取映射后的 `task.status === 'done'`，这样改动最小，也不会把 UI 逻辑搞成两套判断
  - 同步更新原有状态相关回归测试，允许状态归一化函数读取新的 `is_completed`

### 异常处理手法

- 不去大面积改所有 UI 里的完成态判断，而是把兼容统一收口到服务层映射，避免同一语义在多个组件里散着判断。
- 保留旧的 `status` 兼容：
  - 如果 `is_completed` 为 `false`，仍然继续读 `status` 里的 `in_progress`、`cancelled` 等真实状态值。

### 涉及模块

- `src/services/taskService.ts`
- `tests/task-completion-flag-regression.test.mjs`
- `tests/custom-field-api-integration-regression.test.mjs`

### 可能影响范围

- 主任务表格里任务名前面的状态勾选框
- 任务详情页标题前面的状态勾选框
- 活动流和其它读取 `task.status` 判断完成态的区域
- 系统状态字段下拉的真实状态展示

### 测试情况

- 已执行：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-completion-flag-regression.test.mjs`
    - 结果：通过
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-status-checkbox-tooltip-regression.test.mjs`
    - 结果：通过
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/custom-field-api-integration-regression.test.mjs`
    - 结果：通过
- 待确认：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`

## 本轮补充记录：全局字体改成更接近参考图的中文字体栈

### 改动背景

你要求把当前页面上的字体观感对齐到参考图。排查后发现项目全局 `body` 现在的字体栈是：

- 先走 `-apple-system / Segoe UI / Roboto / Helvetica Neue / Arial`
- 最后才回落到 `PingFang SC / Hiragino Sans GB / Microsoft YaHei`

这样在中文界面里，浏览器会优先命中前面的通用系统字体，中文文字观感会和参考图里飞书任务常见的 `PingFang SC` 风格不够一致。

### 主要修改点

- 文件：
  - `src/index.css`
  - `tests/global-font-family-regression.test.mjs`
- 处理方式：
  - 只调整全局 `body` 的 `font-family` 顺序。
  - 把中文字体前置为：
    - `PingFang SC`
    - `Hiragino Sans GB`
    - `Microsoft YaHei`
  - 再回落到系统英文字体：
    - `-apple-system`
    - `BlinkMacSystemFont`
    - `Segoe UI`
    - `Roboto`
    - `Helvetica Neue`
    - `Arial`
  - 不改代码块、预览区等本来就该使用等宽字体的局部样式。

### 异常处理手法

- 只改全局字体栈顺序，不改字号、字重、行高和任何布局参数，避免因为字体联动把宽度、间距和换行逻辑一起放大修改。
- 用独立回归测试约束 `body` 的字体栈顺序，避免后续又被其它全局样式改回去。

### 涉及模块

- `src/index.css`
- `tests/global-font-family-regression.test.mjs`

### 可能影响范围

- 整个任务页面的中文文字观感
- 包括侧栏、任务列表、详情页、工具栏等所有继承 `body` 字体的区域
- 不影响文件预览代码块等单独声明等宽字体的区域

### 测试情况

- 已执行：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/global-font-family-regression.test.mjs`
    - 结果：通过
- 待确认：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`

## 本轮补充记录：左侧边栏文字统一改成深黑色

### 改动背景

你要求把左侧边栏里的字体统一改成深黑色。排查后发现当前边栏里仍有几类文字在用偏灰色：

- 菜单分组标题
- 树里的清单分组标题
- 底部“新建清单分组”按钮文案

这些灰色在当前白底边栏里会显得偏轻，和你要的“深黑色字体”不一致。

### 主要修改点

- 文件：
  - `src/components/Sidebar/index.less`
  - `tests/sidebar-dark-font-regression.test.mjs`
- 处理方式：
  - 菜单分组标题 `ant-menu-item-group-title` 颜色改为 `#1f2329`
  - 树分组标题 `.tree-section > .ant-tree-node-content-wrapper` 颜色改为 `#1f2329`
  - 树分组标题 hover 时也保持深黑，不再切到浅灰
  - 底部“新建清单分组”按钮文案改为 `#1f2329`
  - 保留 hover 蓝色、选中蓝色和功能图标原有交互，不把整套状态色都一起抹掉

### 异常处理手法

- 只改左侧边栏文字颜色，不改边栏宽度、间距、字号、hover 背景和选中态背景。
- 用独立回归测试约束这三类关键文字颜色，避免后续样式合并时又被改回灰色。

### 涉及模块

- `src/components/Sidebar/index.less`
- `tests/sidebar-dark-font-regression.test.mjs`

### 可能影响范围

- 左侧边栏主菜单分组标题
- 左侧任务清单树里的分组标题
- 左侧底部“新建清单分组”入口
- 不影响任务清单选中蓝色态，不影响图标 hover 交互

### 测试情况

- 已执行：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/sidebar-dark-font-regression.test.mjs`
    - 结果：通过
- 待确认：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`

## 本轮补充记录：所有任务分组名和任务名统一改成深黑色

### 改动背景

你要求把页面里所有“任务分组”和“任务”的名字统一改成深黑色。结合当前页面结构，实际涉及的是：

- 主任务表格里的任务分组名称
- 主任务表格里的任务名称
- 左侧边栏里的任务分组名称
- 左侧边栏里的任务清单名称
- 任务详情页标题
- 任务详情页里的任务分组标签名称
- 任务详情页里的子任务名称

当前样式里有一部分名称已经是深黑色，但已完成任务标题、子任务标题、部分分组名仍会切成灰色，所以视觉上不统一。

### 主要修改点

- 文件：
  - `src/components/TaskTable/index.less`
  - `src/components/TaskDetailPanel/index.less`
  - `src/components/Sidebar/index.less`
  - `tests/task-name-dark-color-regression.test.mjs`
- 处理方式：
  - 主任务表格：
    - `.section-name` 改成深黑色
    - `.task-title-cell.done .done-text` 改成深黑色，保留删除线
  - 任务详情页：
    - 已完成主任务标题保持深黑色，保留删除线
    - 任务分组标签名称改成深黑色
    - 子任务标题改成深黑色
    - 已完成子任务标题保持深黑色，保留删除线
  - 左侧边栏：
    - 任务分组名称 `.group-name` 改成深黑色
    - 任务清单名称本身已经是深黑色，这轮回归里一并约束住

### 异常处理手法

- 只改“名字文本”的颜色，不改计数、时间、描述、icon、hover 状态这些辅助信息的颜色。
- 已完成态只保留删除线，不再通过切灰来表达，避免和你要求的“名字都改为深黑色”冲突。
- 用独立回归测试覆盖主表、详情页、侧栏三块，避免只改到一部分页面。

### 涉及模块

- `src/components/TaskTable/index.less`
- `src/components/TaskDetailPanel/index.less`
- `src/components/Sidebar/index.less`
- `tests/task-name-dark-color-regression.test.mjs`

### 可能影响范围

- 主任务列表中的任务分组名和任务名
- 左侧边栏中的任务分组名和任务清单名
- 任务详情页中的任务标题、任务分组标签名、子任务名
- 不影响辅助字段颜色，不影响 hover/选中态背景

### 测试情况

- 已执行：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-name-dark-color-regression.test.mjs`
    - 结果：通过
- 待确认：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`

## 本轮补充记录：左侧边栏支持拖拽调宽

### 改动背景

你要求左侧边栏也像任务详情面板一样，支持通过拖拽边缘来动态调整宽度。

排查后发现当前实现里：

- 左侧边栏在 `TaskList` 页层用 `Sider width={240}` 写死
- 任务详情面板已经有完整的拖拽调宽实现

所以这轮最稳的方案不是在 `Sidebar` 组件内部重新造一套，而是直接在 `TaskList` 页层复用任务详情同样的拖拽模式。

### 主要修改点

- 文件：
  - `src/pages/TaskList.tsx`
  - `src/pages/TaskList.less`
  - `tests/sidebar-resize-regression.test.mjs`
- 处理方式：
  - 页面层新增 `sidebarWidth` 状态，默认宽度 240。
  - 新增 `sidebarResizeStateRef` 记录拖拽起点和初始宽度。
  - 监听全局 `pointermove / pointerup`：
    - 拖动时实时计算新的侧栏宽度
    - 结束时清理拖拽态
  - 给侧栏 `Sider` 增加独立的右侧拖拽手柄 `sidebar-resize-handle`。
  - 宽度做边界限制：
    - 最小 `180`
    - 最大 `360`
  - 拖拽开始时给 `body` 加 `sidebar-resizing`，避免拖动时误选中文字，并统一显示左右拖拽光标。
  - 手柄视觉和任务详情的调宽条保持同一风格：默认透明，hover 时显示蓝色细线。

### 异常处理手法

- 只在页面层管理左侧边栏宽度，不改 `Sidebar` 组件内部结构，避免把拖拽逻辑和边栏业务逻辑耦在一起。
- 收起侧边栏的逻辑保持原样，只有展开态才显示拖拽手柄。
- 拖拽结束时统一移除 `sidebar-resizing`，避免异常中断后页面一直残留 resize 光标。

### 涉及模块

- `src/pages/TaskList.tsx`
- `src/pages/TaskList.less`
- `tests/sidebar-resize-regression.test.mjs`

### 可能影响范围

- 左侧边栏展开态下的宽度调整体验
- 左侧边栏与主内容区的横向占比
- 不影响侧边栏收起/展开逻辑，不影响任务详情面板的调宽逻辑

### 测试情况

- 已执行：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/sidebar-resize-regression.test.mjs`
    - 结果：通过
- 待确认：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`

## 本轮补充记录：去掉左侧边栏底部横向滚动条，并收口侧栏拖拽手感

### 改动背景

你反馈左侧边栏底部出现了一条左右滚动条，界面上会多出一块灰色横条，影响观感。

顺着这块一起排查后，确认有两个相关点：

- 左侧边栏容器只开了纵向滚动，没有明确禁止横向溢出，所以内容宽度在边界上抖出来时会把底部横向滚动条带出来。
- 左侧边栏拖拽调宽虽然已经接上了，但它用的是 antd `Sider`，而 `Sider` 自带宽度过渡动画；如果不在拖拽时关掉这个过渡，手感会比任务详情面板发黏。

### 主要修改点

- 文件：
  - `src/components/Sidebar/index.less`
  - `src/pages/TaskList.tsx`
  - `src/pages/TaskList.less`
  - `tests/sidebar-overflow-regression.test.mjs`
  - `tests/sidebar-resize-regression.test.mjs`
- 处理方式：
  - 给 `.sidebar` 增加 `overflow-x: hidden`，保留 `overflow-y: auto`，只允许左侧边栏纵向滚动。
  - 保留左侧边栏拖拽能力，同时把拖拽宽度计算恢复成正确方向：
    - 手柄在右边缘
    - 向右拖时侧栏变宽
  - 保留拖拽态下关闭 `Sider` 过渡动画的处理，避免宽度跟手势不同步。
  - 新增“侧栏禁止横向滚动”的回归测试，并同步修正侧栏拖拽回归测试对 Less 嵌套写法的匹配方式。

### 异常处理手法

- 只修侧栏容器的横向溢出，不改树节点、菜单项和分组创建区域的布局结构，避免把这轮问题扩散成大范围样式调整。
- 拖拽逻辑继续沿用现有 `pointermove / pointerup + body class` 生命周期，不另外引入新状态或新组件。
- 拖拽手感问题只针对 `Sider` 的过渡动画做收口，不改任务详情面板已有实现。

### 涉及模块

- `src/components/Sidebar/index.less`
- `src/pages/TaskList.tsx`
- `src/pages/TaskList.less`
- `tests/sidebar-overflow-regression.test.mjs`
- `tests/sidebar-resize-regression.test.mjs`

### 可能影响范围

- 左侧边栏底部不再出现横向滚动条。
- 左侧边栏拖拽调宽时的跟手性更稳定。
- 不影响侧边栏收起/展开逻辑，不影响任务详情面板的拖拽逻辑。

### 测试情况

- 已执行：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/sidebar-overflow-regression.test.mjs`
    - 结果：通过
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/sidebar-resize-regression.test.mjs`
    - 结果：通过
- 待确认：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`

### 测试情况

- 已执行：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-multi-section-regression.test.mjs`
    - 结果：通过
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-rich-input-regression.test.mjs`
    - 结果：通过
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-detail-followers-regression.test.mjs`
    - 结果：通过
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/comment-image-preview-regression.test.mjs`
    - 结果：通过
- 待确认：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
