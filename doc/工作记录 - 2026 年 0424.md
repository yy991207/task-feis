# 工作记录 - 2026 年 0424

## 改动背景

这轮按“自定义字段选项 `value → id` 改造”文档，对前端做接口适配和展示链路收口。重点不是只改创建/更新字段的入参，还要把任务列表、动态、详情历史这些显示层一起切到新返回结构，避免出现“保存好了但展示还是旧字段”的半修状态。

## 主要修改点

### 1. 自定义字段服务层改成 `option.id`

- 文件：`src/services/customFieldService.ts`
- 调整点：
  - `FieldOption` 去掉旧的 `value` 字段，只保留 `id / label / color`。
  - 创建字段接口继续按新文档传 `options: [{ label, color }]`。
  - 更新字段接口继续按新文档传 `options: [{ id, label, color }]`。

### 2. 任务模型补了展示态字段

- 文件：
  - `src/services/taskService.ts`
  - `src/types/task.ts`
  - `src/services/projectService.ts`
- 调整点：
  - `ApiTask` 新增 `custom_fields_display`、`priority_option`。
  - `apiTaskToTask` 会把这两个字段一起映射到前端任务对象。
  - `Task` 类型新增可选的 `custom_fields_display`、`priority_option`，这样显示层可以直接消费后端给的 label/color。
  - 项目摘要服务层类型改成数组项结构，和新文档保持一致。

### 3. 任务表格切到新选项字段，并区分“写值”和“显示值”

- 文件：`src/components/TaskTable/index.tsx`
- 调整点：
  - 自定义字段定义映射从 `option.id` 生成前端 `guid`。
  - 字段筛选下拉从旧的 `option.value` 改成 `option.id`。
  - `formatCustomFieldValue` 对 `single_select / multi_select` 优先使用 `task.custom_fields_display` 显示 label。
  - 乐观更新自定义字段值时，同时补本地 `custom_fields_display`，避免保存后短时间显示原始 ID。
  - 优先级乐观更新时同步补 `priority_option`，避免前后展示不一致。

### 4. 字段编辑弹窗不再混用旧的 `value`

- 文件：`src/components/CustomFieldEditorModal/index.tsx`
- 调整点：
  - 新增本地 `DraftOption`，只用于弹窗里临时编辑行的 key。
  - 不再把服务层的 `FieldOption` 直接当成本地草稿结构用，避免把旧 `value` 写法带回接口层。

### 5. 动态和详情历史接住 `*_value_label`

- 文件：
  - `src/components/ActivityView/index.tsx`
  - `src/components/TaskDetailPanel/index.tsx`
- 调整点：
  - 新增活动值展示函数，优先显示 `new_value_label / old_value_label`，没有再回退到原始值。
  - 增加 `task.priority_changed` 的文案显示。
  - `task.custom_field_changed` 和 `task.status_changed` 也统一走新展示值。

## 异常处理手法

- 自定义字段值本地更新时先做乐观更新，接口失败再回滚，避免用户点完后表格没有即时反馈。
- 展示态字段统一做兜底：
  - 有 `custom_fields_display / priority_option` 就优先显示；
  - 没有就回退到原始值和本地选项表，避免接口灰度期间页面直接空掉。

## 涉及模块

- `src/services/customFieldService.ts`
- `src/services/taskService.ts`
- `src/services/projectService.ts`
- `src/types/task.ts`
- `src/components/TaskTable/index.tsx`
- `src/components/CustomFieldEditorModal/index.tsx`
- `src/components/ActivityView/index.tsx`
- `src/components/TaskDetailPanel/index.tsx`
- `scripts/check-custom-field-option-id-migration.mjs`
- `docs/plans/2026-04-24-custom-field-option-id-migration.md`

## 可能影响范围

- 任务列表里的自定义字段筛选、单元格显示、字段编辑弹窗。
- 动态页和任务详情历史里的优先级/自定义字段变更展示。
- 依赖 `Task` 类型的代码如果后面直接读展示态字段，需要优先走可选链。

## 测试情况

### 已通过

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node scripts/check-custom-field-option-id-migration.mjs`
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node scripts/check-lint-effect-reset-regression.mjs`
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node scripts/check-custom-field-columns-use-raw-fields.mjs`
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npx eslint src/components/TaskTable/index.tsx src/components/ActivityView/index.tsx src/components/TaskDetailPanel/index.tsx src/components/CustomFieldEditorModal/index.tsx --quiet`
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npx tsc --noEmit --pretty false`

## 补充修复

后面继续按“修复问题”收了一轮 lint 报错，根因主要不是单行代码写错，而是几个组件长期靠 `effect` 在 props 变化后同步重置本地 state：

- `TaskDetailPanel`：
  - 现在在 [src/pages/TaskList.tsx](/Users/yang/feishu_task/src/pages/TaskList.tsx:448) 按 `selectedTask.guid` 重挂载；
  - 删除了原来那串 `task.guid` 变化时手动清状态的 effect。
- `CustomFieldEditorModal`：
  - 现在在 [src/components/TaskTable/index.tsx](/Users/yang/feishu_task/src/components/TaskTable/index.tsx:4616) 按字段/模式重建；
  - 删除了原来靠 `useEffect` 同步初始化 tab、type、name、options 的逻辑。
- `ActivityView`：
  - `loading` 直接从初始态进入加载，不再在 effect 里同步 `setLoading(true)`。
- `TaskTable`：
  - 把筛选条件修正和可见分组修正从“effect 同步派生”改成初始化函数和明确入口更新；
  - 顺手清了几处未使用变量和一次缩进误改。

### 新增回归文件

- `scripts/check-lint-effect-reset-regression.mjs`

## 补充修复：视图 scope 切到 shared

### 改动背景

后端把视图权限范围统一成了 `shared / personal`。前端这边原来还保留了旧值 `team`，如果继续拿它去调创建视图和修改视图接口，团队/公共视图的请求体就会发错；同时如果列表或详情接口在灰度阶段返回 `team`，页面本地状态里也会混入新旧两套值，后面展示和判断都容易出问题。

### 主要修改点

#### 1. 视图服务层统一收口 scope

## 补充修复：新建任务分组后未进入配置态

### 改动背景

- 这次不是“新建失败”，而是“新建成功后没有留在前端配置输入态”。
- 现场现象是：接口已经返回成功，页面里也能看到新分组行，但输入框没有停住，刷新后新分组已保存。
- 根因是新建按钮点击完成后，输入框刚挂载就被按钮焦点回退打断，`onBlur` 立刻触发，导致重命名态瞬间结束。

### 主要修改点

- 文件：
  - [src/components/TaskTable/index.tsx](/Users/yang/feishu_task/src/components/TaskTable/index.tsx)
  - [src/components/TaskTable/sectionState.ts](/Users/yang/feishu_task/src/components/TaskTable/sectionState.ts)
  - [tests/task-table-section-state.test.ts](/Users/yang/feishu_task/tests/task-table-section-state.test.ts)
- 调整点：
  - 新建任务分组成功后，不再立即 `setEditingSectionGuid(...)`。
  - 先记录一个 `pendingEditingSection`，等创建 loading 结束、分组行稳定挂载后，再用 `setTimeout(..., 0)` 切进输入框。
  - 增加一个纯函数判断，明确“只有创建态结束且分组行已经存在时，才允许进入编辑态”。

### 异常处理手法

- 这是典型的前端焦点时序问题，不是接口异常。
- 修复思路是把“创建成功”和“进入配置输入态”拆成两个时机，避免刚挂载就被 blur 打回普通态。

### 测试情况

- 已执行：
  - `conda activate deepagent && node --experimental-strip-types --test tests/task-table-section-state.test.ts`
- 当前结果：
  - 回归用例 3 条全部通过，覆盖了“新建保留已有分组”“重命名不丢其它分组”“创建态结束后再进入编辑态”。

- 文件：`src/services/viewService.ts`
- 调整点：
  - `ViewScope` 正式改成 `shared | personal`。
  - 新增 `ApiViewScope` 和 `normalizeViewScope`，专门兼容后端灰度阶段可能返回的旧值 `team`。
  - `listViews`、`getView`、`createView`、`updateView` 都统一经过规整，保证组件层拿到的 `scope` 只有 `shared` 或 `personal`。

#### 2. 创建和修改视图时不再透传旧值

- 文件：`src/services/viewService.ts`
- 调整点：
  - `POST /projects/{id}/views` 发送请求前，会把 `scope` 统一规整成 `shared` 或 `personal`。
  - `PUT /views/{view_id}` 只有在调用方显式传了 `scope` 时才做规整，避免“只改名字/filters”的请求被误补成 `personal`。

#### 3. 增加 shared scope 回归测试

- 文件：`tests/view-scope-shared-regression.test.mjs`
- 调整点：
  - 校验前端视图契约已经切到 `shared | personal`。
  - 校验服务层保留了对旧值 `team` 的兼容规整。
  - 校验创建和修改视图时，请求体里的 `scope` 会走统一规整逻辑。

### 异常处理手法

- 这次没有在组件层到处加 `team -> shared` 的分支，而是把兼容逻辑收口到服务层，避免多个入口判断不一致。
- 列表、详情、创建、修改四条链路都统一规整，后端就算还在灰度返回旧值，前端本地状态也只保留新值，后续展示逻辑更稳定。

### 涉及模块

- `src/services/viewService.ts`
- `tests/view-scope-shared-regression.test.mjs`

### 可能影响范围

- 视图创建、编辑时的 `scope` 请求体。
- 视图列表和视图详情返回值在页面内的后续消费。
- 后面如果补“团队/公共视图”显式展示 UI，可以直接按 `shared` 收口，不需要再兼容 `team`。

### 测试情况

#### 已通过

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/view-scope-shared-regression.test.mjs`
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npx tsc --noEmit --pretty false`

## 补充修复：自定义字段删除接口 404

### 改动背景

这轮是顺着你给的 Swagger 文档和前端实际请求一起排的。前端删除自定义字段时还在调旧地址 `DELETE /api/v1/task-center/custom-fields/{field_id}`，但我用 Playwright 打开 `http://192.168.30.238:8000/docs` 并读取 `openapi.json` 后确认，后端现在只有项目级删除接口：

- `DELETE /api/v1/task-center/projects/{project_id}/custom-fields/{field_id}`
- 参数要求：
  - `project_id`：path
  - `field_id`：path
  - `user_id`：query

所以前端现在收到的 `{"detail":"Not Found"}` 不是业务校验失败，就是路由没对上。

### 主要修改点

#### 1. 删除接口改成项目级路径

- 文件：`src/services/customFieldService.ts`
- 调整点：
  - `deleteCustomField` 现在显式接收 `projectId` 和 `fieldId`。
  - 请求路径改成 `api/v1/task-center/projects/${projectId}/custom-fields/${fieldId}?user_id=${uid()}`，和 Swagger 文档保持一致。

#### 2. 两个删除入口都补传 `projectId`

- 文件：
  - `src/components/CustomFieldEditorModal/index.tsx`
  - `src/components/CustomFieldsModal/index.tsx`
- 调整点：
  - 编辑字段弹窗删除时改成 `deleteCustomField(projectId, field.field_id)`。
  - 字段管理弹窗列表删除时改成 `deleteCustomField(projectId, fieldId)`。

#### 3. 补删除接口回归测试，并顺手收口同链路类型

- 文件：
  - `tests/custom-field-update-api-regression.test.mjs`
  - `src/components/CustomFieldEditorModal/index.tsx`
  - `src/components/TaskTable/index.tsx`
- 调整点：
  - 回归测试新增校验：删除接口必须走项目级路径，两个删除调用点都必须传 `projectId`。
  - `DraftOption` 补上可选 `id`，避免编辑已有字段选项时丢失旧 option id。
  - 推荐字段草稿的本地结构改成和编辑弹窗一致的 `key / label / color`，避免这条自定义字段链路继续冒类型错误。

### 异常处理手法

- 这次没有改请求层通用错误处理，只修真实根因：前端路由和 Swagger 文档不一致。
- 删除接口继续保留 `user_id` query 参数，因为文档明确要求它是必填 query。

### 涉及模块

- `src/services/customFieldService.ts`
- `src/components/CustomFieldEditorModal/index.tsx`
- `src/components/CustomFieldsModal/index.tsx`
- `src/components/TaskTable/index.tsx`
- `tests/custom-field-update-api-regression.test.mjs`

### 可能影响范围

- 自定义字段编辑弹窗里的删除操作。
- 自定义字段列表弹窗里的删除操作。
- 推荐字段快捷创建时，传给编辑弹窗的本地草稿结构。

### 测试情况

#### 已通过

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/custom-field-update-api-regression.test.mjs`

#### 未通过 / 现存问题

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
- 当前失败不是这次删除接口改动单独引入的 404 问题，构建里还有项目里已有的 TypeScript 报错，主要在：
  - `src/components/TaskDetailPanel/index.tsx`
  - `src/components/TaskTable/index.tsx`
  - `src/services/projectService.ts`

## 补充修复：侧边栏清单分组三角按钮左对齐

### 改动背景

这轮是修左侧任务清单树的视觉对齐问题。清单分组行默认不显示拖拽图标，但树节点前面还是保留了一段隐藏占位，结果展开/收起三角按钮整体被往右顶了 7px，看起来就比左边“全部任务 / 我创建的”这组菜单图标更靠右，不在同一条竖线上。

### 主要修改点

#### 1. 收窄清单分组行左侧隐藏拖拽占位

- 文件：`src/components/Sidebar/index.less`
- 调整点：
  - 给 `.tree-section > .ant-tree-draggable-icon` 单独收窄宽度。
  - 只动清单分组行，不改普通清单节点的拖拽列宽，避免影响任务清单项的拖拽交互。

#### 2. 增加样式回归测试

- 文件：`tests/sidebar-tree-switcher-alignment-regression.test.mjs`
- 调整点：
  - 校验清单分组行已经把隐藏拖拽占位收窄到目标宽度。
  - 防止后面继续改侧边栏样式时，把这段空白占位又带回来。

### 异常处理手法

- 这次没有去改 Tree 结构和拖拽逻辑，只修真实根因：隐藏拖拽图标还在占宽度。
- 改动范围收在样式层，避免把分组展开、选中、拖拽排序这些行为一起带出回归。

### 涉及模块

- `src/components/Sidebar/index.less`
- `tests/sidebar-tree-switcher-alignment-regression.test.mjs`

### 可能影响范围

- 左侧任务清单树里“任务清单”和各个清单分组的三角按钮横向位置。
- 清单分组行 hover 时的拖拽图标显示宽度。

### 测试情况

#### 已通过

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/sidebar-tree-switcher-alignment-regression.test.mjs`

## 补充修复：空清单分组保留展开按钮

### 改动背景

这轮是修左侧任务清单树的空分组状态。之前分组下面一旦 0 个清单，就直接给 Tree 传了空数组，`antd` 会把这种节点当成叶子节点处理，结果展开/收起三角按钮直接消失。这样既不符合你给的参考样式，也会让“这个分组还能继续接收拖拽清单”这件事表达不出来。

### 主要修改点

#### 1. 空分组改成挂占位子节点

- 文件：`src/components/Sidebar/index.tsx`
- 调整点：
  - 新增空分组占位节点 key 前缀和构造函数。
  - 当分组下没有清单时，不再传 `children: []`，而是回退成一个不可选的占位子节点。
  - 占位子节点文案是“可拖拽清单加入该分组”，这样分组仍然保留三角按钮，展开后也有明确提示。

#### 2. 拖拽规则接住空分组占位行

- 文件：`src/components/Sidebar/index.tsx`
- 调整点：
  - 任务清单拖到空分组提示行时，会正确解析回对应分组 id。
  - 这样空分组不仅能显示展开态，还能继续作为拖拽落点使用。

#### 3. 补空分组提示样式和回归测试

- 文件：
  - `src/components/Sidebar/index.less`
  - `tests/sidebar-empty-group-switcher-regression.test.mjs`
- 调整点：
  - 增加空分组提示行的浅灰文字样式，贴近参考图里的弱提示效果。
  - 回归测试会校验：空分组必须用占位子节点保留 switcher，提示文案存在，占位节点不可选。

### 异常处理手法

- 这次没有改 Tree 的底层渲染方式，只修真实根因：空分组被喂成了空 children。
- 占位节点显式设成 `selectable: false` 和叶子占位，避免它被误当成真实清单参与切换或拖拽。

### 涉及模块

- `src/components/Sidebar/index.tsx`
- `src/components/Sidebar/index.less`
- `tests/sidebar-empty-group-switcher-regression.test.mjs`

### 可能影响范围

- 左侧任务清单树里所有 0 清单分组的展开/收起显示。
- 清单拖入空分组时的落点识别。
- 空分组展开后的提示文案样式。

### 测试情况

#### 已通过

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/sidebar-empty-group-switcher-regression.test.mjs`
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/sidebar-tree-switcher-alignment-regression.test.mjs`

## 补充修复：负责人弹窗双层显示

### 改动背景

这轮是按你给的截图继续查前端样式问题。现象很明确：点开负责人以后，外面先出来一层白色 Popover，里面的人员候选列表又自己再弹一层，最后就成了“双层白框 + 双层阴影”。正确效果应该像你给的参考图，只保留一层负责人面板，搜索框、候选人列表、完成模式和进度都在同一个容器里。

### 根因

- `TaskDetailPanel` 和 `TaskTable` 里的负责人弹窗外层本来就用了 `Popover`。
- 里面复用的 `UserSearchSelect` 组件，默认又会把候选人列表按“浮层”方式绝对定位出来。
- 两层叠在一起后，视觉上就像一个面板里又套了一个面板，所以会出现截图里的双层显示。

### 主要修改点

#### 1. 给 `UserSearchSelect` 增加内嵌列表模式

- 文件：
  - `src/components/UserSearchSelect/index.tsx`
  - `src/components/UserSearchSelect/index.less`
- 调整点：
  - 新增 `optionsVariant?: 'floating' | 'inline'`。
  - 默认还是 `floating`，这样筛选面板等旧场景不受影响。
  - 当传 `inline` 时，候选人列表不再绝对定位，也不再保留第二层边框和阴影，直接作为当前面板的内容渲染。

#### 2. 负责人相关弹窗切到内嵌模式

- 文件：
  - `src/components/TaskTable/index.tsx`
  - `src/components/TaskDetailPanel/index.tsx`
- 调整点：
  - 任务表格里的负责人弹窗改成 `optionsVariant="inline"`。
  - 任务详情里的负责人、关注人、子任务负责人、子任务创建时负责人选择都改成 `optionsVariant="inline"`。
  - 这样这几处都会收成单层面板，样式保持一致。

#### 3. 增加回归测试

- 文件：`tests/user-search-select-inline-regression.test.mjs`
- 调整点：
  - 校验 `UserSearchSelect` 已支持 `floating | inline` 两种模式。
  - 校验 `inline` 模式的样式确实会去掉绝对定位和第二层阴影。
  - 校验任务表格和任务详情的负责人弹窗调用点已经切到 `inline`。

### 异常处理手法

- 这次没有去改 Popover 的结构，也没有把 `UserSearchSelect` 拆成第二套组件，而是只给现有组件补一个场景开关，尽量缩小影响范围。
- 默认模式继续保持原样，避免把筛选面板等原本就依赖浮层列表的地方一起带坏。

### 涉及模块

- `src/components/UserSearchSelect/index.tsx`
- `src/components/UserSearchSelect/index.less`
- `src/components/TaskTable/index.tsx`
- `src/components/TaskDetailPanel/index.tsx`
- `tests/user-search-select-inline-regression.test.mjs`

### 可能影响范围

- 任务表格里的负责人选择弹窗。
- 任务详情里的负责人、关注人、子任务负责人相关弹窗。
- 其它没有显式传 `optionsVariant="inline"` 的 `UserSearchSelect` 场景，仍然走原来的浮层模式。

### 测试情况

#### 已通过

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/user-search-select-inline-regression.test.mjs`
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/user-search-select-collapsed-regression.test.mjs`
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-completion-mode-regression.test.mjs`

#### 浏览器复测

- 用 Chrome 复测 `11111` 这条多人负责人任务。
- 任务详情里点负责人后，现在弹窗已经收成单层，搜索框、完成模式 `全部负责人均需完成` 和进度 `1/3 人已完成` 都在同一个面板里，不再出现双层白框。

## 补充修复：多负责人完成模式和个人完成态对齐

### 改动背景

这轮是按你给的后端新协议，把“多人负责人完成模式”从只看任务顶层 `done/todo`，补成真正区分“任一人完成即可 / 所有人都完成才算完成”，同时把每个负责人自己的完成状态也接到前端。

原来的问题有三层：

- 前端没接 `completion_mode`，负责人弹层里没法展示和切换“全部负责人均需完成 / 任一负责人完成即可”。
- 前端没接 `assignee_completions`，所以负责人头像上没法给已完成的人打勾，也没法显示 `1/2 人已完成`。
- 主表和详情页切状态还是老的“直接 done/todo”，没有把后端 `scope=self/all` 吃进去，所以你截图里的“仅我完成 / 为所有负责人完成 / 重启任务”都对不上。

### 主要修改点

#### 1. 任务类型和服务层补 completion mode 契约

- 文件：
  - `src/types/task.ts`
  - `src/services/taskService.ts`
  - `src/utils/taskCompletion.ts`
- 调整点：
  - `Task` 新增 `completion_mode`、`assignee_completions`。
  - `ApiTask` 新增 `completion_mode`、`assignee_completions` 映射。
  - `createTaskApi`、`updateTaskApi` 支持 `completion_mode`。
  - `patchTaskStatus` 支持 `scope: self | all`。
  - 新增 `taskCompletion` 工具，统一收口“当前人是否已完成、是否管理员、该展示哪些状态动作”。

#### 2. 主表负责人弹层补完成模式和进度

- 文件：
  - `src/components/TaskTable/index.tsx`
  - `src/components/TaskTable/index.less`
- 调整点：
  - 负责人弹层底部补了完成模式下拉：
    - `全部负责人均需完成`
    - `任一负责人完成即可`
  - 多负责人任务会显示 `x/y 人已完成`。
  - 已完成负责人的头像右下角补绿色对号。
  - 多负责人新建任务默认带 `completion_mode: any`。

#### 3. 主表和详情页完成入口切到 scope 感知

- 文件：
  - `src/components/TaskTable/index.tsx`
  - `src/components/TaskDetailPanel/index.tsx`
  - `src/components/TaskDetailPanel/index.less`
- 调整点：
  - 主表任务行 checkbox 不再一律直接切状态。
  - 当当前用户同时是“管理员 + 负责人”且任务有多个负责人时，点击会弹出范围菜单：
    - 未完成自己时：`仅我完成` / `为所有负责人完成`
    - 已完成自己时：`重启任务` / `为所有负责人完成`
  - 详情页主任务和子任务都同步用了同一套规则。
  - 详情页负责人区域也补了完成模式切换、进度文案和头像对号。

### 异常处理手法

- 状态切换继续沿用接口返回值回刷，避免前端自己推演顶层完成态，把 `completion_mode=all` 算错。
- 完成模式切换只改当前任务，不顺手动别的字段，尽量把回归范围压在负责人和完成状态链路。
- 管理员 / 普通负责人 / 非负责人这三种身份的最终生效范围仍以服务端为准，前端只做菜单引导和 `scope` 透传。

### 涉及模块

- `src/types/task.ts`
- `src/services/taskService.ts`
- `src/utils/taskCompletion.ts`
- `src/components/TaskTable/index.tsx`
- `src/components/TaskTable/index.less`
- `src/components/TaskDetailPanel/index.tsx`
- `src/components/TaskDetailPanel/index.less`
- `tests/task-completion-mode-regression.test.mjs`

### 可能影响范围

- 主表任务行和详情页里“点击勾选完成”的交互。
- 负责人弹层的底部区域，现在线上会多出完成模式和已完成进度。
- 多负责人子任务在详情页里的勾选菜单和头像状态。

### 测试情况

#### 已通过

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-completion-mode-regression.test.mjs`
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npx tsc --noEmit --pretty false`

## 补充修复：详情页子任务负责人触发器同步主页面完成角标逻辑

### 改动背景

你这轮指出的是详情页里的“子任务负责人配置弹窗”虽然内容结构已经参考主页面负责人弹窗对齐了，但子任务负责人触发器本身还没同步主页面/主任务那套头像完成态显示逻辑。

现象就是：

- 子任务负责人弹窗已经有“添加负责人 + 完成模式 + 已完成进度”；
- 但子任务负责人按钮上的头像，还只是普通头像组；
- 已完成状态没有像主页面和主任务那样，挂在头像右下角绿色对号。

### 根因

- 子任务负责人弹窗内容本身已经接了 `detail-assignee-completion-config` 这些样式；
- 真正漏掉的是触发器头像结构：
  - 之前直接 `renderUserAvatar(...)`
  - 没包 `detail-assignee-avatar-wrap`
  - 也没渲染 `detail-assignee-completed-badge`
- 所以子任务负责人按钮无法复用主任务那套完成角标样式。

### 主要修改点

#### 1. 子任务负责人触发器头像结构改成和主任务一致

- 文件：`src/components/TaskDetailPanel/index.tsx`
- 调整点：
  - 子任务负责人按钮里的每个头像外层都补上 `detail-assignee-avatar-wrap`。
  - 已完成负责人会补 `is-completed` class。
  - 已完成负责人头像右下角补 `detail-assignee-completed-badge`，内部继续用 `CheckOutlined`。

#### 2. 回归测试加严，直接锁住子任务负责人触发器

- 文件：`tests/assignee-picker-reference-style-regression.test.mjs`
- 调整点：
  - 先精确定位 `detail-subtask-assignee-popover` 这段局部代码块。
  - 新增断言：子任务负责人触发器必须带 `detail-assignee-avatar-wrap`。
  - 新增断言：子任务负责人触发器里的已完成头像必须带 `detail-assignee-completed-badge`。

### 异常处理手法

- 这次没有动子任务负责人弹窗内容区，也没有改选人接口。
- 只补“子任务负责人按钮上的已完成头像显示”这一层，控制改动范围。

### 涉及模块

- `src/components/TaskDetailPanel/index.tsx`
- `tests/assignee-picker-reference-style-regression.test.mjs`

### 可能影响范围

- 详情页子任务列表里的负责人按钮
- 子任务负责人的已完成头像显示

### 测试情况

#### 已通过

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/assignee-picker-reference-style-regression.test.mjs`
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npx tsc --noEmit --pretty false`

## 补充修复：新建子任务负责人入口同步主页面负责人弹窗外观

### 改动背景

你最后这轮截图说明，之前我同步到的是“已有子任务负责人弹窗”，但你实际点到的入口是“新建子任务”输入框右侧那个负责人按钮。

这套入口原来还是旧逻辑：

- 没挂 `detail-subtask-assignee-popover`；
- 还是旧的小面板；
- 输入框文案还是 `选择负责人`；
- 所以你看到的仍然是图 1，而不是主页面那种图 2。

### 主要修改点

#### 1. 新建子任务负责人入口挂同一套 overlay class

- 文件：`src/components/TaskDetailPanel/index.tsx`
- 调整点：
  - 给“新建子任务”负责人 Popover 补 `overlayClassName="detail-subtask-assignee-popover"`。
  - 让它直接吃现有子任务负责人弹窗的样式规则。

#### 2. 新建子任务负责人面板切到同一套容器

- 文件：`src/components/TaskDetailPanel/index.tsx`
- 调整点：
  - 旧的 `style={{ width: 200 }}` 小面板改成：
    `detail-popover-panel detail-popover-panel-assignee detail-popover-panel-subtask-assignee`
  - 这样宽度、留白、边框和主页面/子任务负责人弹窗都统一。

#### 3. 输入框文案同步成主页面

- 文件：`src/components/TaskDetailPanel/index.tsx`
- 调整点：
  - placeholder 从 `选择负责人` 改成 `添加负责人`。
  - 避免同一个详情页里，不同负责人入口出现两套文案。

#### 4. 回归测试补到“新建子任务”入口

- 文件：`tests/assignee-picker-reference-style-regression.test.mjs`
- 调整点：
  - 新增专门针对“新建子任务负责人入口”的校验。
  - 要求它也必须：
    - 挂 `detail-subtask-assignee-popover`
    - 使用 `detail-popover-panel-assignee detail-popover-panel-subtask-assignee`
    - placeholder 为 `添加负责人`

### 异常处理手法

- 这次没有改接口和保存逻辑，只同步了弹窗容器、overlay class 和文案。
- 先把你截图里命中的旧入口收口到主页面同一套外观，避免继续出现“一半新一半旧”的状态。

### 涉及模块

- `src/components/TaskDetailPanel/index.tsx`
- `tests/assignee-picker-reference-style-regression.test.mjs`

### 可能影响范围

- 详情页“新建子任务”输入框右侧负责人按钮
- 新建子任务时的负责人选择弹窗外观

### 测试情况

#### 已通过

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/assignee-picker-reference-style-regression.test.mjs`
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npx tsc --noEmit --pretty false`

## 补充修复：主表和详情页负责人完成角标样式统一

### 改动背景

你这轮补的截图把问题钉得很清楚：主表任务列表里的负责人完成显示，和任务详情页里的完成显示还是不一样。

表面现象像是：

- 详情页里是“头像右下角绿色圆形对号”；
- 主表列表里却更像“头像旁边多了一个单独的勾”。

继续往下排以后确认，问题不在数据，也不在 DOM：

- 两边都有 completed badge 节点；
- 真正不一致的是样式命中范围。

### 根因

- 主表负责人完成角标的样式，之前有一部分被写在了 `.task-title-cell` 这种局部作用域里。
- 但真正的负责人列不是 title cell，所以主表列表列会吃不到这套角标定位样式。
- 结果就是主表里的 badge 退化成普通行内勾图标，看起来和详情页不一致。

### 主要修改点

#### 1. 把负责人完成角标样式提成主表公共样式

- 文件：`src/components/TaskTable/index.less`
- 调整点：
  - 把这三套样式提到顶层公共作用域：
    - `.tasklist-assignee-avatar`
    - `.tasklist-assignee-avatar-wrap`
    - `.tasklist-assignee-completed-badge`
  - 这样主表负责人列、分组头像、其它复用这套 class 的地方，都会命中同一套样式。

#### 2. 删除原来锁在局部作用域里的重复定义

- 文件：`src/components/TaskTable/index.less`
- 调整点：
  - 删除 `.section-row-content` 和 `.task-title-cell` 里那几段重复的 completed badge 样式。
  - 避免后面又出现“某一块生效、某一块不生效”的分叉。

#### 3. 回归测试改成卡真正的根因

- 文件：`tests/task-completion-mode-regression.test.mjs`
- 调整点：
  - 新增断言：主表负责人完成角标样式必须有顶层公共定义。
  - 新增断言：不能继续把这套样式锁在 `.task-title-cell` 私有作用域里。

### 异常处理手法

- 这次没有动 TSX 结构，也没有继续碰详情页逻辑。
- 只修主表 completed badge 的样式作用域，让主表和详情页真正共用同一套视觉规则。

### 涉及模块

- `src/components/TaskTable/index.less`
- `tests/task-completion-mode-regression.test.mjs`

### 可能影响范围

- 主表任务列表负责人头像
- 分组头像里复用这套 `tasklist-assignee-*` class 的区域
- 多负责人任务的已完成角标视觉表现

### 测试情况

#### 已通过

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-completion-mode-regression.test.mjs`
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npx tsc --noEmit --pretty false`

## 补充修复：主表负责人列漏同步已完成头像角标

### 改动背景

上一轮把“已完成负责人头像右下角绿色对号”补上以后，详情页能看到，但主表任务列表里的负责人列还没同步出来。

这次根因不是接口没回，也不是主表没有渲染 badge 节点，而是：

- 主表负责人列的 DOM 已经有 `tasklist-assignee-avatar-wrap` 和 `tasklist-assignee-completed-badge`；
- 但真正作用在主表负责人列上的 `.cell-assignee` 样式作用域里，没有把这套 completed badge 样式带进去；
- 结果就是详情页生效，主表列表列看起来像“没改”。

### 主要修改点

#### 1. 给主表负责人列补齐 completed badge 样式作用域

- 文件：`src/components/TaskTable/index.less`
- 调整点：
  - 在 `.cell-assignee` 作用域里补上：
    - `.tasklist-assignee-avatar-wrap`
    - `.tasklist-assignee-avatar-wrap.is-completed`
    - `.tasklist-assignee-completed-badge`
  - 让主表负责人列自己就能正确吃到：
    - 头像右下角绿色对号
    - 已完成头像提层
    - 白边和阴影

#### 2. 回归测试加严，直接卡住这类“节点有了但样式没进作用域”的问题

- 文件：`tests/task-completion-mode-regression.test.mjs`
- 调整点：
  - 新增断言：主表负责人列自己的 `.cell-assignee` 样式作用域里，必须包含 completed badge 相关定义。
  - 后面如果再出现“详情页有、主表没”的情况，脚本会直接失败。

### 异常处理手法

- 这次没有继续改 DOM，也没有去碰详情页逻辑。
- 只修主表负责人列样式作用域缺失这一层，保持改动最小。

### 涉及模块

- `src/components/TaskTable/index.less`
- `tests/task-completion-mode-regression.test.mjs`

### 可能影响范围

- 主表任务列表的负责人头像显示
- 多负责人任务在主表里的“仅我完成”完成态角标

### 测试情况

#### 已通过

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-completion-mode-regression.test.mjs`
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npx tsc --noEmit --pretty false`

## 补充修复：已完成负责人头像改成右下角绿色对号

### 改动背景

你补的参考图很明确：当点“仅我完成”以后，已完成的负责人头像不该只是在旁边放一个普通小图标，而是要像参考图那样，直接挂在头像右下角一个绿色圆形对号角标。

这个场景在多人头像重叠时还多一个细节：

- 如果只把绿色角标画出来，不处理头像层级；
- 后一个头像很容易把前一个头像的右下角角标盖住；
- 最后视觉上就会变成“有时有，有时被吃掉”。

### 主要修改点

#### 1. 主表负责人头像补已完成态 class

- 文件：`src/components/TaskTable/index.tsx`
- 调整点：
  - 已完成负责人头像外层 `tasklist-assignee-avatar-wrap` 增加 `is-completed` class。
  - 保持原来“右下角挂角标”的 DOM 结构不变，只补状态 class，缩小改动范围。

#### 2. 详情页负责人头像同步补 class

- 文件：`src/components/TaskDetailPanel/index.tsx`
- 调整点：
  - 详情页负责人头像外层 `detail-assignee-avatar-wrap` 也补 `is-completed` class。
  - 这样主表和详情页都走同一套“头像右下角绿色对号”规则。

#### 3. 角标样式收口成参考图效果

- 文件：
  - `src/components/TaskTable/index.less`
  - `src/components/TaskDetailPanel/index.less`
- 调整点：
  - 已完成头像外层提 `z-index`，避免重叠头像时绿色角标被旁边头像盖住。
  - 绿色角标位置收紧到头像右下角：`right: -3px`、`bottom: -2px`。
  - 角标边框改成 `2px solid #fff`，更像贴在头像上的独立圆点。
  - 补轻微阴影，让绿色对号在浅背景和头像边缘上更清晰。

#### 4. 增加回归测试

- 文件：`tests/task-completion-mode-regression.test.mjs`
- 调整点：
  - 校验主表和详情页已完成头像都加了 `is-completed` class。
  - 校验两处已完成头像都会提层。
  - 校验绿色角标仍然是右下角绿色圆点，并保留白边。

### 异常处理手法

- 这次没有改完成状态判断逻辑，只改“已完成头像怎么表现”这一层样式。
- 头像 DOM 没重构，只补 class 和样式，尽量避免把负责人列表别的交互带坏。

### 涉及模块

- `src/components/TaskTable/index.tsx`
- `src/components/TaskTable/index.less`
- `src/components/TaskDetailPanel/index.tsx`
- `src/components/TaskDetailPanel/index.less`
- `tests/task-completion-mode-regression.test.mjs`

### 可能影响范围

- 主表负责人头像
- 详情页负责人头像
- 多负责人任务里“仅我完成”后的头像完成态显示

### 测试情况

#### 已通过

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-completion-mode-regression.test.mjs`
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npx tsc --noEmit --pretty false`

#### 未通过

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npx eslint src/components/TaskTable/index.tsx src/components/TaskDetailPanel/index.tsx src/services/taskService.ts src/types/task.ts src/utils/taskCompletion.ts --quiet`
- 结果：
  - 只剩 `src/components/TaskTable/index.tsx` 里原本就存在的两条 `react-hooks/set-state-in-effect` 规则报错；
  - 分别在筛选条件和可见分组同步的旧 `useEffect` 上，这次没有继续扩大范围去重构它们。

## 补充修复：TaskDetailPanel 运行时报错和 Tooltip 废弃警告

### 改动背景

上一轮把多负责人完成模式接进详情页后，`TaskDetailPanel` 里有一处声明顺序没摆对：

- `taskStatusActions = getTaskCompletionActions(task, teamMembers)` 提前用了 `teamMembers`
- 但 `const [teamMembers, setTeamMembers] = useState([])` 放在后面

这会直接触发 `Cannot access 'teamMembers' before initialization`，导致详情页整块挂掉。另外，这次新增的完成入口 Tooltip 还沿用了 `overlayInnerStyle`，会在 antd 6 下持续打废弃警告。

### 主要修改点

- 文件：
  - `src/components/TaskDetailPanel/index.tsx`
  - `src/components/TaskTable/index.tsx`
  - `tests/task-completion-mode-regression.test.mjs`
- 调整点：
  - 把 `TaskDetailPanel` 里的 `availableUsers / teamMembers` state 提前到所有使用点之前，消掉暂时性死区。
  - 把主表和详情页这次新增的 Tooltip 从 `overlayInnerStyle` 改成 `styles={{ body: { color: '#fff' } }}`。
  - 回归测试补了两条约束：
    - `teamMembers` 必须先声明再使用；
    - `TaskTable` 和 `TaskDetailPanel` 里不能再出现 `overlayInnerStyle`。

### 异常处理手法

- 这次只修运行时崩溃和控制台警告，不扩改完成模式本身的业务逻辑。
- Tooltip 只替换本轮新增和当前文件里命中的废弃属性，不顺手大范围扫别的组件，避免扩大回归范围。

### 测试情况

#### 已通过

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-completion-mode-regression.test.mjs`
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npx tsc --noEmit --pretty false`
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && rg -n "overlayInnerStyle" src/components/TaskTable/index.tsx src/components/TaskDetailPanel/index.tsx`

## 补充修复：隐藏侧边栏头部两个入口

### 改动背景

你给的截图里，左侧“任务”标题右边有两个不想展示的 UI：

- 身份切换入口
- 团队管理入口

这轮目标很简单，就是把这两个头部按钮隐藏掉，同时保留通知铃铛，不去扩大到别的导航和任务页逻辑。

### 主要修改点

#### 1. 侧边栏头部移除两个入口

- 文件：`src/components/Sidebar/index.tsx`
- 调整点：
  - 删除头部的 `UserSwitcher` 渲染。
  - 删除“团队管理”按钮和对应点击跳转。
  - 保留 `NotificationBell`，避免把通知入口一起误删。

#### 2. 新增回归测试，防止后面又加回来

- 文件：`tests/sidebar-header-actions-regression.test.mjs`
- 调整点：
  - 校验侧边栏源码里不再出现 `UserSwitcher`。
  - 校验不再出现“团队管理”按钮文案。
  - 校验通知入口 `NotificationBell` 仍然保留。

### 异常处理手法

- 这次没有去改侧边栏布局结构，也没有顺手动别的菜单项，只做头部入口级别的最小删除。
- 保留通知入口，避免用户以为“右上角全没了”是误伤。

### 涉及模块

- `src/components/Sidebar/index.tsx`
- `tests/sidebar-header-actions-regression.test.mjs`

### 可能影响范围

- 左侧侧边栏头部右侧操作区。
- “settings” 入口少了头部快捷按钮，但如果别处还有入口，这次不受影响。

### 测试情况

#### 已通过

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/sidebar-header-actions-regression.test.mjs`
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npx eslint src/components/Sidebar/index.tsx tests/sidebar-header-actions-regression.test.mjs --quiet`

#### 未通过 / 现存问题

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
- 这条失败不是这次隐藏头部按钮引入的，当前仓库里本来就有其他 TypeScript 问题，主要在：
  - `src/components/TaskDetailPanel/index.tsx`
  - `src/components/TaskTable/index.tsx`
  - `src/services/projectService.ts`
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run lint`
- 这条失败也不是这次改动单独引入的，当前仓库里还有一批历史 `react-hooks/set-state-in-effect`、`react-hooks/refs`、`react-refresh/only-export-components` 相关报错。

## 补充修复：负责人/关注人内嵌选人面板固定展开

### 改动背景

你后面补的截图说明了两个更细的交互问题：

- 负责人弹窗右侧还有一个下拉开关图标，不符合目标样式；
- 点“全部负责人均需完成”后，人员列表会消失，弹窗变成半展开状态；
- 关注人弹窗也希望和负责人一样，打开后固定显示人员列表，不要再出现可收起的下拉层。

这轮目标就是把负责人和关注人现在用到的内嵌选人面板改成“默认常开、无开关、列表常驻”，同时不影响其他还在用浮层模式的选人场景。

### 主要修改点

#### 1. `UserSearchSelect` 的 `inline` 模式改成固定展开

- 文件：`src/components/UserSearchSelect/index.tsx`
- 调整点：
  - 新增 `isInlineAlwaysOpen` 分支，只有 `optionsVariant="inline"` 时才固定展开。
  - `inline` 模式下，列表始终渲染，不再依赖输入框聚焦或点击后才出现。
  - `inline` 模式下，隐藏输入框右侧上下箭头，不再暴露“可展开/收起”的错误暗示。
  - `inline` 模式下，点击外部区域和按 `Escape` 不再只关闭内部列表，弹窗开关统一交给外层 `Popover` 控制。

#### 2. 内嵌面板样式收口

- 文件：`src/components/UserSearchSelect/index.less`
- 调整点：
  - `inline` 模式下去掉 suffix 的占位影响，避免右侧看起来还有多余空白。
  - 保持单层面板样式，不再出现“输入框 + 内部伪下拉框”的错觉。

#### 3. 回归测试补强

- 文件：
  - `tests/user-search-select-inline-regression.test.mjs`
  - `tests/user-search-select-collapsed-regression.test.mjs`
- 调整点：
  - 新增约束：`inline` 模式必须固定展开、隐藏开关、不能被点外部或 `Escape` 单独折叠。
  - 同时保留旧约束：默认 `floating` 模式仍然是按需展开，避免把筛选等老场景一起改坏。

### 异常处理手法

- 这次只收口 `UserSearchSelect` 的 `inline` 分支，不去重构负责人和关注人的上层 `Popover` 结构，控制改动范围。
- 弹窗关闭职责仍然放在外层容器，避免内外层都在管开关，继续出现“点模式按钮把人员列表打断”的问题。

### 涉及模块

- `src/components/UserSearchSelect/index.tsx`
- `src/components/UserSearchSelect/index.less`
- `tests/user-search-select-inline-regression.test.mjs`
- `tests/user-search-select-collapsed-regression.test.mjs`

### 可能影响范围

- 负责人弹窗
- 关注人弹窗
- 所有显式传了 `optionsVariant="inline"` 的选人面板

默认浮层选人场景不受影响。

### 测试情况

#### 已通过

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/user-search-select-inline-regression.test.mjs`
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/user-search-select-collapsed-regression.test.mjs`
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-completion-mode-regression.test.mjs`
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npx tsc --noEmit --pretty false`

## 补充修复：负责人完成模式底部条样式对齐参考图

### 改动背景

你最新补的对比图说明，负责人弹窗底部的“完成模式 + 完成进度”虽然功能对了，但样式还不对：

- 现在的底部条会被挤成上下两层；
- 左边模式文案看起来像一个额外的小块，不够贴近参考图；
- 目标效果应该是单行底栏，左侧模式说明，右侧蓝色进度。

这轮只收口这个底部条的布局和字号，不动完成模式逻辑。

### 主要修改点

#### 1. 主表负责人弹窗底部条改成单行紧凑布局

- 文件：`src/components/TaskTable/index.less`
- 调整点：
  - 给 `.assignee-completion-config` 增加 `flex-wrap: nowrap`，避免底部条被压成两层。
  - 收紧上下 padding 和分隔线颜色，让底部条更像参考图里的单行底栏。
  - 给 `.assignee-completion-mode-btn` 增加 `white-space: nowrap`、更小的横向内边距和更明确的字号。
  - 给右侧进度文案加固定单行和稍强一点的字重，避免视觉上发虚。

#### 2. 详情页负责人弹窗同步同款样式

- 文件：`src/components/TaskDetailPanel/index.less`
- 调整点：
  - `detail` 侧同样改成单行底栏，和主表格保持一致。
  - 左侧模式按钮和右侧进度文案统一字号、换行策略和箭头颜色。

#### 3. 回归测试补一条样式约束

- 文件：`tests/task-completion-mode-regression.test.mjs`
- 调整点：
  - 新增约束：主表和详情页底部模式条都必须 `nowrap`。
  - 新增约束：模式按钮文案必须强制单行，避免后续样式回退后再次出现图 1 那种断行块。

### 异常处理手法

- 这次只改样式层，不改 `Dropdown`、按钮点击和状态切换逻辑。
- 主表和详情页共用同一套视觉规则，避免一个入口修好了，另一个入口还是旧样式。

### 涉及模块

- `src/components/TaskTable/index.less`
- `src/components/TaskDetailPanel/index.less`
- `tests/task-completion-mode-regression.test.mjs`

### 可能影响范围

- 主表负责人弹窗底部完成模式条
- 详情页负责人弹窗底部完成模式条

### 测试情况

#### 已通过

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-completion-mode-regression.test.mjs`
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/user-search-select-inline-regression.test.mjs`
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npx tsc --noEmit --pretty false`

## 补充修复：多负责人任务勾选不发请求

### 改动背景

这轮不是后端返回了但前端没渲染，而是多负责人任务在主表和详情页里，点击左侧勾选框时根本没有把状态动作菜单打开，所以也就没有后续的状态请求。

现象和你截图一致：

- 点击多负责人任务左侧 checkbox；
- 页面没有弹出“仅我完成 / 为所有负责人完成”这类范围菜单；
- Network 面板里也没有对应的状态请求。

### 根因

- 多负责人场景下，主表和详情页都走了 `Dropdown + Checkbox` 这一层组合。
- 之前这里完全依赖 `Dropdown` 的默认 `click` 触发。
- 但实际点在 checkbox 上时，这层组合没有稳定把菜单打开，所以动作菜单没出来，请求自然也不会发。

### 主要修改点

#### 1. 主表勾选入口改成显式控制菜单开关

- 文件：`src/components/TaskTable/index.tsx`
- 调整点：
  - `TaskTitleCell` 新增本地 `statusMenuOpen`。
  - 多负责人场景下，`Dropdown` 改成受控开关。
  - 点击 checkbox 时不再赌默认触发，而是直接切换 `statusMenuOpen`，确保范围菜单一定能打开。
  - 选中菜单项后主动关闭菜单，再继续走原来的 `patchTaskStatus(..., { scope })`。

#### 2. 详情页主任务勾选入口同步修正

- 文件：`src/components/TaskDetailPanel/index.tsx`
- 调整点：
  - 新增本地 `taskStatusMenuOpen`。
  - 详情页主任务在多负责人场景下，同样改成受控 `Dropdown`。
  - 点击 checkbox 时直接切换菜单开关，避免“点了没反应、也没有请求”。

#### 3. 补回归测试，防止后面再退回静默失效

- 文件：`tests/task-completion-mode-regression.test.mjs`
- 调整点：
  - 新增断言：主表多负责人勾选入口必须显式管理 `Dropdown` 开关。
  - 新增断言：详情页主任务多负责人勾选入口也必须显式管理 `Dropdown` 开关。
  - 这样后面如果又改回“只靠默认 click 触发”，脚本会直接失败。

### 异常处理手法

- 这次没有去改状态接口，也没有改完成模式本身的业务判断。
- 只收口“点击勾选后菜单没打开”这一层交互触发，尽量把影响范围压在主表和详情页的多负责人入口。

### 涉及模块

- `src/components/TaskTable/index.tsx`
- `src/components/TaskDetailPanel/index.tsx`
- `tests/task-completion-mode-regression.test.mjs`

### 可能影响范围

- 主表里多负责人任务左侧的完成勾选入口。
- 详情页主任务的完成勾选入口。
- 单负责人任务原来的直接勾选完成逻辑不受影响。

### 测试情况

#### 已通过

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-completion-mode-regression.test.mjs`
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npx tsc --noEmit --pretty false`

## 补充修复：负责人选人面板按参考图复刻

### 改动背景

你这轮给了两张对比图，目标不是继续“小修一下”，而是把负责人选人弹窗直接收成参考图那种样子：

- 面板整体更宽，底部“完成模式 + 进度”必须稳定保持单行；
- 输入框文案要改成“添加负责人”，不再显示搜索图标；
- 人员列表项需要更高、更圆，头像更大，选中态改成更浅的蓝底；
- 主表和详情页两处负责人入口都要统一，不允许一处像参考图、一处还是旧样式。

### 主要修改点

#### 1. 主表负责人弹窗直接按参考图尺寸收口

- 文件：`src/components/TaskTable/index.tsx`
- 调整点：
  - 负责人弹窗宽度从 `220` 调整到 `332`。
  - 输入框 placeholder 改成 `添加负责人`。

#### 2. `UserSearchSelect` 的 `inline` 模式改成参考图风格

- 文件：
  - `src/components/UserSearchSelect/index.tsx`
  - `src/components/UserSearchSelect/index.less`
- 调整点：
  - `inline` 模式下，候选项头像改成 32px。
  - 输入框高度、圆角、边框颜色按参考图收紧。
  - 隐藏搜索图标，避免和参考图不一致。
  - 列表项改成更高、更圆的单行样式。
  - 选中态背景改成更浅的蓝色，勾选图标和文案颜色同步收口。

#### 3. 详情页负责人弹窗同步同款面板

- 文件：
  - `src/components/TaskDetailPanel/index.tsx`
  - `src/components/TaskDetailPanel/index.less`
- 调整点：
  - 详情页负责人弹窗新增 `detail-popover-panel-assignee`，宽度同步到 `332px`。
  - 输入框 placeholder 同步改成 `添加负责人`。
  - 底部完成模式条的左右留白、字号、单行布局和主表保持一致。

#### 4. 增加参考图样式回归测试

- 文件：`tests/assignee-picker-reference-style-regression.test.mjs`
- 调整点：
  - 校验主表负责人弹窗宽度已经切到 `332`。
  - 校验主表和详情页输入框文案已经改成 `添加负责人`。
  - 校验 `inline` 模式下输入框、列表项、头像、选中态已切到参考图样式。
  - 校验主表和详情页底部完成模式条的留白和字号已同步。

### 异常处理手法

- 这次只改负责人相关的 `inline` 模式，不碰筛选面板等还在用浮层模式的老场景。
- 详情页只给负责人弹窗单独补 class，没有把关注人和子任务选人一起强行放大，避免无关入口被一并改形。

### 涉及模块

- `src/components/UserSearchSelect/index.tsx`
- `src/components/UserSearchSelect/index.less`
- `src/components/TaskTable/index.tsx`
- `src/components/TaskTable/index.less`
- `src/components/TaskDetailPanel/index.tsx`
- `src/components/TaskDetailPanel/index.less`
- `tests/assignee-picker-reference-style-regression.test.mjs`

### 可能影响范围

- 主表负责人弹窗
- 详情页负责人弹窗
- 所有显式传了 `optionsVariant="inline"` 的选人列表样式

默认浮层模式不受影响。

### 测试情况

#### 已通过

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/assignee-picker-reference-style-regression.test.mjs`
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/user-search-select-inline-regression.test.mjs`
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/user-search-select-collapsed-regression.test.mjs`
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-completion-mode-regression.test.mjs`

## 补充修复：负责人蓝色完成文案命中和子任务负责人配置对齐

### 改动背景

这轮继续往下排时确认了两个真实问题：

- 主表负责人弹窗右下角“x/y 人已完成”虽然源码里写了蓝色，但实际弹层是挂到 `body`，原来的样式又写在表格容器作用域下面，所以页面上根本没吃到这条蓝色样式，看起来还是黑字；
- 任务详情里的子任务负责人弹窗还停在旧版本：面板更窄、文案还是旧的，也没有把完成模式和主任务那套配置一起接进去；负责人更新请求也还是单独走 `patchTaskAssignee`，没和主任务对齐。

### 主要修改点

#### 1. 主任务负责人弹窗改成真正可命中的弹层样式

- 文件：
  - `src/components/TaskTable/index.tsx`
  - `src/components/TaskTable/index.less`
  - `src/components/TaskDetailPanel/index.tsx`
  - `src/components/TaskDetailPanel/index.less`
- 调整点：
  - 主表负责人弹窗补了 `overlayClassName="task-assignee-popover"`。
  - 详情页主任务负责人弹窗补了 `overlayClassName="detail-assignee-popover"`。
  - 底部完成模式条和右下角蓝色完成文案的样式，从原来容易失效的容器作用域，收口到真正会命中的 Popover 外层 class 下面。

#### 2. 子任务负责人弹窗切到和主任务同款配置

- 文件：
  - `src/components/TaskDetailPanel/index.tsx`
  - `src/components/TaskDetailPanel/index.less`
- 调整点：
  - 子任务负责人弹窗补了 `overlayClassName="detail-subtask-assignee-popover"`。
  - 子任务负责人弹窗容器改成和主任务同款的 `detail-popover-panel detail-popover-panel-assignee`，宽度同步到 `332px`。
  - 输入框 placeholder 改成 `添加负责人`。
  - 底部补上完成模式切换和 `x/y 人已完成` 进度文案，布局和主任务一致。

#### 3. 子任务负责人更新请求和主任务对齐

- 文件：`src/components/TaskDetailPanel/index.tsx`
- 调整点：
  - 新增 `handleSubtaskCompletionModeChange`，单独处理子任务完成模式切换。
  - 子任务负责人更新不再只调 `patchTaskAssignee`，改成 `updateTaskApi(subtask.guid, { assignee_ids, completion_mode })`，一次把负责人和完成模式一起提交，和主任务配置链路保持一致。

### 异常处理手法

- 这次没有继续在原来的容器作用域里堆 `!important`，而是把样式挂到真实渲染的 Popover 外层 class，直接修根因。
- 子任务负责人链路也没有新起第二套组件，还是沿用现有负责人弹窗结构，只把缺的完成模式和接口收口补齐，尽量控制改动面。

### 涉及模块

- `src/components/TaskTable/index.tsx`
- `src/components/TaskTable/index.less`
- `src/components/TaskDetailPanel/index.tsx`
- `src/components/TaskDetailPanel/index.less`
- `tests/assignee-picker-reference-style-regression.test.mjs`

### 可能影响范围

- 主表负责人弹窗底部完成模式和右下角完成进度文案
- 详情页主任务负责人弹窗
- 详情页子任务负责人弹窗
- 子任务负责人和完成模式更新请求

### 测试情况

#### 已通过

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/assignee-picker-reference-style-regression.test.mjs`
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-completion-mode-regression.test.mjs`
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/user-search-select-inline-regression.test.mjs`
- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npx tsc --noEmit --pretty false`
## 任务分组新建时页面分组被清空问题修复

### 改动背景
- 任务清单页面在点击“新建任务分组”后，页面上原来已经加载出来的分组会先消失，只剩下刚创建的分组输入框。
- 后端新建和保存逻辑本身没问题，问题出在前端本地分组状态和父层异步返回的分组数据没有对齐，导致新建时拿旧的空本地状态做了追加。

### 主要修改点
- 在 [src/components/TaskTable/index.tsx](/Users/yang/feishu_task/src/components/TaskTable/index.tsx) 调整任务分组的新建和重命名入口：
  - 改成基于当前页面正在展示的 `sectionSource` 做增量更新。
  - 给新建入口补了中文注释，说明这里为什么不能直接拿初始本地状态追加，避免后面维护时误删。
- 抽出 [src/components/TaskTable/sectionState.ts](/Users/yang/feishu_task/src/components/TaskTable/sectionState.ts)：
  - 统一处理分组同步、新建追加、重命名这几个纯状态变更。
  - 这样可以把问题收敛在一个很小的边界里，避免再把页面渲染和状态拼接混在一起。
- 新建分组和重命名分组都改成基于当前页面实际分组源做增量更新，避免异步时序把旧分组冲掉。

### 异常处理手法
- 这次不是接口异常，是典型的“异步分组数据已经展示出来了，但本地编辑状态还停留在初始空数组”的状态覆盖问题。
- 修复方式是让新建、重命名都直接基于当前页面分组源计算，避免异步时序把已有分组冲掉。

### 涉及模块
- [src/components/TaskTable/index.tsx](/Users/yang/feishu_task/src/components/TaskTable/index.tsx)
- [src/components/TaskTable/sectionState.ts](/Users/yang/feishu_task/src/components/TaskTable/sectionState.ts)
- [tests/task-table-section-state.test.ts](/Users/yang/feishu_task/tests/task-table-section-state.test.ts)

### 可能影响范围
- 任务清单页面里的任务分组新建、重命名、异步加载展示。
- 不影响后端分组创建接口，也不影响任务本身的新建保存逻辑。

### 测试情况
- 已执行：
  - `conda activate deepagent && node --experimental-strip-types --test tests/task-table-section-state.test.ts`
- 当前结果：
  - 回归用例 2 条全部通过，覆盖了“新建保留已有分组”“重命名不丢其它分组”。
- 补充验证：
  - 已执行 `conda activate deepagent && npm run build`
  - 已执行 `conda activate deepagent && npm run lint`

## 任务详情分组弹层首屏精简

### 改动背景
- 这次先处理任务详情页里“任务清单 | 任务分组”弹层的首屏展示。
- 你标出来的“已添加任务分组”标题和下面那排已选标签，会让弹层顶部显得挤，而且和下面真正可选的分组列表信息重复。
- 这一轮先按最小改动收口，先把这块重复内容删掉，后续再继续做整体美化。

### 主要修改点
- 在 [src/components/TaskDetailPanel/index.tsx](/Users/yang/feishu_task/src/components/TaskDetailPanel/index.tsx) 里：
  - 删除弹层顶部的 `已添加任务分组` 标题。
  - 删除当前已选分组的标签区和标签上的移除按钮。
  - 保留下面的搜索框和候选分组列表，用户还是从这里切换分组。
  - 触发器上显示的当前分组文案，改成直接基于当前任务分组引用计算，避免删掉标签区后丢展示。
- 在 [src/components/TaskDetailPanel/index.less](/Users/yang/feishu_task/src/components/TaskDetailPanel/index.less) 里：
  - 清掉标题区、标签区、标签按钮这几组废样式。
  - 给弹层主体保留更紧凑的 `gap`，让顶部结构更干净。
- 新增 [scripts/check-task-detail-section-popover-cleanup.mjs](/Users/yang/feishu_task/scripts/check-task-detail-section-popover-cleanup.mjs)：
  - 用静态检查卡住这几个旧节点和样式，避免后面回归。

### 异常处理手法
- 这次不是接口问题，也不是状态同步 bug，重点是把重复展示层剥掉。
- 改动时没有去碰分组加载、切换接口和弹层开关状态，只删掉顶部重复 UI，尽量把影响面收在当前组件。

### 涉及模块
- [src/components/TaskDetailPanel/index.tsx](/Users/yang/feishu_task/src/components/TaskDetailPanel/index.tsx)
- [src/components/TaskDetailPanel/index.less](/Users/yang/feishu_task/src/components/TaskDetailPanel/index.less)
- [scripts/check-task-detail-section-popover-cleanup.mjs](/Users/yang/feishu_task/scripts/check-task-detail-section-popover-cleanup.mjs)

### 可能影响范围
- 任务详情页里任务分组弹层的顶部结构和交互入口。
- 不影响任务分组切换接口、不影响任务列表页分组、不影响别的详情区块。

### 测试情况
- 已通过：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node scripts/check-task-detail-section-popover-cleanup.mjs`
- 构建校验：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 当前未通过，但报错来自仓库里现有的 TypeScript 问题，包括 `Tooltip styles.body`、`FieldOption.value`、`ProjectSummaryDistributionItem` 等，和这次弹层精简改动无关。

## 任务详情分组弹层宽度收紧

### 改动背景
- 你继续要求把任务详情里这个分组弹层再收窄一点，参考你红框里的宽度。
- 当前弹层虽然已经去掉了顶部重复内容，但整体还是偏宽，和详情面板左侧这块信息密度不太匹配。

### 主要修改点
- 在 [src/components/TaskDetailPanel/index.less](/Users/yang/feishu_task/src/components/TaskDetailPanel/index.less) 里：
  - 把 `.tasklist-section-panel` 从原来的 `260-320px` 收紧到 `188-204px`。
  - 给 `.tasklist-section-search-list` 补上 `width: 100%`，让列表容器始终跟着窄弹层铺满，避免外层变窄后内层还保持旧宽度。
- 新增 [scripts/check-task-detail-section-popover-width.mjs](/Users/yang/feishu_task/scripts/check-task-detail-section-popover-width.mjs)：
  - 用静态检查卡住弹层宽度范围和列表容器铺满规则，避免后面样式被带回去。

### 异常处理手法
- 这次只是样式比例调整，没有碰组件结构、接口状态和弹层交互。
- 宽度只收在任务分组弹层自己的 class 上，避免误伤别的 `Popover`。

### 涉及模块
- [src/components/TaskDetailPanel/index.less](/Users/yang/feishu_task/src/components/TaskDetailPanel/index.less)
- [scripts/check-task-detail-section-popover-width.mjs](/Users/yang/feishu_task/scripts/check-task-detail-section-popover-width.mjs)

### 可能影响范围
- 任务详情页里任务分组弹层的展示宽度。
- 不影响负责人弹层、不影响详情页其它浮层。

### 测试情况
- 已通过：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node scripts/check-task-detail-section-popover-width.mjs`
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node scripts/check-task-detail-section-popover-cleanup.mjs`

## 左侧任务清单图标替换

### 改动背景
- 你要求把左侧任务清单前面的蓝点换掉，参考另一张图里更像“清单/任务单”的图标样式。
- 当前蓝点太弱，信息语义不够明显，看起来更像状态点，不像可点击的任务清单入口。

### 主要修改点
- 在 [src/components/Sidebar/index.tsx](/Users/yang/feishu_task/src/components/Sidebar/index.tsx) 里：
  - 把任务清单项和重命名态前面的 `Badge` 蓝点替换成 Ant Design 的 `FileDoneOutlined` 图标。
  - 这样左侧清单入口的语义会更接近“任务单/清单”，也更贴近你给的参考图方向。
- 在 [src/components/Sidebar/index.less](/Users/yang/feishu_task/src/components/Sidebar/index.less) 里：
  - 删除原来的 `.ant-badge-status-dot` 圆点样式。
  - 新增 `.tasklist-icon` 样式，控制图标大小、灰度和激活态颜色跟随。
- 新增 [scripts/check-sidebar-tasklist-icon.mjs](/Users/yang/feishu_task/scripts/check-sidebar-tasklist-icon.mjs)：
  - 用静态检查卡住左侧任务清单不能再回退成蓝点，同时要求保留新的清单图标和图标样式。

### 异常处理手法
- 这次只替换图标和对应样式，不碰任务清单点击、重命名、拖拽和菜单逻辑。
- 激活态图标颜色直接跟 `currentColor`，避免单独维护两套颜色状态。

### 涉及模块
- [src/components/Sidebar/index.tsx](/Users/yang/feishu_task/src/components/Sidebar/index.tsx)
- [src/components/Sidebar/index.less](/Users/yang/feishu_task/src/components/Sidebar/index.less)
- [scripts/check-sidebar-tasklist-icon.mjs](/Users/yang/feishu_task/scripts/check-sidebar-tasklist-icon.mjs)

### 可能影响范围
- 左侧边栏任务清单节点的视觉展示。
- 不影响左侧分组节点、菜单行为和任务清单交互逻辑。

### 测试情况
- 已通过：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node scripts/check-sidebar-tasklist-icon.mjs`

## 任务描述区粘贴图片撑爆详情页修复

### 改动背景
- 任务详情页的描述区在粘贴图片后，会把内嵌图片按原始尺寸直接渲染出来，导致详情面板内容区被整张大图撑开。
- 评论区没有这个问题，因为评论里的图片走的是附件缩略图卡片，不会把正文布局顶坏。
- 这次目标就是参考评论区的处理方式，把描述区内嵌图片也收敛成安全尺寸，同时保留点图预览能力。

### 主要修改点
- 在 [src/components/TaskDetailPanel/index.less](/Users/yang/feishu_task/src/components/TaskDetailPanel/index.less) 里：
  - 给 `.detail-description-text img` 补了固定缩略图样式，统一限制成 `160 x 108`，并加上 `max-width: 100%`、圆角、描边和 `object-fit: cover`。
  - 这样描述里即使贴进大图，也只会按缩略图展示，不会再把详情面板撑爆。
- 在 [src/components/TaskRichInput/index.less](/Users/yang/feishu_task/src/components/TaskRichInput/index.less) 里：
  - 给编辑态 `.task-rich-input-editor img` 也补了同样的尺寸约束。
  - 这样用户刚粘贴图片时，编辑器本身也不会先被原图瞬间撑开。
- 在 [src/components/TaskDetailPanel/index.tsx](/Users/yang/feishu_task/src/components/TaskDetailPanel/index.tsx) 里：
  - 描述展示态点击事件里先识别 `img[data-attachment-id]`，命中后直接复用现有附件预览弹窗。
  - 没点到链接和图片时，仍然保持原来的“点击进入编辑态”行为。
- 新增 [tests/task-description-image-regression.test.mjs](/Users/yang/feishu_task/tests/task-description-image-regression.test.mjs)：
  - 用静态回归测试卡住描述区图片约束和点图预览逻辑，避免后面样式或点击行为再退回去。

### 异常处理手法
- 这次没有改接口、没有改附件上传协议，也没有改评论区逻辑，只在描述区展示层做最小修复。
- 描述区点图预览直接复用现有附件弹窗和附件列表映射，避免再造一套预览状态。

### 涉及模块
- [src/components/TaskDetailPanel/index.tsx](/Users/yang/feishu_task/src/components/TaskDetailPanel/index.tsx)
- [src/components/TaskDetailPanel/index.less](/Users/yang/feishu_task/src/components/TaskDetailPanel/index.less)
- [src/components/TaskRichInput/index.less](/Users/yang/feishu_task/src/components/TaskRichInput/index.less)
- [tests/task-description-image-regression.test.mjs](/Users/yang/feishu_task/tests/task-description-image-regression.test.mjs)

### 可能影响范围
- 任务详情页描述区的内嵌图片展示和点击行为。
- 评论区附件缩略图逻辑不受影响。
- 任务附件上传、下载、预览接口不受影响。

### 测试情况
- 已通过：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && node tests/task-description-image-regression.test.mjs`
- 构建校验：
  - `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 当前未通过，但失败项来自仓库现有 TypeScript 问题，包括 `Tooltip styles.body`、`FieldOption.value`、`UserSearchSelect mode`、`ProjectSummaryDistributionItem`，和这次描述图片区修复无关。

## 补充修复：任务详情子任务区交互和样式收口

### 改动背景

这轮连续收的是任务详情右侧面板里子任务区的几个细节问题：

- “添加子任务”默认态不该一直带阴影底，只有鼠标移上去才该出现。
- 子任务标题既然是点击后进入编辑态，鼠标就不该再显示成小手。
- 子任务截止时间图标 hover 时，背景范围不该被固定宽度撑长。
- 只要当前已经有子任务，“子任务”这三个字就该切成进度头，显示“已完成 / 全部”的状态，而不是继续保留静态标题。

这几处都是同一块 UI 的细节收口，所以这次统一在 `TaskDetailPanel` 里一起修。

### 主要修改点

#### 1. 子任务标题支持单击进入行内编辑

- 文件：
  - `src/components/TaskDetailPanel/index.tsx`
  - `src/components/TaskDetailPanel/index.less`
- 处理内容：
  - 给子任务标题补了单独的编辑状态和提交流程。
  - 点击子任务标题直接切输入框，`Enter` 保存，`Esc` 取消，失焦自动保存。
  - 子任务标题按钮的鼠标样式改成 `cursor: text`。
- 原因：
  - 这里的语义是“可直接改名”，不是跳详情，所以鼠标反馈和行为都要按输入入口来。

#### 2. “添加子任务”默认态改成无底色，hover 才显示阴影

- 文件：
  - `src/components/TaskDetailPanel/index.less`
- 处理内容：
  - 默认态去掉背景和阴影。
  - 只有 hover 时才显示浅灰底和轻阴影。
  - 宽度改成 `fit-content`，阴影只包住“添加子任务”这段内容，不再铺满整行。
- 原因：
  - 参考图里这块默认是轻的，只有鼠标滑上去才有卡片感。

#### 3. 子任务截止时间 hover 背景宽度收紧

- 文件：
  - `src/components/TaskDetailPanel/index.less`
- 处理内容：
  - 去掉子任务截止时间按钮原来的 `min-width: 90px`。
  - 改成按内容宽度自适应，hover 背景只跟当前图标或文案宽度走。
- 原因：
  - 之前 hover 背景过长，不是视觉稿要求，而是固定最小宽度把按钮撑大了。

#### 4. 子任务区标题改成进度头

- 文件：
  - `src/components/TaskDetailPanel/index.tsx`
  - `src/components/TaskDetailPanel/index.less`
- 处理内容：
  - 只要当前存在子任务，就用“已完成数量 / 总数 + 进度条”替换原来的“子任务”文案。
  - 进度条颜色按完成情况变化：有完成项时用绿色，没有完成项时保持中性灰。
  - 原来临时挂在列表区里的创建预览进度头已删掉，统一收口到标题行。
- 原因：
  - 这块应该先表达当前子任务完成进度，而不是在有数据时还保留一个静态标题。

### 异常处理手法

- 这轮没有动子任务创建接口、状态变更接口、截止时间保存接口和负责人接口。
- 所有修改都收在 `TaskDetailPanel` 当前这块 UI 里，没有扩大到任务表格和侧边栏。
- 顺手把中途废弃掉的进度预览变量和残留样式一起删掉，避免留死代码。

### 涉及模块

- `src/components/TaskDetailPanel/index.tsx`
- `src/components/TaskDetailPanel/index.less`

### 可能影响范围

- 任务详情右侧面板里的子任务标题区
- 子任务标题行内编辑体验
- 子任务截止时间图标 hover 反馈
- “添加子任务”入口的默认态和 hover 态

### 测试情况

#### 已执行

- 本轮主要做的是详情面板样式和交互收口，已完成静态代码回读确认：
  - 子任务标题点击只进入编辑态
  - 右侧箭头仍然保留“查看详情”
  - 子任务进度头已挪到标题行
  - 截止时间 hover 背景宽度已按内容收紧

#### 当前未重新执行

- `source $(conda info --base)/etc/profile.d/conda.sh && conda activate deepagent && npm run build`
  - 说明：仓库当前仍有既有 TypeScript 报错，之前已确认包括 `Tooltip styles.body`、`FieldOption.value`、`UserSearchSelect` 类型比较、`ProjectSummaryDistributionItem` 缺失等问题，这轮没有继续重复触发同样的全量构建。
